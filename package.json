{
  "name": "qhse-platform",
  "private": true,
  "version": "0.1.0",
  "description": "QHSE Enterprise Platform — monorepo (TDD §4.1). Lihat ../TASK_INSTRUCTION.md Phase 0 untuk urutan kerja.",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@11.16.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:integration": "turbo run test:integration"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  }
}
