# Task 1.7 — base URL apps/api dipanggil dari browser (lib/api-client.ts).
# Default fallback (http://localhost:3001) sudah cocok utk dev lokal biasa
# (apps/api/.env.example PORT=3001) — file .env cuma perlu dibuat kalau
# nilainya beda dari default itu.
NEXT_PUBLIC_API_URL="http://localhost:3001"
