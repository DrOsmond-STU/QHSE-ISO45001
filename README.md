# qhse-platform

Monorepo implementasi **QHSE Enterprise Platform**, mengikuti struktur [TDD §4.1](../tdd/00-MASTER-TDD.md#41-keputusan-monorepo).

Dokumen desain & keputusan produk/teknis ada satu level di atas folder ini:
[`../prd/`](../prd/00-MASTER-PRD.md) · [`../tdd/`](../tdd/00-MASTER-TDD.md) · [`../SECURITY.md`](../SECURITY.md) · [`../DESIGN.md`](../DESIGN.md) · [`../ARCHITECTURE.md`](../ARCHITECTURE.md) · [`../BRAND.md`](../BRAND.md) · [`../TESTING.md`](../TESTING.md) · [`../DEPLOYMENT.md`](../DEPLOYMENT.md) · [`../TASK_INSTRUCTION.md`](../TASK_INSTRUCTION.md)

## Struktur

```
apps/
  api/      NestJS backend (REST API)
  web/      Next.js frontend
  worker/   Job consumer (BullMQ), reuse module code dari api
packages/
  shared-types/   DTO & tipe TypeScript dipakai bareng FE/BE
  ui-components/  Design system component library
  i18n/           Kamus label i18n_labels ID/EN
  eslint-config/  Konfigurasi lint & boundary rules bersama
infra/            IaC (Terraform/k8s manifests, disi belakangan)
```

## Status

Dibangun bertahap mengikuti `../TASK_INSTRUCTION.md` Phase 0 (Bootstrap & Shared Platform Layer) — cek task tracker untuk progres per-task.

- [x] 0.1 Monorepo (pnpm + Turborepo) — `turbo build` sukses 6/6 package.
- [x] 0.2 Database, ORM & tenant context — PostgreSQL 16 lokal (portable, TANPA Docker/admin — lihat `apps/api/scripts/dev-db-setup.sh`), Prisma 5.22, `TenantContextMiddleware` (AsyncLocalStorage) + `PrismaService.withRls()` (SET LOCAL per transaksi) terverifikasi end-to-end lewat endpoint `GET /platform/tenancy-smoke-test`.
- [x] 0.3 RLS policy template & generic test harness — `prisma/rls-policy.template.sql` + `test/rls/generic-tenant-isolation.integration-spec.ts` (Jest), introspeksi `information_schema` otomatis menemukan tabel domain baru. 4/4 test lulus (`pnpm test:integration`).
- [x] 0.4 Pipeline CI/CD — `.github/workflows/ci.yml` (lint→build→migrate→integration test, pakai Postgres service container) dan `deploy.yml` (build image→staging→approval gate→production). **Belum pernah benar-benar trigger** (belum ada remote GitHub) — YAML sudah divalidasi sintaksnya lokal; `deploy.yml` masih berisi placeholder untuk registry/cluster/GitHub Environment yang belum dikonfigurasi.
- [x] 0.5 Design tokens & komponen UI inti — `packages/ui-components` (StatusBadge 4-tone icon+label wajib, DataTable dense, FormField, KpiCard, WorkflowStepper, Button tap-target 44px) + token DESIGN §3-5 di `tokens.css`. Diverifikasi: `tsc` bersih, grep membuktikan nol hex/px hardcode di luar `tokens.css`, dan render sungguhan di `apps/web/app/design-system` (dev server + Browser pane — CSS ter-bundle benar via `@qhse/ui-components/styles.css`).
- [x] 0.6 Autentikasi OIDC + sesi — `apps/api/src/platform/auth/*`: Authorization Code Flow + PKCE (S256), JWT access token 15 menit, refresh token rotasi wajib dengan deteksi reuse → revoke seluruh sesi, sesi di Redis (`RedisSessionStore`, key `session:{user_id}:{session_id}`) untuk revocation langsung, `JwtAuthGuard` global (fail-closed, `@Public()` untuk opt-out), lockout akun (Postgres, durable) + rate limit login (Redis). Model `users`/`user_sessions`/`password_policies` (subset auth-critical, expand di task 1.3 Modul 02). Redis lokal portable (`tporadowski/redis`, `.local-redis/` satu level di atas repo ini) — lihat `apps/api/scripts/dev-redis-setup.sh`. Diverifikasi: 18 unit test + 20 integration test lulus (`pnpm test` & `pnpm test:integration`), termasuk kedua acceptance criteria (reuse detection cascade & logout-all-device) via test otomatis dan manual curl walkthrough end-to-end.
- [x] 0.7 MFA (TOTP) — `apps/api/src/platform/auth/mfa.service.ts`: TOTP Google Authenticator-compatible (`otplib`), `mfa_secret_encrypted` AES-256-GCM at-rest, endpoint `/auth/mfa/setup` + `/auth/mfa/confirm` (enrollment mandiri, protected). Login (`LoginDto.totpCode`) menolak sysadmin tanpa MFA ter-enroll (`403 MFA_SETUP_REQUIRED`) dan mewajibkan kode valid kalau `mfaEnabled=true` (`401 MFA_REQUIRED`). Cek sysadmin awalnya pakai placeholder `users.isSysadmin` — **sudah dipensiunkan task 0.8**, diganti cek role `SUPER_ADMIN_PLATFORM` sungguhan. Bootstrap sysadmin pertama lewat `prisma/seed-auth-dev.ts` (di luar API — enrollment API butuh sesi valid dulu, sysadmin pertama belum punya). Diverifikasi: 6 unit test (TOTP generate/verify, enkripsi) + 7 integration test lulus, termasuk acceptance criterion (`login sysadmin.* tanpa MFA ditolak`) via test otomatis dan manual curl.
- [x] 0.8 RBAC Engine — `apps/api/src/platform/rbac/*`: `PermissionService` (resolusi `user_roles`→`role_permissions`→cek scope), guard `@RequirePermission` (kedua `APP_GUARD`, additive/no-op tanpa decorator), permission cache Redis (`perm:{user_id}`, TTL 5 menit) dengan invalidasi event-driven (`@nestjs/event-emitter`, event `role.changed`/`permission.changed` — belum ada producer, ditunda task 1.3), fail-closed murni (Redis cache down → resolve DB; DB down → `503`, bukan bypass — diverifikasi via DI fault-injection **dan** mematikan Postgres/Redis sungguhan secara manual). Data-scoping primitive `getScopedIds()` (deny-all eksplisit kalau assignment kosong, bukan `WHERE x IN ()`). Model `roles`/`permissions`/`permission_groups`/`role_permissions`/`user_roles` (subset auth-critical, expand di task 1.3 Modul 02) — kebijakan RLS asimetris untuk `roles`/`role_permissions` (tenant_id nullable, baris system role terlihat lintas tenant). Placeholder `users.isSysadmin` (task 0.7) dipensiunkan, diganti role sistem `SUPER_ADMIN_PLATFORM`. Proof-of-concept `GET /rbac/permissions` digated permission `user_mgmt.permission.manage` (Modul 02 §3). Diverifikasi: 82/82 test lulus seluruh proyek (`pnpm test` & `pnpm test:integration`, termasuk 4 suite/29 test baru khusus RBAC) + manual curl fail-closed dengan Postgres/Redis sungguhan dimatikan.
- [x] 0.9 Workflow Engine — core — `apps/api/src/platform/workflow-engine/*`: `WorkflowEngineService` (`startInstance`/`actOnTask`/`evaluateTransition`, signature persis TDD §9, tenant selalu ambient lewat `tenantContextStorage` — tidak ada parameter tenantId eksplisit), kondisi transisi lewat JSON Logic (`json-logic-js`, satu titik import di `json-logic.evaluator.ts` — tidak pernah `eval()`), approval paralel `ALL_APPROVE`/`ANY_ONE_APPROVE` (REJECT mana pun langsung gagalkan grup), idempotency `actOnTask` via row lock (`SELECT ... FOR UPDATE`, `FOR UPDATE` pertama di codebase ini) di dalam transaksi RLS yang sama. Job `workflow-sla-scan` (BullMQ repeatable tiap 15 menit) — cross-tenant job PERTAMA (bootstrap admin-role read-only, lalu per-tenant RLS penuh). `apps/worker` sekarang punya consumer sungguhan: `NestFactory.createApplicationContext()` + deep import compiled `@qhse/api` (workspace dep, `declaration:true` di `apps/api/tsconfig.json` — .d.ts asli, bukan ambient shim) — reuse `WorkflowSlaScanService` yang SAMA persis, nol duplikasi logic. `ApproverResolutionService` sebagai seam task 1.4 (Delegation) — `ROLE_IN_SCOPE` pakai `UserRole` task 0.8, `REPORTING_LINE` throw eksplisit (belum ada data HRIS). Model `workflow_definitions/stages/transitions/instances/tasks` (`parallel_completion_rule` & `context_data` ditambah beyond PRD literal, gap didokumentasikan TDD §9/§26). Diverifikasi: 127/127 test lulus seluruh proyek (13 test baru workflow-engine mencakup 6 skenario TESTING §9 + wiring BullMQ), `pnpm build` sukses lintas `@qhse/api`→`@qhse/worker`, dan manual end-to-end dengan proses worker BullMQ sungguhan (bukan mock) — job di-enqueue dari luar, worker nyata memprosesnya, `escalatedAt` task ter-update di DB.
- [x] 0.10 Numbering Service — `apps/api/src/platform/numbering/*`: `NumberingService.generateNext(moduleCode, { scopeId?, variables? })` — tenant selalu ambient lewat `tenantContextStorage` (tidak ada parameter tenantId eksplisit, pola sama `WorkflowEngineService` 0.9; contoh kode TESTING §6 `generateNext('WORK_PERMIT', tenantId)` diperlakukan pseudo-code ilustratif, bukan kontrak signature), atomic via row lock (`SELECT ... FOR UPDATE` dengan `IS NOT DISTINCT FROM` untuk scope nullable) di transaksi RLS yang sama, pola persis `actOnTask` 0.9. Model `numbering_configs` (Master PRD §10) ditambah dua kolom beyond literal — `scopeId` (UUID mentah tanpa FK, pola sama `UserRole.scopeId` 0.8) dan `lastPeriodKey` (`"2026"`/`"2026-07"`/`NULL`) — supaya `scope_level` SITE/COMPANY dan `reset_period` YEARLY/MONTHLY benar-benar berfungsi; tanpa itu satu counter akan dibagi semua site/company/periode sekaligus dan menghasilkan nomor bentrok, persis yang seharusnya dicegah row lock (gap didokumentasikan TDD §26 poin 8, pola sama `parallel_completion_rule` 0.9). Partial unique index tambahan (`WHERE scope_id IS NULL`) menutup celah Postgres yang tidak men-dedup NULL di composite unique constraint biasa. Pattern renderer (`numbering-pattern.ts`, pure function, unit-tested terpisah) resolve `{PREFIX}`/`{YYYY}`/`{MM}`/`{QUARTER}`/`{YYYYMMDD}`/`{SEQ:n}` sendiri; token lain (mis. `{SITE_CODE}` — Modul 01 belum ada Phase 0) disuplai caller lewat `variables`, gagal eksplisit (bukan diam-diam biarkan literal `{TOKEN}`) kalau tidak disuplai. Authoring config (CRUD `numbering_configs`) BUKAN cakupan task ini — itu Modul 31 (System Administration); baris config di-seed langsung lewat Prisma di test, pola sama `workflow_definitions` 0.9 yang juga belum punya "builder service". Diverifikasi: 10 unit test (`numbering-pattern.spec.ts`) + 11 integration test baru (konkurensi 10-submission TESTING §6, isolasi tenant saat konkuren, partisi counter per scope, rollover YEARLY vs NEVER, config hilang, RLS fail-closed) — 148/148 test lulus seluruh proyek (`pnpm test`), `pnpm build` sukses lintas `@qhse/api`→`@qhse/worker`, generic RLS suite (0.3) otomatis mendeteksi `numbering_configs` tanpa perubahan kode di sana.
- [x] 0.11 Notification Service — infra — `apps/api/src/platform/notification/*` + `apps/worker/src/notification.worker.ts`: `NotificationService.enqueue(event)` (tenant ambient, pola sama 0.9/0.10) — IN_APP dibuat SINKRON (BR-02 Modul 25, tidak lewat queue karena tidak ada "provider" eksternal yang bisa gagal untuk cuma insert row), kanal tambahan (EMAIL/WHATSAPP/TELEGRAM) diresolusi `notification-channel-resolution.ts` (BR-01 CRITICAL bypass quiet hours, BR-03 tenant-disable menang mutlak atas segalanya termasuk CRITICAL — interpretasi BR-01 didokumentasikan TDD §26 karena rule aslinya tidak eksplisit soal urutan menang lawan preferensi user) lalu di-push ke BullMQ `notification-queue` SETELAH transaksi DB commit (fire-and-forget, NFR §11 Modul 25). Template engine Handlebars (`notification-template.ts`) dengan whitelist ketat — token tak dikenal/tidak disuplai `variables` gagal eksplisit, HTML auto-escape utk data user-generated. Fallback resolusi template 2 tingkat: tenant-specific → default sistem (`tenantId NULL`, kebijakan RLS asimetris pola `roles`/`role_permissions` 0.8). Provider adapter (`NotificationProvider`: `sendEmail`/`sendWhatsApp`/`sendTelegram`/`sendInApp`) — implementasi default (`StubNotificationProvider`) SEMUA channel eksternal throw eksplisit "belum dikonfigurasi" (Modul 30/API Integration belum dikerjakan; `sendInApp` selalu sukses trivial, baris `notifications` itu sendiri sudah jadi delivery-nya) — dipakai SEBAGAI skenario realistis membuktikan acceptance criterion (retry → dead-letter) tanpa pernah mengirim pesan sungguhan. Retry exponential backoff max 5x (`NOTIFICATION_MAX_ATTEMPTS`, TDD §10 & task instruction — **konflik dgn PRD Modul 25 BR-04 yang sebut 3x, didokumentasikan TDD §26**, belum direkonsiliasi); percobaan TERAKHIR gagal → `notification_logs` `FAILED` (ditulis `NotificationDeliveryService`, single writer) DAN job masuk queue terpisah `notification-dead-letter-queue` (`worker.on('failed')` di `apps/worker`, pola alert placeholder sama `workflow-sla-scan.worker.ts` 0.9 — Alertmanager sungguhan belum ada, task 0.13 observability belum dikerjakan). File worker flat `notification.worker.ts` (bukan `consumers/notification.consumer.ts` yang disebut TASK_INSTRUCTION.md) — mengikuti konvensi `apps/worker/src/*.worker.ts` yang sudah dipakai 0.9, sesuai forward-reference comment yang sudah ditulis di `apps/worker/src/index.ts` sejak task 0.9. Model `notifications/notification_channels/notification_templates/notification_preferences/notification_logs` (Modul 25 §5) — `notification_preferences` & `notification_logs` ditambah `tenant_id` BEYOND literal PRD (draft PRD tidak mencantumkannya sama sekali di kedua tabel itu, kemungkinan oversight — WAJIB ada demi RLS, Master PRD §11 poin 2 & TASK_INSTRUCTION §1 prinsip 3; gap didokumentasikan TDD §26); partial unique index tambahan utk `notification_templates`/`notification_channels` (pola sama `numbering_configs` 0.10, Postgres tidak men-dedup NULL). `users.language` belum ada kolomnya (menyusul task 1.3 Modul 02) — `NotificationService` terima `language` opsional, default `"ID"`. Mode `DIGEST_DAILY`/`DIGEST_WEEKLY` (§4.2 Modul 25) SENGAJA belum diimplementasikan — butuh job batching terjadwal terpisah yang tidak disebut task 0.11, kolom `delivery_mode` sudah ada di skema tapi baru `REAL_TIME` yang benar-benar dihormati. Inbox in-app & halaman preferensi UI BUKAN cakupan task ini — itu task 1.7. Diverifikasi: 19 unit test (`notification-template.spec.ts`, `notification-channel-resolution.spec.ts`, `stub-notification.provider.spec.ts`) + 17 integration test baru (producer wiring termasuk isolasi tenant konkuren, dead-letter/FAILED via akumulasi percobaan nyata, quiet hours bypass CRITICAL, RLS asimetris template sistem) — 184/184 test lulus seluruh proyek (`pnpm test`), `pnpm build` sukses lintas `@qhse/api`→`@qhse/worker`, generic RLS suite (0.3) otomatis mendeteksi ke-5 tabel baru, dan manual end-to-end dengan proses worker BullMQ sungguhan (bukan mock) — job di-enqueue dari luar, worker nyata memprosesnya, `notification_logs` berakhir `FAILED` + job sungguhan muncul di `notification-dead-letter-queue`.

- [x] 0.12 Attachment & Object Storage Service — `apps/api/src/platform/attachment/*` + `apps/worker/src/attachment-scan.worker.ts`: endpoint HTTP PERTAMA di `platform/*` (`POST /attachments/presign`, `POST /attachments/confirm`, `GET /attachments/:id/download`) — numbering (0.10)/notification (0.11) murni in-process, task ini beda karena browser klien upload LANGSUNG ke object storage (TDD §11, menghindari bottleneck backend). `presign()` STATELESS (tidak nulis DB, cuma hitung storage key + sign PUT URL); `confirm()` HEAD storage OTORITATIF (size/mime SUNGGUHAN, bukan klaim client saat presign — dibuktikan test eksplisit) baru insert baris `attachments` (`scanStatus=PENDING_SCAN`) + enqueue `attachment-scan`. Object storage: **MinIO portable sungguhan** (`.local-minio/`, binary resmi `dl.min.io`, download atas izin eksplisit user — pola sama Postgres/Redis) via `@aws-sdk/client-s3` (`forcePathStyle:true`); bucket create-if-not-exists via kode (`ObjectStorageService.onModuleInit()`), bukan `mc.exe` terpisah. Antivirus scan: `MalwareScanner` adapter (pola provider adapter sama Notification 0.11) — stub default (`StubMalwareScanner`) deteksi **EICAR test string sungguhan** (standar industri eicar.org, bukan marker palsu — ClamAV asli JUGA mendeteksi string yang sama, jadi kontrak test tetap valid begitu diganti implementasi nyata) alih-alih selalu "clean", supaya jalur INFECTED (acceptance criterion) teruji tanpa malware sungguhan; ClamAV sungguhan ditunda (gap TDD §26 — `freshclam` database ~200MB+ di luar cakupan disetujui). Thumbnail: `sharp` sungguhan (resize nyata dibuktikan dimensi hasil), hanya untuk MIME image, upload ke storage terpisah. **Acceptance criterion dibuktikan langsung**: `getDownloadUrl()` cek `scanStatus===CLEAN` sebelum generate presigned GET URL (di-generate ON DEMAND, tidak pernah disimpan) — `PENDING_SCAN`/`INFECTED` → `ForbiddenException`, tidak ada URL diterbitkan sama sekali. Model `attachments` (Master PRD §11.5 + `scan_status` TDD §11) — kolom `file_url`/`thumbnail_url` PERSIS nama literal PRD tapi isinya storage KEY bukan URL publik (URL presigned time-limited, disimpan sebagai key baru digenerate on demand saat dibutuhkan). **Dua temuan signifikan selama development** (keduanya didokumentasikan TDD §26): (1) antivirus lokal (kemungkinan Windows Defender) meng-quarantine file berisi EICAR begitu MinIO menulisnya ke disk — GET berikutnya ECONNRESET bukan bug kode (dibuktikan lewat psql/reproduksi manual); jalur test INFECTED karena itu pakai DI `overrideProvider(MALWARE_SCANNER)` (scanner palsu), bukan EICAR sungguhan lewat storage. (2) **FK constraint Postgres TIDAK menghormati RLS pada tabel yang direferensikan** (dibuktikan empiris via psql: `INSERT` dgn `uploaded_by` user tenant lain BERHASIL walau `SELECT` biasa terhadap user itu 0 baris di RLS yang sama) — `confirm()` karena itu memvalidasi `uploadedBy` eksplisit lewat `findUnique` (yang TETAP tunduk RLS) sebelum insert, bukan mengandalkan FK constraint semata; berpotensi relevan untuk modul lain yang FK ke tabel ber-RLS dari caller kurang trusted. Diverifikasi: 20 test baru (5 unit `stub-malware-scanner.spec.ts` + 15 integration: presign/confirm penuh terhadap MinIO sungguhan, validasi MIME/size otoritatif, scan pipeline CLEAN+thumbnail nyata & INFECTED via DI override, akses unduh terblokir utk PENDING_SCAN/INFECTED, isolasi tenant RLS) — 204/204 test lulus seluruh proyek (`pnpm test`), `pnpm build` sukses lintas `@qhse/api`→`@qhse/worker`, generic RLS suite (0.3) otomatis mendeteksi `attachments`, dan manual end-to-end dengan proses worker BullMQ sungguhan (bukan mock) — job attachment-scan di-enqueue dari luar, worker nyata mendownload dari MinIO+scan+update DB, `scanStatus` berakhir `CLEAN`.

- [x] 0.13 Audit Log & Observability — `apps/api/src/platform/audit-log/*` + `apps/api/src/platform/observability/*` + `apps/worker/src/audit-log-partition-maintenance.worker.ts`: `system_audit_logs` (Master PRD §11.6) dipartisi bulanan (`PARTITION BY RANGE (created_at)`, TDD §6.1), PK komposit `(log_id, created_at)` (wajib Postgres utk tabel partisi). **Observer global append-only DIIMPLEMENTASI SEBAGAI TRIGGER POSTGRES** (`audit_log_capture()`, `AFTER INSERT OR UPDATE OR DELETE ... FOR EACH ROW`, `SECURITY DEFINER`), **BUKAN** Prisma Client Extension seperti dugaan awal "Eloquent/Prisma-equivalent observer" — dibuktikan empiris selama development (didokumentasikan TDD §26 poin 15): hook `$allOperations` Prisma extension cuma dikasih `{model,operation,args,query}`, TIDAK ADA akses ke `tx` yang sedang berjalan (`$extends` malah eksplisit ada di deny-list tipe transactional client Prisma) — insert audit dari closure client committed INDEPENDEN dari transaksi pemicunya, dibuktikan test: transaksi yang di-`ROLLBACK` tetap menyisakan baris "audit" hasil extension padahal baris utamanya lenyap (audit log BISA BERBOHONG). Trigger native Postgres dijamin atomic dgn statement pemicunya (fundamental transaksi RDBMS) — dibuktikan ulang: `ROLLBACK` tidak menyisakan baris audit sama sekali, `COMMIT` menghasilkan before/after akurat. PK column trigger disuplai lewat trigger argument (`TG_ARGV[0]`, bukan introspeksi pg_index) — konvensi Master PRD §11 poin 1 (`{entity}_id`) beda nama fisik per tabel (lihat `@map` schema.prisma), pola dirujuk `prisma/audit-log-trigger.template.sql` (pasangan `rls-policy.template.sql` 0.3) utk tabel domain Phase 1+ melekatkannya sendiri — task 0.13 SENGAJA baru attach ke `_audit_log_smoke_test` (pola sama `_rls_smoke_test` 0.2/0.3), belum retrofit tabel Phase 0 lain (workflow/notification/attachment) yang belum diminta task manapun. **Immutability (acceptance criterion)** ditegakkan via `REVOKE UPDATE, DELETE ... FROM qhse_app` (SELECT/INSERT tetap ada, dipakai `AuditLogService.record()` utk entri semantik manual — login/export/approve, Master PRD §11.6 poin 6 — yang bukan mutasi baris sederhana) — **dua temuan empiris kritis** (TDD §26 poin 16) yang membuat REVOKE ini WAJIB diulang eksplisit, bukan cukup sekali di parent: (1) RLS TIDAK diwariskan parent→partisi SAMA SEKALI, dan mengakses partisi LANGSUNG (bukan lewat nama parent) SEPENUHNYA BYPASS RLS kalau tidak diulang; (2) privilege partisi BARU diberikan INDEPENDEN oleh `ALTER DEFAULT PRIVILEGES` yang sudah berlaku sejak migration 0.8 (bukan mewarisi ACL parent saat ini) — parent yang sudah direvoke UPDATE/DELETE tetap menghasilkan partisi baru dengan privilege UTUH kalau tidak direvoke ulang. Job `audit-log-partition-maintenance` (BullMQ repeatable, cron `10 0 1 * *` — TDD §13.1 "cron bulanan") karena itu SENGAJA connect via `DATABASE_URL` (role admin), BUKAN `qhse_app`/`APP_DATABASE_URL` — supaya `qhse_app` tidak pernah jadi owner partisi manapun (kalau `qhse_app` yang `CREATE TABLE ... PARTITION OF`, REVOKE dari diri sendiri bukan lagi boundary keras karena owner selalu bisa GRANT balik). Job idempotent (transaksional per partisi: CREATE TABLE + ENABLE/FORCE RLS + CREATE POLICY + REVOKE dalam satu `$transaction`), memastikan partisi bulan berjalan + bulan berikutnya (buffer, TESTING gaya §6). Drop/arsip partisi kedaluwarsa SENGAJA belum diimplementasikan (Master PRD §14.6: retensi audit "wajib dikonfirmasi ke regulasi spesifik industri tenant", default tidak dihapus otomatis — gap TDD §26 poin 18). `correlation_id` ditambah BEYOND kolom literal Master PRD §11.6 (gap TDD §26 poin 17) supaya leg "DB" korelasi ID bisa ditelusuri balik.
  Observability (TDD §15): `AppLoggerService` (Pino terstruktur, `service:"qhse-api"`, ISO timestamp) dipasang jadi logger global Nest (`app.useLogger()`, `bufferLogs:true`) — method `event(level, message, {module, action, ...})` auto-stamp `tenant_id`/`user_id`/`correlation_id` dari AsyncLocalStorage (bukan parameter manual, pola sama `getCurrentTenantId()` 0.2), method standar Nest (`log/error/warn/debug/verbose`) membungkus log framework internal. `tenant-context.ts` (0.2) diperluas tipis (tambah `userId`, diisi dari klaim JWT `sub` yang SAMA dipakai `TenantContextMiddleware`, bukan decode kedua kali) + `observability/request-context.ts` AsyncLocalStorage BARU (correlationId/ipAddress/userAgent) — dua ALS independen, TIDAK saling nested, urutan middleware antar modul karena itu tidak masalah. `CorrelationIdMiddleware` generate `X-Correlation-Id` (UUID) kalau klien belum kirim, echo balik di response header selalu. `PrismaService.withRls()` (0.2) diperluas — turut `SET LOCAL app.current_user_id/correlation_id/ip_address/user_agent` (opsional, kalau context ada) SETELAH `app.current_tenant_id` — inilah jalur SATU-SATUNYA yang menyambungkan AsyncLocalStorage ke trigger `audit_log_capture()` (baca lewat `current_setting()`), tanpa modul manapun yang sudah ada perlu tahu soal audit log sama sekali (observer benar-benar global, nol perubahan di 12 task sebelumnya di luar `prisma.service.ts` sendiri). Metrics Prometheus dasar (`prom-client`, prefix `qhse_api_` utk default metrics Node.js): `http_requests_total`/`http_request_duration_seconds` (histogram, via `HttpObservabilityMiddleware` — middleware BUKAN interceptor, supaya request yang ditolak guard SEBELUM handler, mis. 401, tetap terukur), `bullmq_queue_depth` (gauge per queue: workflow-sla-scan/notification-queue/notification-dead-letter-queue/attachment-scan/audit-log-partition-maintenance), `qhse_workflow_instances_active`/`qhse_notifications_failed` (gauge bisnis, TDD §15 — query admin cross-tenant, pola sama `WorkflowSlaScanService` 0.9). Semua gauge PULL-based (`collect()` async prom-client, dipanggil SAAT `/metrics` di-scrape, bukan `setInterval` terus-menerus). `GET /metrics` (`@Public()`, pola sama `/health`) expose format Prometheus text. **"DB connection pool usage"** (disebut TDD §15) SENGAJA belum diimplementasikan — Prisma baru expose lewat `previewFeatures:["metrics"]` yang masih preview di v5.22 dan berdampak ke SELURUH client dipakai 12 task sebelumnya, ditunda (gap, TDD §26 — belum diberi nomor eksplisit, tercakup catatan Files task 0.13 di atas). Diverifikasi: 8 unit test baru (`audit-log-partition.spec.ts` pure function partisi, `app-logger.service.spec.ts` field injection ALS) + 18 integration test baru (trigger capture INSERT/UPDATE/DELETE akurat, atomicity rollback, immutability UPDATE/DELETE ditolak Postgres via jalur aplikasi sungguhan, RLS fail-closed `system_audit_logs`, jalur manual `AuditLogService`, partition-maintenance idempotent + RLS/privilege benar per partisi baru + rentang tanggal akurat, `GET /metrics` berisi seluruh metrik yg diharapkan, round-trip `X-Correlation-Id`) — 230/230 test lulus seluruh proyek (`pnpm test`), `pnpm build` sukses lintas `@qhse/api`→`@qhse/worker`→`@qhse/web`, generic RLS suite (0.3) otomatis mendeteksi `system_audit_logs` DAN kedua partisi awalnya (`_y2026m07`/`_y2026m08`) sebagai tabel terpisah (introspeksi `information_schema.columns` menemukan tenant_id di tiap partisi individual, bukan cuma parent), dan manual end-to-end dengan proses API + worker BullMQ sungguhan (bukan mock) — `/metrics` sungguhan dicurl menunjukkan `bullmq_queue_depth` utk kelima queue + gauge bisnis terisi data nyata, `X-Correlation-Id` custom diteruskan apa adanya lewat curl, job `audit-log-partition-maintenance` di-enqueue dari luar ke worker nyata dan selesai tanpa error.

### Phase 1 — Fondasi (Organization, User & Role, System Administration, Notification, Workflow domain)

- [x] 1.1 Organization & Master Data — hierarki (Modul 01) — `apps/api/src/modules/domains/organization/*` + `apps/worker/src/reminder-scan.worker.ts`: modul DOMAIN **pertama** (beda dari `platform/*` Phase 0, lihat `ARCHITECTURE.md` §4). 8 tabel hierarki `tenants → companies → branches → sites → departments` (+ `cost_centers`, `locations`, `organization_charts`) sesuai Master PRD §6.1/Modul 01 §5 — `tenants` PERTAMA KALI ada di skema (sebelumnya cuma dirujuk `tenant_id` mentah lintas 12 task Phase 0); PK-nya di-`@map("tenant_id")` langsung (bukan `id` generik) supaya kebijakan RLS-nya `tenant_id = current_setting(...)` identik bentuknya dgn tabel lain walau PK bukan FK. `users.tenant_id`/`user_sessions.tenant_id` (bare UUID sejak 0.6) SEKARANG di-FK-kan ke `tenants` (forward-reference eksplisit sejak task 0.6/0.8) — tabel Phase 0 lain SENGAJA tidak (gap TDD §26 poin 22, RLS tetap jaminan utama terlepas dari FK ini). `industry_template_id`(tenants/companies)/`holiday_calendar_id`(sites) tetap bare UUID tanpa FK (task 1.2 dependency, pola sama `UserRole.scopeId` 0.8). `departments.company_id`/`branch_id` didenormalisasi beyond literal PRD (gap TDD §26 poin 19, alasan sama persis `sites.company_id` PRD sendiri). Trigger `audit_log_capture()` (0.13) dilekatkan ke companies/branches/sites/departments — **konsumen pertama** template-nya di luar tabel uji.
  **Containment hierarki RBAC (acceptance criterion literal)**: `evaluatePermission`/`evaluateScopedIds` (0.8, `permission-resolution.ts`) diperluas — assignment di level LEBIH TINGGI (mis. BRANCH) sekarang mencakup turunannya (SITE/DEPARTMENT di bawahnya, Master PRD §8.3 "HSE Officer scope Site X melihat Site X dan turunannya") lewat `scopeCovers()` (pure, `platform/rbac/scope-hierarchy.ts`) + `PrismaScopeHierarchyResolver` (query LANGSUNG ke companies/branches/sites/departments). Resolver ini SENGAJA hidup di `platform/rbac/`, BUKAN diimpor dari `OrganizationModule` — platform tidak boleh impor domain (arah modular monolith terbalik, berpotensi circular begitu organization butuh RbacModule juga); `OrganizationModule` (domain) yang boleh impor platform, bukan sebaliknya, pola sama `PrismaPermissionRepository` (0.8) query `user_roles`/`roles` langsung tanpa "modul User & Role" (Modul 02/task 1.3 belum ada). Baseline 0.8 (exact-match-or-TENANT) tetap jadi fallback kalau `targetAncestors` tidak diresolusi (entity scope tidak ditemukan → fail closed, BUKAN throw/500). `PermissionService.hasPermission()` HANYA memanggil resolver (DB round-trip tambahan) kalau baseline belum memutuskan `true` DAN ada assignment yang secara teoritis butuh containment — menghindari query tambahan di jalur paling umum. `ApproverResolutionService.ROLE_IN_SCOPE` (0.9) TETAP tenant-wide sengaja — belum ada modul domain konkret dgn entity beralamat site/department utk menentukan bentuk parameter yang tepat (gap TDD §26 poin 23).
  **Business rules** (`OrganizationService`, BELUM ada controller HTTP — Goal literal TASK_INSTRUCTION.md 1.1 cuma minta tabel+service resolusi hierarki, dites langsung pola sama numbering/workflow-engine): BR-01 (site `PROJECT` wajib `start_date`; `end_date` wajib sebelum `ARCHIVED`) via `validateSiteLifecycle()` pure function; BR-02 (uniqueness code per scope) via unique index DB + `ConflictException` bersih (tangkap `P2002`); BR-03 (soft-delete company direstrict kalau ada anak `ACTIVE`, cascade opsional men-nonaktifkan BUKAN menghapus) via `softDeleteCompany({cascadeDeactivateChildren})`; BR-06 (site `PROJECT` lewat `end_date` → `INACTIVE` otomatis, BUKAN `ARCHIVED` langsung) via job `reminder-scan` (BullMQ cron harian `30 0 * * *` — **konsumen pertama** job generik TDD §13.1, dipakai bersama modul Phase 2+ lain nanti bukan job baru per modul); BR-07 (siklus `parent_department_id`) via `assertNoParentCycle()` (traversal pure, DB lookup disuntik caller); BR-08 (kategori site berisiko tinggi tanpa geo → warning, BUKAN blok) via `shouldWarnMissingGeoCoordinates()`.
  Tenant creation (provisioning wizard penuh) BUKAN cakupan task ini — itu task 1.5 (Modul 31, depends on 1.1+1.3); test seed `tenants` langsung lewat Prisma.
  **Temuan operasional**: menambah FK `users.tenant_id -> tenants` di database dev yang sudah berjalan lama menabrak 1.579 baris `users` fixture Jest terakumulasi (tenant_id yatim, `tenants` belum pernah ada sebelumnya) — diselesaikan `TRUNCATE users CASCADE` (approval eksplisit user diminta lebih dulu, bukan otomatis) alih-alih backfill placeholder (gap TDD §26 poin 22 detail).
  Diverifikasi: 15 unit test baru (`site-lifecycle.spec.ts`, `department-hierarchy.spec.ts`) + 6 unit test RBAC baru (containment `scopeCovers`/`evaluateScopedIds` dgn ancestor map) + 39 integration test baru (containment RBAC sungguhan lintas tenant, seluruh BR-01/02/03/07/08 via `OrganizationService` sungguhan, `reminder-scan` idempotent+cross-tenant) — 275/275 test lulus seluruh proyek (`pnpm test`), `pnpm build` sukses lintas `@qhse/api`→`@qhse/worker`→`@qhse/web`, generic RLS suite (0.3) otomatis mendeteksi seluruh 8 tabel baru termasuk `tenants` (kebijakan PK-sebagai-tenant_id), dan manual end-to-end dgn proses API+worker BullMQ sungguhan — `OrganizationModule`/`ReminderScanQueueService` terdaftar bersih di boot, `/metrics` menunjukkan `bullmq_queue_depth{queue="reminder-scan"}` nyata.

- [x] 1.2 Organization & Master Data — template industri & kalender (Modul 01) — `apps/api/src/modules/domains/organization/industry-template/*`: 3 tabel baru — `industry_templates` (katalog GLOBAL platform, TIDAK ADA `tenant_id` sama sekali, PRD Modul 01 §5 eksplisit "tabel global/lookup", TDD §6.3), `holiday_calendars`/`holiday_calendar_entries` (tenant-scoped biasa). Menutup 3 forward-reference bare-UUID dari task 1.1 (gap TDD §26 poin 21) — `tenants.industry_template_id`/`companies.industry_template_id` → FK `industry_templates`, `sites.holiday_calendar_id` → FK `holiday_calendars` — Prisma auto-diff FK-nya sekaligus (relasi eksplisit ditambah di schema, bukan `ALTER TABLE` manual) begitu ketiga model baru ditambahkan dalam satu migration `init_industry_template_holiday_calendar_tables`.
  **RLS & audit trigger — kasus baru "tabel tanpa tenant_id"**: `industry_templates` OTOMATIS terkecualikan dari generic RLS suite (0.3, introspeksi `information_schema` kolom `tenant_id`) — tidak perlu whitelist manual, arsitektur sudah mengantisipasi ini (TDD §6.3). TAPI tabel ini TETAP melekatkan `audit_log_trigger` (0.13) — tabel PERTAMA tanpa `tenant_id` yang melakukannya — diverifikasi empiris AMAN sebelum ditulis ke migration: `audit_log_capture()` menulis `tenant_id` dari `current_setting('app.current_tenant_id')` (session context), BUKAN dari kolom baris sumbernya, dan `system_audit_logs.tenant_id` sendiri NULLABLE (pola sama baris system `roles`/`role_permissions` 0.8) — hasil entri audit `tenant_id=NULL` yang semantik benar (perubahan katalog global, bukan milik satu tenant), dibuktikan lewat query langsung setelah seed jalan. `PrismaService.withGlobalContext()` (varian baru `withRls()`, `platform/tenancy/prisma.service.ts`) ditambahkan utk kasus ini — set `actor_user_id`/dst kalau context ADA, TAPI TIDAK mensyaratkan tenant context sama sekali (beda `withRls()` yang fail-closed tanpa itu) — `IndustryTemplateService.listActive()`/`findByCode()` bahkan HARUS bisa dipanggil sebelum tenant apa pun dibuat (PRD §4.1 langkah 2, setup wizard).
  **BR-05** (hanya 1 kalender `is_default=true` per tenant) ditegakkan MURNI di `HolidayCalendarService` (transactional unset-then-set dalam `withRls()` yang sama) — PRD §5 eksplisit sebut ini "constraint aplikasi, bukan hanya DB unique index sederhana", jadi TIDAK ada partial unique index tambahan (beda dari pola `numbering_configs` 0.10/`notification_templates` 0.11 yang menambahkannya utk masalah lain — Postgres tidak men-dedup NULL, bukan soft-delete filtering). **BR-04** (ganti `industry_template_id` tenant tidak retroaktif mengubah `regulatory_register`) trivial terpenuhi — Modul 04 belum ada tabelnya, tidak ada yang bisa retroaktif berubah.
  `holiday-calendar.ts` (pure function, pola sama `site-lifecycle.ts`/`department-hierarchy.ts`) — `isHoliday`/`isBusinessDay`/`addBusinessDays`; weekend Sabtu/Minggu BEYOND literal teks PRD Modul 01 §5 (gap TDD §26 — tanpa asumsi ini "business-day aware" tidak lengkap). `HolidayCalendarService.resolveEffectiveCalendar()` (site override → default tenant → null) + `isBusinessDayForSite()` primitif SIAP dipakai modul lain (acceptance criterion literal "jika berlaku") — SENGAJA belum di-wire ke `workflow-sla-scan.worker.ts` (0.9), pola sama `ApproverResolutionService.ROLE_IN_SCOPE` (gap #23) — belum ada pemanggil task eksplisit.
  `OrganizationService.createCompany()` (1.1) diperluas — `industryTemplateId` opsional sekarang DIVALIDASI (`IndustryTemplateService.getActiveOrThrow()`) sebelum create, bukan disimpan mentah/yatim. Katalog baseline (`MANUFACTURING`/`OIL_GAS`/`MINING`/`CONSTRUCTION`/`GENERIC`, PRD §5 contoh literal) di-seed `prisma/seed-industry-templates.ts` (idempotent upsert, pola sama `seed-rbac-baseline.ts` 0.8) via admin `DATABASE_URL` (bukan `withRls()`).
  **Temuan operasional**: `organization.service.ts` (impor `IndustryTemplateService` utk validasi) dan `industry-template/*.service.ts` (semula impor helper balik dari `organization.service.ts`) membentuk circular import TypeScript/CommonJS — gejalanya BUKAN error jelas, tapi NestJS DI gagal resolusi dgn pesan "argument dependency at index [1]" jadi `?` (reflect-metadata decorator melihat class masih `undefined` saat cycle terjadi). Diselesaikan ekstrak `requireActorUserId`/`requireTenantId`/`withCleanUniqueViolation` ke file leaf baru `organization-context.ts` yang tidak diimpor balik oleh siapa pun — gap didokumentasikan TDD §26, relevan utk modul domain manapun nanti dgn 2+ service saling silang referensi.
  Diverifikasi: 10 unit test baru (`holiday-calendar.spec.ts` — isHoliday/isBusinessDay/addBusinessDays, tanggal 2026 dipakai assertion diverifikasi empiris via `Date.UTC` dulu sebelum ditulis) + 23 integration test baru (`industry-template.integration-spec.ts` 12 test termasuk pemanggilan TANPA context sama sekali, `holiday-calendar.integration-spec.ts` 11 test termasuk BR-05 lintas-tenant dan RLS-vs-FK-tidak-respect-RLS pola gap #14) — 308/308 test lulus seluruh proyek (`pnpm test`, naik dari 275), `pnpm build` sukses lintas seluruh 7 package monorepo (`@qhse/worker` fresh build tervalidasi ulang lewat compiled `@qhse/api`), dan verifikasi manual langsung ke Postgres (bukan cuma test otomatis) — `pg_class.relrowsecurity` benar per tabel (`industry_templates` FALSE, 2 lainnya TRUE+FORCE), FK constraint 3/3 ada, trigger `audit_log_trigger` terpasang 3/3 tabel baru, seed idempotent (dijalankan 2x -> tetap 5 baris).

- [x] 1.3 User & Role Management (Modul 02) — `apps/api/src/modules/domains/user-role/*`: modul DOMAIN **kedua**. Memperluas subset auth-critical `users`/`roles`/`role_permissions`/`user_roles` (0.6/0.8) jadi skema PENUH Modul 02 §5 lewat `ALTER TABLE ADD COLUMN` (bukan tabel baru, DEPLOYMENT.md §6) + tabel baru `sso_mappings`. `users` dapat ~18 kolom (`employee_id`, `username`, `phone_number`, `user_type`, `department_id`/`site_id`/`position_id` FK ke Modul 01, `reporting_to_user_id` self-FK, `invited_at`/`activated_at`/`suspended_at`/`deactivated_at`, `preferred_language`, `avatar_url`, `hris_sync_source`/`hris_last_synced_at`, plus `created_by`/`updated_by`/`deleted_at` yang belum pernah ada). `roles`/`user_roles` dapat `created_by`/`updated_by`/`deleted_at` ("audit standar" literal PRD, sengaja ditunda 0.8 — lihat banner comment blok RBAC schema.prisma). `roles.based_on_role_id` (BR-05 clone tracking) ditambahkan. `departments.head_user_id` (bare UUID sejak 1.1, forward-reference eksplisit "FK menyusul task 1.3") SEKARANG di-FK-kan ke `users`.
  **BR-11** (`users.tenant_id` NULL hanya utk `user_type=PLATFORM_ADMIN`) — kolom dilonggarkan jadi nullable, FK di-override eksplisit `onDelete: Restrict` (BUKAN default Prisma `SET NULL` utk relasi opsional — `tenant_id=NULL` py arti KHUSUS di skema ini, cascade-null diam-diam lewat DELETE tenant akan salah mengklasifikasi user biasa jadi kelihatan PLATFORM_ADMIN). Kebijakan RLS `users` TETAP strict (BUKAN diberi kebijakan asimetris `OR tenant_id IS NULL` seperti `roles`/`role_permissions` 0.8) — baris PLATFORM_ADMIN semestinya TIDAK terlihat dari konteks tenant manapun sama sekali, beda semantik dari "role sistem berlaku lintas tenant". **Hanya kesiapan skema** — bootstrap sysadmin (`seed-auth-dev.ts`, 0.7) dan alur login (`platform/auth/*`, 0.6) TIDAK diubah task ini utk benar-benar menerima user tanpa tenant (gap TDD §26).
  **BR-03** (`user_roles.scope_id` wajib konsisten `tenant_id`-nya) — `UserService.assignRole()` query LANGSUNG ke companies/branches/sites/departments (BUKAN impor `OrganizationService`), pola PERSIS `platform/rbac/prisma-scope-hierarchy.resolver.ts` (1.1): modul domain lain yang butuh data organisasi query Prisma langsung, bukan lewat service class modul lain — `UserRoleModule` dan `OrganizationModule` TIDAK saling impor. **Temuan**: karena query validasi ini jalan DI DALAM `withRls()` (tenant A), `scope_id` milik tenant B TIDAK PERNAH terlihat sama sekali oleh query-nya sendiri — RLS memfilternya SEBELUM baris sempat dibandingkan `tenantId`-nya, jadi hasilnya SELALU `NotFoundException`, bukan pernah `BadRequestException` yang tadinya diasumsikan (ketahuan lewat test yang gagal, bukan tebakan) — efek sampingnya JUSTRU baik: caller tidak bisa membedakan "scope_id tidak ada" dari "scope_id milik tenant lain", mencegah kebocoran info keberadaan entitas lintas tenant. Perbandingan eksplisit tetap dipertahankan sbg defense-in-depth.
  **BR-02** (lifecycle `INVITED→ACTIVE→SUSPENDED⇄ACTIVE→DEACTIVATED`) via `validateUserStatusTransition()` pure function (pola sama `validateSiteLifecycle` 1.1) + `UserService` — `suspendUser()`/`deactivateUser()` cascade merevoke SELURUH `user_sessions` aktif (literal PRD §4.2); `deactivateUser()` turut men-nonaktifkan SELURUH `user_roles` (status→`INACTIVE`, BUKAN dihapus, demi audit trail — literal PRD). `reason` suspend (free text) dicatat lewat `AuditLogService.record()` (semantik manual, BUKAN kolom `users` — trigger otomatis 0.13 sudah menangkap before/after baris status-nya sendiri). **BR-05** (role sistem immutable, hanya bisa di-clone) via `RoleService.cloneSystemRole()` (menyalin `role_permissions` sumber sbg starting point) + `updateRolePermissions()` (fail-closed `SystemRoleImmutableError` utk role sistem, rekonsiliasi additive/subtractive utk role kustom — bukan hapus-semua-lalu-insert-ulang, menghindari gap window tanpa permission).
  **Baseline RBAC PENUH** (`prisma/seed-rbac-baseline.ts`, deliverable konkret acceptance criterion literal "Matriks RBAC Modul 02 lolos") — diperluas dari 1 role/1 permission (0.8, bukti model doang) jadi **13 role sistem** (Master PRD §8.2) + **59 permission** (katalog literal Modul 01 §3 `ORG.*` + Modul 02 §3 `USER_MGMT.*`, action `.* ` diekspansi CREATE/READ/UPDATE/DELETE eksplisit KECUALI entity yang PRD sendiri tidak sebut DELETE-nya, mis. `user` — tidak pernah hard-delete, hanya `DEACTIVATED`), diwire ke role PERSIS tabel §3 masing-masing dokumen modul; "seluruh user terautentikasi" (7 permission self-service) diberikan ke SEMUA 13 role tanpa kecuali. 5 role yang TIDAK muncul di tabel §3 manapun (`HSE_OFFICER`/`WORKER_EMPLOYEE`/`CONTRACTOR_USER`/`VISITOR_SELF_SERVICE`/`OCCUPATIONAL_HEALTH_STAFF`) TETAP dibuat (bagian roster baseline literal Master PRD §8.2) tapi HANYA dapat ke-7 permission self-service itu — permission spesifik perannya menyusul modul yang benar-benar mendefinisikannya (Work Permit, Occupational Health, dst., Phase 2+, gap TDD §26). `sso_mappings.auto_provision` default `FALSE` — PRD §13 Open Question #4 sendiri sudah condong ke jawaban itu, diikuti apa adanya (bukan keputusan baru).
  **`sso_mappings`** — HANYA pemetaan identitas (`SsoMappingService`), BUKAN handshake SAML/OIDC sungguhan (Modul 30 belum ada tabelnya). `delegation_of_authority` (PRD §2.1 in-scope Modul 02) **BUKAN cakupan task ini** — TIDAK disebut Goal literal TASK_INSTRUCTION.md 1.3 (beda dari 9 tabel lain yang eksplisit disebut), kemungkinan sengaja ditunda bareng `workflow_delegations` (belum ada, disebut task 1.4 di komentar schema.prisma sejak 0.9) karena PRD §5 keduanya saling terkait erat (delegation_of_authority OTOMATIS provisioning workflow_delegations).
  **Temuan operasional**: `prisma migrate dev` di environment non-interaktif ini **kategoris menolak jalan** (bukan cuma skip prompt) begitu ada warning "unique constraint baru terhadap tabel berisi data, berpotensi gagal kalau ada duplikat" (kasus `users.employee_id` — kolom baru, aman krn seluruh baris existing otomatis NULL dan Postgres tidak men-dedup NULL, TAPI Prisma tetap minta konfirmasi interaktif) — dibuktikan bahkan dgn `echo y | prisma migrate dev` tetap exit 1. Diselesaikan comment-out constraint sementara → generate migration utk kolom lain dulu → tulis migration index unique TERPISAH secara manual (folder+SQL manual, bukan lewat `--create-only` yang akan mengulang wall yang sama) → uncomment. Relevan utk task Phase 1+ manapun yang menambah unique constraint ke tabel existing berisi data.
  Diverifikasi: 17 unit test baru (`user-lifecycle.spec.ts` — BR-02 seluruh transisi valid/invalid, BR-11) + 48 integration test baru (`user.integration-spec.ts` 15 test lifecycle+BR-01/03/09 sungguhan, `role.integration-spec.ts` 10 test BR-05 clone+reconcile+baseline roster, `sso-mapping.integration-spec.ts` 7 test) — 356/356 test lulus seluruh proyek (`pnpm test`, naik dari 308; satu kegagalan transien `/auth/logout-all` di run paralel penuh — dikonfirmasi flaky bukan regresi via re-run terisolasi, pola sama kegagalan flaky `require-permission-guard` yg juga ditemui task ini, kategori sudah terdokumentasi TESTING.md/memory: kontensi koneksi bersama `maxWorkers:2`), `pnpm build` sukses lintas seluruh 7 package (`@qhse/worker` fresh build), dan verifikasi manual langsung ke Postgres — RLS benar per tabel (`users`/`roles`/`role_permissions`/`user_roles`/`sso_mappings` TRUE+FORCE, `permissions`/`permission_groups` FALSE), `audit_log_trigger` terpasang 5/5 tabel target, seluruh 7 FK `users` baru (`tenant`/`department`/`site`/`position`/`reporting_to_user`/`created_by`/`updated_by`) ada, seed baseline idempotent (2x run → tetap 13 role).
  Task berikutnya: 1.4 Delegation of Authority (Modul 02, `apps/api/src/modules/domains/user-role/delegation/*` — subfolder BARU di modul yang SAMA, depends on 1.3+0.9) — `delegation_of_authority` yang ditunda task ini, terintegrasi ke resolusi approver Workflow Engine.

- [x] 1.4 Delegation of Authority (Modul 02) — `apps/api/src/modules/domains/user-role/delegation/*`: 2 tabel baru — `delegation_of_authority` (master record organisasional/HR-driven, Modul 02 §5, modul domain user-role) DAN `workflow_delegations` (lapisan eksekusi teknis Workflow Engine, Master PRD §9.1 literal — `delegator_user_id`/`delegate_user_id`/`date_from`/`date_to`/`scope`, "scope" tunggal dipecah `scopeType`+`scopeId`+`roleId` supaya BENTUK datanya identik `delegation_of_authority` demi BR-08 auto-provisioning tinggal salin field). `workflow_delegations` model ditaruh di section "Task 0.9" schema.prisma (konsep Workflow Engine, bukan Modul 02) — `sourceDelegationOfAuthorityId` UUID mentah TANPA FK (platform TIDAK BOLEH FK ke tabel domain, arah modular monolith terbalik, pola sama `entityId` polymorphic `workflow_instances`).
  **`ApproverResolutionService` diperluas PERSIS sesuai banner comment yang ditulis sejak 0.9** ("task 1.4 akan wrap/extend method ini, cek workflow_delegations dulu baru fallback ke resolusi natural, TANPA menyentuh WorkflowEngineService") — `resolveApprovers()` sekarang wrap `resolveNaturalApprovers()` (logic 0.9 asli, byte-for-byte tidak berubah) dengan `substituteActiveDelegates()`: SETIAP approver hasil resolusi natural dicek py delegasi aktif (role_id NULL=semua role ATAU cocok `stage.approverRoleId`), kalau ya disubstitusi delegate_user_id — efeknya `workflow_tasks.assigned_to` LANGSUNG jadi delegate sejak task DIBUAT, **nol baris kode `WorkflowEngineService` berubah**, persis seperti dijanjikan. Digerbangi `WorkflowStage.allowDelegation` (kolom sudah ada sejak 0.9, default `false`) — dibuat OPSIONAL di interface (bukan wajib) supaya caller lama (`WorkflowSlaScanService` reassignment eskalasi) yang mengonstruksi objek sempit tidak perlu diubah; `undefined` diperlakukan sama seperti `false` (fail safe, default TIDAK ada substitusi).
  **Task PENDING yang SUDAH ADA sebelum delegasi aktif** (acceptance criterion literal: "task yang menunggu approver terdelegasi otomatis terarah ke delegate") — ini BUKAN tanggung jawab `ApproverResolutionService` (yang hanya jalan saat task BARU dibuat), jadi job `delegation-scan` (cron harian `45 0 * * *`, pola PERSIS `reminder-scan` 1.1) yang menanganinya: begitu `delegation_of_authority` SCHEDULED→ACTIVE, scan LANGSUNG mereroute seluruh `workflow_tasks` PENDING milik delegator di stage `allowDelegation=true` (UPDATE `assigned_to`+`delegated_to` langsung ke delegate, status TETAP `PENDING` — `WorkflowTaskStatus.DELEGATED` SENGAJA tidak dipakai, lihat gap TDD §26) — dibuktikan end-to-end lewat test: delegator asli `actOnTask()` → `ForbiddenException`, delegate → berhasil sampai `APPROVED`. `workflow_tasks` TIDAK membawa referensi scope organisasi langsung (cuma `entityType`/`entityId` polymorphic yang platform tidak boleh tahu bentuknya) — reroute karena itu HANYA mempersempit lewat `roleId` (tersedia langsung di `workflow_stages`), TIDAK lewat `scopeType`/`scopeId` (gap TDD §26, pola sama keterbatasan `ApproverResolutionService` gap #23 sejak 1.1).
  **BR-07** (delegasi tidak boleh overlap tanggal utk kombinasi delegator/role/scope yang sama) via `assertNoDelegationOverlap()` pure function (pola sama `site-lifecycle.ts` 1.1) dipanggil `DelegationOfAuthorityService.create()` SEBELUM insert, dicek terhadap existing `SCHEDULED`/`ACTIVE` saja (riwayat terminal tidak menghalangi). Delegator/delegate divalidasi ADA di tenant yang sama via query di dalam `withRls()` SEBELUM create (gap TDD §26 poin 14 — FK constraint tidak menghormati RLS, validasi eksplisit tetap wajib). **BR-08** (SCHEDULED→ACTIVE memprovisioning `workflow_delegations`; ACTIVE→EXPIRED menonaktifkannya) via `delegation-scan` job, idempotent (natural-key: `status` filter di query kandidat, pola sama `escalatedAt` `workflow-sla-scan` 0.9 — dibuktikan test run 2x tidak menghasilkan `workflow_delegations` duplikat). `revoke()` (PRD §4.3 poin 5, "delegator dapat REVOKE sewaktu-waktu sebelum date_to") menonaktifkan `workflow_delegations` terkait SEKETIKA, menolak revoke delegasi yang statusnya sudah terminal.
  **Temuan operasional**: extend `ApproverResolutionService` menambah query `tx.workflowDelegation.findMany(...)` yang TIDAK ADA di `fakeTx()` mock lama (`approver-resolution.service.spec.ts`, 0.9) — seluruh test lama yang mengembalikan approver non-kosong langsung `TypeError` (mock tidak punya properti `workflowDelegation`). Diperbaiki dgn extend `fakeTx()` (parameter kedua opsional utk delegation rows, default `[]`) — ditemukan lewat test run yang gagal, bukan diantisipasi dari awal; relevan utk task mana pun nanti yang meng-extend method dgn dependency Prisma baru sementara ada unit test lama yang mem-mock `tx` secara manual (bukan lewat `withRls()` sungguhan).
  Diverifikasi: 7 unit test baru extend `approver-resolution.service.spec.ts` (substitusi delegasi + gate `allowDelegation`) + 11 unit test baru `delegation-lifecycle.spec.ts` (BR-07 overlap seluruh kasus rentang, transisi aktivasi/kedaluwarsa) + 11 integration test baru `delegation.integration-spec.ts` (BR-07 sungguhan, revoke+double-revoke, SCHEDULED→ACTIVE dgn provisioning `workflow_delegations`, idempotensi 2x scan, ACTIVE→EXPIRED, reroute task existing dgn bukti `actOnTask()` end-to-end delegator ditolak/delegate berhasil, gate `allowDelegation:false`, substitusi task baru via `ApproverResolutionService`, isolasi tenant) — 385/385 test lulus seluruh proyek (`pnpm test`, naik dari 356), `pnpm build` sukses lintas 7 package (`@qhse/worker` fresh build memvalidasi ulang deep-import `delegation-scan.worker.ts` ke `@qhse/api/dist`), dan verifikasi manual langsung ke Postgres — RLS `delegation_of_authority`/`workflow_delegations` TRUE+FORCE, `audit_log_trigger` terpasang 2/2, seluruh FK (7 `delegation_of_authority`, 4 `workflow_delegations`) ada dan menunjuk tabel yang benar.
  Task berikutnya: 1.5 System Administration — provisioning tenant penuh (Modul 31, depends on 1.1+1.3) — kandidat pertama yang benar-benar butuh `user_type=PLATFORM_ADMIN`/`tenant_id=NULL` sungguhan (gap TDD §26 poin 30, ditunda sejak 1.3).

- [x] 1.5 System Administration — provisioning & entitlement (Modul 31) — `apps/api/src/modules/domains/system-administration/provisioning/*`: modul DOMAIN KETIGA — **modul domain PERTAMA yang mengimpor modul domain LAIN** (`OrganizationModule` utk `IndustryTemplateService`, `UserRoleModule` utk `UserService`) — `ProvisioningService.provisionTenant()` genuinely butuh orkestrasi lintas keduanya, beda dari larangan platform→domain (lihat banner comment). 4 tabel baru — `subscription_plans` (katalog GLOBAL platform, TIDAK ADA `tenant_id`, pola PERSIS `industry_templates` 1.2), `tenant_subscriptions`/`module_entitlements`/`usage_counters` (tenant-scoped biasa). `module_entitlements` dapat `created_at`/`updated_at` BEYOND kolom literal PRD (PRD Modul 31 §5 tidak mencantumkan timestamp apa pun utk tabel ini — memutus konvensi "audit standar" seluruh tabel lain — ditambah demi konsistensi, gap TDD §26).
  **Temuan empiris kritis SEBELUM menulis kode**: kebijakan RLS `tenants` (task 1.1, PK ganda sbg `tenant_id`) TERNYATA menolak MUTLAK setiap INSERT lewat `qhse_app` — dibuktikan psql: error Postgres 42501 "new row violates row-level security policy for table tenants". Sebabnya: `tenant_id` baris BARU baru di-generate SAAT insert, jadi TIDAK ADA nilai `app.current_tenant_id` yang mungkin bisa dipasang lebih dulu (beda dari `industry_templates`/`subscription_plans` yang aman lewat `withGlobalContext()` krn TIDAK punya RLS sama sekali). `ProvisioningService.provisionTenant()` karena itu py koneksi admin SENDIRI (`DATABASE_URL`, pola PERSIS `reminder-scan`/`delegation-scan`/`audit-log-partition-maintenance`) KHUSUS utk INSERT tenant — SETIAP langkah SETELAHNYA (subscription, entitlements, Tenant Admin, aktivasi status) balik ke `withRls()` biasa via `qhse_app`, dibuktikan JUGA empiris: UPDATE ke baris `tenants` yang SAMA berhasil via `qhse_app` begitu `app.current_tenant_id` (di-set `tenantContextStorage.run()`) cocok dgn baris yang SUDAH ADA (beda dari INSERT). Alur lengkap: INSERT admin (status `PROVISIONING`) → masuk context tenant baru → `tenant_subscriptions` + `module_entitlements` (BR-01, via `EntitlementService.activateFromPlan()`) + `UserService.inviteUser()` (Tenant Admin pertama) + `UserService.assignRole()` (`TENANT_ADMIN`, scope `TENANT`) → UPDATE `tenants.status→ACTIVE` via `qhse_app`. TIDAK atomik lintas SELURUH langkah (gap TDD §26) — kegagalan di tengah flow bisa menyisakan tenant `PROVISIONING` tanpa admin lengkap, belum ada mekanisme retry/cleanup otomatis.
  **BR-01** (`module_entitlements.is_enabled=false` WAJIB blokir akses API secara penuh) via `EntitlementGuard` — **APP_GUARD KETIGA** (setelah `JwtAuthGuard`/`EntitlementModule` diimpor SEBELUM `RbacModule` di app.module.ts, supaya blokir modul jalan LEBIH DULU dari cek permission granular). Reuse metadata `@RequirePermission()` yang SAMA (BUKAN decorator baru `@RequireModule()`) — module_code diresolusi dari `Permission.moduleCode` (lookup katalog, **BUKAN string-parsing prefix `permission_code`** — dibuktikan empiris ADA mismatch casing: prefix `permission_code` "user_mgmt" lowercase vs `Permission.moduleCode` tersimpan "USER_MGMT" uppercase). Hidup di `platform/entitlement/` (query Prisma LANGSUNG ke `tenant_subscriptions`/`module_entitlements`/`permissions`, BUKAN impor `EntitlementService` dari modul domain — pola PERSIS `PrismaScopeHierarchyResolver` 1.1) — **keputusan desain kunci**: tenant yang BELUM PERNAH py `tenant_subscriptions` sama sekali (SELURUH fixture test 0.1-1.4, tenant "legacy" pra-1.5) lolos TANPA syarat (fail OPEN khusus kasus ini) — dibuktikan REGRESI NOL: seluruh 13 test RBAC lama (`test/rbac/*`) tetap lulus persis seperti sebelum `EntitlementGuard` ada, termasuk skenario 200/403/503 yang SAMA. Tenant yang SUDAH py minimal 1 baris `tenant_subscriptions` (APAPUN statusnya) baru tunduk fail-closed penuh. Dibuktikan end-to-end lewat HTTP sungguhan (PKCE+MFA+JWT nyata, bukan mock): `module_entitlements` `is_enabled=false` → `GET /rbac/permissions` 403 "Modul ... tidak aktif"; `is_enabled=true` → lolos gate (200).
  **BR-02** (`usage_counters` melebihi `max_users`/`max_sites` TIDAK memblokir operasional, cuma notifikasi) via job `usage-counter-scan` (cron harian `0 1 * * *`, pola PERSIS `reminder-scan`/`delegation-scan`) — `checkQuota()` pure function (`exceeded` murni deteksi, TIDAK pernah throw). **Temuan performa**: bootstrap query AWALNYA `SELECT tenant_id FROM tenants` (SEMUA tenant tanpa filter, meniru reminder-scan sepintas) — timeout >20s begitu dijalankan bareng full test suite, krn `tenants` di DB dev lokal bersama sudah mengakumulasi ribuan baris dari fixture Jest lintas SEMUA task 0.1-1.5 yang tidak pernah dibersihkan (pola sama gap TDD §26 poin 22, insiden 1,579 baris `users` task 1.1). Diperbaiki mengikuti pola reminder-scan/delegation-scan yang SUDAH benar sejak awal: filter DI query bootstrap itu sendiri (`tenant_subscriptions` berstatus `TRIAL`/`ACTIVE` sekaligus JOIN `subscription_plans` utk kuota — SATU query, bukan query terpisah per tenant di dalam loop) — hasilnya bukan cuma lebih benar tapi juga jauh lebih cepat (full suite turun dari ~158s ke ~120s). Notifikasi kuota terlampaui (PRD §8) SENGAJA belum di-wire (gap TDD §26, pola konsisten seluruh task 1.1-1.4) — cuma log terstruktur.
  **BR-04** (hanya Super Admin Platform override entitlement di luar plan) via `EntitlementService.override()` — upsert `(tenantId, moduleCode)`, `overriddenBy`/`overrideReason` wajib terisi. Otorisasi RBAC-nya sendiri (`sysadmin.entitlement.override`) BELUM ditegakkan di layer ini (BELUM ada controller HTTP, pola sama 1.1-1.4 — method ini murni menegakkan BENTUK data, `PermissionGuard`/`EntitlementGuard` yang akan menggerbanginya begitu ada endpoint).
  **E2E-4** (TESTING.md §7, literal: "Provisioning tenant baru → aktivasi modul via plan → buat Tenant Admin pertama → login pertama") — task PERTAMA dgn acceptance criterion literal berupa **Playwright E2E test**, TAPI belum ada satu pun halaman `apps/web` (UI provisioning/login) maupun infrastruktur Playwright di codebase ini — dibuktikan di level API/service sebagai gantinya (konsisten disiplin "belum ada controller HTTP" 1.1-1.4, diperluas ke "belum ada UI" jadi eksplisit sekarang, gap TDD §26): 3 langkah pertama via `provisionTenant()` sungguhan, langkah "login pertama" via `POST /auth/login`+`POST /auth/token` SUNGGUHAN (0.6, tidak disentuh) memakai kredensial Tenant Admin yang baru dibuat — password di-set langsung test (mensimulasikan user menyelesaikan link undangan, langkah yang SENDIRI di luar cakupan 1.3/1.5, gap TDD §26).
  Diverifikasi: 4 unit test baru (`usage-quota.spec.ts` — BR-02 batas inklusif/unlimited) + 8 integration test baru (`provisioning.integration-spec.ts` — E2E-4 penuh dgn login sungguhan, tolak plan tidak ada, tolak tenant_code duplikat, BR-01 dua arah via HTTP nyata, BR-04 override, BR-02 snapshot+quota-exceeded cross-tenant) — 397/397 test lulus seluruh proyek (`pnpm test`, naik dari 385), **regresi NOL dibuktikan eksplisit** pada 13 test RBAC pre-existing (`test/rbac/*`) walau `EntitlementGuard` sekarang APP_GUARD global ketiga, `pnpm build` sukses lintas 7 package (`@qhse/worker` fresh build memvalidasi ulang deep-import `usage-counter-scan.worker.ts`), dan verifikasi manual langsung ke Postgres — RLS benar per tabel (`subscription_plans` FALSE, 3 lainnya TRUE+FORCE), `audit_log_trigger` terpasang 3/4 (`usage_counters` SENGAJA tidak, snapshot murni system-generated tanpa lifecycle bermakna, pola sama `locations`/`cost_centers` 1.1), seluruh 7 FK ada dan menunjuk tabel yang benar, seed baseline idempotent (3x run → tetap 3 plan).
  Task berikutnya: 1.6 System Administration — import data & branding (Modul 31, `system-administration/data-import/*` + `.../branding/*`, depends on 1.1+0.12) — `data_import_jobs`/`data_import_errors`/`tenant_branding_configs` yang ditunda task ini.

- [x] 1.6 System Administration — import data & branding (Modul 31) — `apps/api/src/modules/domains/system-administration/{data-import,branding}/*`: 3 tabel baru — `data_import_jobs`/`data_import_errors` (2 tabel, `system-administration/data-import/`) + `tenant_branding_configs` (1 tabel, `system-administration/branding/`), MELENGKAPI Modul 31 sepenuhnya (bareng 4 tabel task 1.5). `DataImportJob` dapat `sourceDataImportJobId` self-FK BEYOND kolom literal PRD — Acceptance TASK_INSTRUCTION.md 1.6 eksplisit minta "re-upload parsial tertaut ke data_import_job_id asal", pola PERSIS `WorkflowDelegation.sourceDelegationOfAuthorityId` (1.4). `DataImportError` dapat `tenant_id` LANGSUNG (BEYOND literal PRD yang cuma sebut FK `data_import_job_id`) — RLS WAJIB kolom tenant_id di tabel itu sendiri, pola PERSIS `WorkflowStage`/`HolidayCalendarEntry` (SETIAP tabel anak di codebase ini selalu dapat tenant_id sendiri, tanpa kecuali). `TenantBrandingConfig` dapat kolom audit (`created_by/at`, `updated_by/at`) BEYOND literal PRD — gap sama persis `module_entitlements` (1.5). `custom_domain` UNIQUE lintas SELURUH platform (bukan composite per-tenant seperti kebanyakan `@@unique` lain) — 2 tenant tidak boleh klaim domain yang sama.
  **Reuse infrastruktur attachment (0.12) APA ADANYA** utk file Excel — TIDAK ada kolom `attachment_id` terpisah di `data_import_jobs`: relasinya lewat asosiasi polymorphic yang SUDAH ada (`Attachment.entityType="DATA_IMPORT_JOB"`/`entityId`), memakai teknik **client-proposed-id** (job ID digenerate `randomUUID()` DI SERVICE LAYER SEBELUM attachment di-`confirm()`, dipakai sbg `entityId`-nya, BARU SETELAH itu baris `data_import_jobs` dibuat dgn id yang SAMA) supaya urutan ayam-telur (attachment butuh entity yang "sudah ada"; job belum py `file_url` sebelum attachment confirm) selesai tanpa kolom FK redundan. Malware-scan (0.12) dipakai utuh — job baru TIDAK diterima kalau `scanStatus=PENDING_SCAN` blocking secara SYNCHRONOUS; sebaliknya `DataImportProcessingService` mem-THROW dari job "validate"-nya sendiri kalau masih pending, menjadikan **retry+backoff BullMQ (attempts tinggi) sbg mekanisme tunggu-hasil-scan** tanpa infra polling terpisah.
  **Temuan library kritis SEBELUM kode final**: `ExcelJS.stream.xlsx.WorkbookReader` (streaming reader, dipilih AWAL demi literal TDD §20 "bukan load seluruh file ke memori") diverifikasi empiris **TIDAK RELIABLE** — dibuktikan lewat pengulangan 5-10x membaca ulang buffer hasil `Workbook.xlsx.writeBuffer()` (writer exceljs SENDIRI, bukan file aneh dari luar): gagal 60-90% percobaan dengan error internal `Cannot read properties of undefined (reading 'sheets')` (root cause: `workbook-reader.js` mengasumsikan entry zip `xl/workbook.xml` SELALU tiba sebelum `xl/worksheets/sheet1.xml`, asumsi yang TIDAK dijamin writer exceljs sendiri — kemungkinan kompresi internal tiap entry berjalan tidak berurutan). Diverifikasi baik lewat stream S3 SDK maupun file sementara di disk, POLA GAGAL SAMA — root cause di reader-nya, bukan sumber input. `Workbook.xlsx.load(buffer)` (API non-streaming, jauh lebih matang) terbukti 100% reliable di percobaan berulang yang sama — **dipakai sbg pengganti**, konsekuensinya file Excel dimuat penuh ke memori (`ObjectStorageService.getObjectBuffer()`, method `getObjectStream()` yang sempat ditambah lalu DIHAPUS lagi krn jadi dead code) — TIDAK memenuhi TDD §20 literal utk file yang genuinely sangat besar (gap TDD §26, true streaming butuh library lain/parser SAX custom di luar timebox task ini).
  **Batch "transaksi kecil" (TDD §20, "mis. 500 baris per transaksi") DIINTERPRETASI di level LOOP APLIKASI, bukan literal 1 transaksi Postgres per 500 baris** — SAVEPOINT/ROLLBACK TO SAVEPOINT per-baris dalam SATU transaksi bersama TERBUKTI bekerja lewat Prisma interactive transaction (diverifikasi empiris via probe psql-equivalent SEBELUM keputusan diambil), TAPI SENGAJA tidak dipakai supaya `UserService.inviteUser()` (1.3, py `withCleanUniqueViolation()`+validasi sendiri) bisa direuse APA ADANYA tanpa dibongkar — setiap baris tetap transaksi kecilnya SENDIRI (lewat mapper), `DATA_IMPORT_BATCH_SIZE` dipakai murni utk flush progress `successRows`/`errorRows` berjalan tiap N baris.
  **Row-mapper registry** (`DataImportRowMapper` interface + `DataImportRowMapperRegistry`, DI factory provider — pola standar NestJS "kumpulkan N provider jadi array" krn NestJS tidak py `multi:true` ala Angular) — HANYA `EmployeeRowMapper` (`targetModuleCode=EMPLOYEE`, reuse `UserService.inviteUser()`) yang konkret; `ORGANIZATION`/`ASSET`/`DOCUMENT`/`TRAINING_CERTIFICATE`/`INCIDENT_HISTORY` (contoh literal PRD) SENGAJA belum ada mapper — modul target belum ada tabelnya sama sekali (Phase 2+) atau butuh desain kolom terpisah (ORGANIZATION: hierarki company/site/department tidak muat 1 baris Excel flat), pola sama pembatasan `includedModuleCodes` 1.5.
  **Temuan konkurensi PALING KRITIS task ini** (ditemukan lewat test yang flaky TIDAK KONSISTEN, diinvestigasi tuntas bukan diabaikan): dev environment ini TERBUKTI EMPIRIS kadang punya **proses `apps/worker` sungguhan berjalan independen** (Redis `CLIENT LIST` menunjukkan consumer `bzpopmin` aktif di SEMUA nama queue termasuk `data-import` yang baru dibuat task ini) — begitu test memanggil `processValidate()`/`processCommit()` LANGSUNG (pola sama 1.5) SAMBIL `createJob()`/`confirmImport()` JUGA enqueue job SUNGGUHAN, worker asli itu BISA JADI ikut memproses job yang SAMA secara paralel, menyebabkan baris `data_import_errors` dobel & `mapper.importRow()` terpanggil dua kali. **Bukan bug test infra semata** — DIPERBAIKI di level SERVICE (bukan cuma test) via **klaim atomik**: `updateMany({where:{id,status:from}})` (BUKAN `update({where:{id}}`) sbg SATU-SATUNYA cara transisi `UPLOADED→VALIDATING` (processValidate) dan `VALIDATED→IMPORTING` (processCommit, DIPINDAH dari `confirmImport()` yg SENGAJA TIDAK lagi menuliskan status sendiri — cuma validasi+enqueue, mencegah double-confirm JUGA jadi double-enqueue) — Postgres row-level lock inheren MENJAMIN hanya SATU invocation menang per transisi, siapa pun yang kalah `return` tanpa efek samping. Konsekuensi: job yang macet di tengah proses (crash pasca-klaim) TERTAHAN permanen di status intermediate — gap TDD §26, butuh job stalled-sweep terpisah di luar timebox ini. Integration test task ini SENDIRI juga diperbaiki jadi **polling** (`waitForJobStatus()`, bukan asumsi "setelah `await processValidate()` selesai SAYA pasti pemenang klaim") — benar terlepas dari siapa yang menang race, worker asli atau test manual.
  **CORS whitelist tenant custom domain** (TDD §16) via `TenantCorsResolverService` (`platform/tenancy/`) — instance KEEMPAT pola "koneksi admin (RLS bypass) utk operasi genuinely lintas-tenant" (setelah `ProvisioningService` insert tenant 1.5, 3 job cross-tenant scan) — CORS preflight terjadi SEBELUM tenant context APA PUN ada (origin header cuma hostname, bukan tenant_id), jadi resolusinya SECARA STRUKTURAL butuh mencari lintas SELURUH tenant. Query DIBATASI KETAT ke SATU kolom (`customDomain`, cuma return boolean) — TIDAK pernah expose `logo_url`/`primary_color`/`display_name` tenant lain. `main.ts` `app.enableCors()` diganti dari origin statis (`WEB_ORIGIN`) ke origin callback ASYNC. **Ditemukan & diperbaiki**: `test/auth/test-helpers.ts` `finishTestApp()` (komentar sendiri: "mirror bootstrap main.ts") TERNYATA tidak pernah diupdate ikut CORS — SEMUA test integration (bukan cuma modul ini) sebelumnya jalan TANPA CORS middleware aktif sama sekali. Diperbaiki + diekstrak `buildCorsOptions()` factory (dipakai KEDUA tempat) supaya tidak bisa drift lagi.
  `tenants.primary_domain`/`tenants.logo_url` (kolom SEJAK task 1.1, diverifikasi grep KOSONG — tidak pernah dipakai kode apa pun) tumpang tindih semantik dgn `customDomain`/`logoUrl` `TenantBrandingConfig` — SENGAJA TIDAK dibackfill/disatukan (2 sumber kebenaran dibiarkan terpisah, gap TDD §26) krn `tenants.*` kolom bootstrap-time (provisioning 1.5) sedangkan `tenant_branding_configs` py lifecycle CRUD sendiri (Tenant Admin self-service) — menyatukan sekarang berisiko migrasi data diam-diam tanpa keputusan produk mana yang otoritatif.
  Diverifikasi: unit test baru (`data-import-lifecycle.spec.ts` 8, `employee-row-validation.spec.ts` 7, `cors-origin.spec.ts` 5, `custom-domain.spec.ts` 7) + integration test baru (`data-import.integration-spec.ts` 7 — alur penuh VALIDATED→IMPORTING→COMPLETED_WITH_ERRORS dgn User sungguhan dibuat, BR-03 retry dua arah, module tidak didukung, attachment reuse ditolak, PENDING_SCAN throw, INFECTED→FAILED; `branding-cors.integration-spec.ts` 9 — upsert+idempotent+cross-tenant custom_domain conflict+format invalid, resolver 4 skenario, CORS header end-to-end via supertest) — **445/445 test lulus seluruh proyek** (`pnpm test`, naik dari 397), diverifikasi **stabil lintas run berulang** (bukan cuma sekali kebetulan lulus, mengingat riwayat race condition di atas — dijalankan 4x isolasi + 2x bareng modul 1.5 sebelum dianggap selesai), `pnpm build` sukses lintas 7 package, dan verifikasi manual langsung ke Postgres — RLS benar TRUE+FORCE di ke-3 tabel baru, `audit_log_trigger` terpasang 2/3 (`data_import_errors` SENGAJA tidak, system-generated murni, pola sama `usage_counters` 1.5), seluruh 8 FK ada & menunjuk tabel yang benar, unique index `custom_domain`+`tenant_id` pada `tenant_branding_configs` terpasang (terverifikasi lewat `pg_indexes`, bukan `information_schema.table_constraints` — Prisma `@unique`/`@@unique` menghasilkan `CREATE UNIQUE INDEX` biasa, bukan `ADD CONSTRAINT`, jadi tidak muncul di catalog constraint tapi tetap fungsional identik).
  Task berikutnya: 1.7 Notification Center — domain UI & preferensi (Modul 25, `apps/web/app/(dashboard)/notifications/*` + `apps/api/src/modules/domains/notification/*`, depends on 0.11+1.3) — task PERTAMA dgn deliverable `apps/web` sungguhan.

- [x] 1.7 Notification Center — domain UI & preferensi (Modul 25) — `apps/api/src/modules/domains/notification/*` + `apps/web/app/(dashboard)/notifications/*`: modul DOMAIN KEEMPAT, **task PERTAMA dgn deliverable `apps/web` sungguhan** (0-13 seluruhnya backend/infra murni, dites API-level). `NotificationDomainModule` (nama SENGAJA beda dari `platform/notification/NotificationModule` 0.11 yang sudah ada — tabrakan nama class kalau disamakan) — TIDAK ada tabel baru; murni layer QUERY+PREFERENCE di atas 5 tabel 0.11 yang sudah ada (`notifications`/`notification_preferences` dipakai langsung, 3 lainnya tetap murni internal 0.11). Migration baru HANYA attach `audit_log_trigger` (0.13) ke `notification_preferences` (perubahan preferensi = aksi user eksplisit bermakna, sejalan heuristik "low-frequency/high-signal" seluruh task sebelumnya) — `notifications` SENGAJA tidak (system-generated volume tinggi, pola sama `usage_counters`/`data_import_errors`). 2 permission baru (`notification.read`/`notification.preference.manage`, Modul 25 §3 "Seluruh User" self-scope) ditambah `AUTHENTICATED_USER_PERMISSIONS` (pola PERSIS 7 permission self-service 1.3) — diberikan ke SEMUA 13 role baseline tanpa kecuali, bukan `ROLE_PERMISSIONS` per-role.
  **Ditemukan & diperbaiki SEBELUM ke kode fitur**: `NOTIFICATION` module_code TIDAK PERNAH ada di `SUBSCRIPTION_PLAN_SEEDS.includedModuleCodes` manapun (1.5) — kalau dibiarkan, `EntitlementGuard` (1.5, APP_GUARD ketiga) akan mem-403 SETIAP tenant yang sudah py `tenant_subscriptions` (yakni SEMUA tenant hasil `provisionTenant()` sejak 1.5) di endpoint modul ini, walau permission RBAC-nya sendiri benar — cuma tenant "legacy" pra-1.5 (fail-open by design, gap #37) yang akan lolos. Diperbaiki menambah `"NOTIFICATION"` ke ketiga plan. **Ditemukan sekalian bug idempotency terpisah**: `seedSubscriptionPlans()` (1.5) SELAMA INI cuma `create`-if-missing (`skipDuplicates`-style) — re-run seed dgn `includedModuleCodes` yang SUDAH BERUBAH (persis kasus ini) TIDAK PERNAH mengupdate baris plan yang sudah ada, seed jadi tidak truly idempotent utk perubahan konten walau idempotent utk keberadaan baris. Diperbaiki jadi `upsert` sungguhan (update eksplisit kalau sudah ada) — regresi laten yang baru kepakai/ketahuan begitu task pertama post-1.5 butuh mengubah isi plan yang sudah di-seed, bukan cuma menambah plan baru.
  **`EVENT_CATEGORIES`** (`event-category.ts`, 14 entri) DITURUNKAN dari kolom "Modul Sumber" PRD Modul 25 §9 (katalog event trigger) — PRD **tidak pernah** mendefinisikan `event_category` (`notification_preferences.event_category`, Modul 25 §5) sbg katalog kode formal, cuma prosa contoh "mis. WORK_PERMIT, INCIDENT, TRAINING" — scoped/best-effort krn TIDAK SATU PUN dari ke-14 modul sumber itu (Work Permit, Incident, dst., seluruhnya Phase 2+) benar-benar ada di codebase utk dikonfirmasi balik, pola sama `includedModuleCodes` (1.5)/row-mapper registry (1.6) yang JUGA dibatasi ke apa yang genuinely bisa diverifikasi sekarang (gap TDD §26). **`buildPreferenceMatrix()`** (`preference-matrix.ts`) SENGAJA TIDAK mengubah perilaku backend 0.11 yang sudah ada — dicek eksplisit dgn membaca ulang test case `notification-channel-resolution.spec.ts` (0.11) SEBELUM menulis kode: IN_APP SELALU `{isEnabled:true, editable:false}` (BR-02, bukan cuma UI disabled — `NotificationPreferenceService.upsertPreference()` MENOLAK `channelCode:"IN_APP"` dgn `BadRequestException`, defense-in-depth di service layer bukan cuma omit kontrol UI), EMAIL/WHATSAPP/TELEGRAM default `isEnabled:false` kalau BELUM ADA baris preference (opt-in, BUKAN opt-out) — **mengonfirmasi ulang, bukan mengubah**, perilaku `resolveAdditionalChannels()` 0.11 yang sudah begitu sejak awal. `notification-time.ts` (`HH:mm` string ↔ `@db.Time` sbg `Date`) — base date `1970-01-01T00:00:00Z` diverifikasi empiris via probe script SEBELUM dipakai assertion test, bukan diasumsikan dari dokumentasi Prisma.
  **`NotificationQueryService.markAsRead()`** — `NotFoundException` SERAGAM baik utk notifikasi yang genuinely tidak ada MAUPUN yang milik user lain (BUKAN `ForbiddenException` utk kasus kedua) — pola PERSIS alasan gap #31 (1.3, BR-03 `scope_id` lintas tenant): caller tidak bisa membedakan "tidak ada" dari "milik orang lain", mencegah kebocoran info keberadaan row. `event-category.ts` (lookup table + 2 fungsi trivial) DAN `notification-context.ts` (leaf helper ambil userId/tenantId ambient) SENGAJA TIDAK py file spec terpisah — cukup sederhana utk dianggap teruji cukup lewat integration test yang memanggil `/notifications/preferences` (tiap kategori di-iterate di sana), beda dari `preference-matrix.ts`/`notification-time.ts` yang py logic non-trivial dan DAPAT unit test sendiri.
  **Frontend — `apps/web` BENAR-BENAR KOSONG sebelum task ini** (nol API client, nol auth state, nol route group selain `/` & `/design-system` 0.5): dibangun MINIMAL murni cukup utk 2 halaman task ini, TIDAK LEBIH. `lib/api-client.ts` (`apiFetch`/`apiFetchWithMeta`/`ApiError`, token dari `sessionStorage` key `qhse.accessToken`, base URL `NEXT_PUBLIC_API_URL` default `http://localhost:3001`) + `lib/use-auth-token.ts` (`useHasAccessToken()`, `boolean|null` — `null` = belum tahu, hindari flash-of-wrong-state saat SSR/hydration). **TIDAK ADA HALAMAN LOGIN** — digrep eksplisit ke `TASK_INSTRUCTION.md` SELURUHNYA (Phase 0-2+): tidak ada satu task pun yang literal menyebut "halaman login" sbg deliverable `apps/web`, walau `platform/auth/*` (0.6) sudah lengkap sisi backend sejak lama — GAP GENUINELY BELUM ADA PEMILIK, bukan ditunda ke task bernama (didokumentasikan TDD §26). Verifikasi browser task ini karena itu memakai token asli hasil PKCE login manual (`POST /auth/login`+`/auth/token` sungguhan) di-inject ke `sessionStorage` lewat script sekali-pakai (dihapus setelah verifikasi selesai), BUKAN lewat form UI (belum ada). `app/(dashboard)/layout.tsx` — shell PLACEHOLDER minimal (brand+2 link nav), eksplisit BUKAN task 0.14 "App shell & state primitives" (topbar/sidebar/scope-picker, belum pernah dikerjakan, BUKAN dependency task ini) — komentar banner di file itu sendiri menandainya.
  **`packages/ui-components`**: `Toggle` (switch component baru, dipakai matriks preferensi) — nambah `"lib":["ES2022","dom","dom.iterable"]` ke `tsconfig.json` package ini (SEBELUMNYA tidak ada, `HTMLInputElement`/`event.target.checked` gagal compile tanpanya — package ini sebelumnya nol komponen yang genuinely butuh DOM event typing). `DataTable<T extends Record<string,unknown>>` (0.5) constraint generic-nya menolak interface bernama biasa (`NotificationDto`/`CategoryRow`) — diperbaiki nambah `[key:string]:unknown` index signature ke KEDUA interface itu (compile-time-only, TIDAK melemahkan akses properti bernama yang sudah typed), BUKAN melonggarkan constraint `DataTable` itu sendiri. `@qhse/i18n` `t(key,locale,vars?)` diperluas dukungan interpolasi `{{var}}` mustache-style — **konsumen PERTAMA** fitur ini (`notifications.inbox.unreadCount`/`pagination.summary`, PRD §12 butuh angka dinamis di label), 3-key dictionary awal (0.5) sebelumnya tidak pernah butuh variable substitution sama sekali.
  **Temuan Windows/Turborepo BARU**: `@qhse/web` (Next.js `output:"standalone"`, SEJAK awal utk task 0.4 Docker — TIDAK diubah task ini) build/dev-start GAGAL REPRODUCIBLE (`EPERM: operation not permitted, scandir '...\.next\standalone\...'`) kalau jalan BERSAMAAN dgn sibling package lain lewat Turborepo di Windows box ini, TAPI sukses 100% reliable baik terisolasi maupun via `turbo run build --concurrency=1` (dibuktikan berulang kali) — BUKAN cacat kode, murni Windows file-lock scandir bentrok antar proses paralel; mitigasi `--concurrency=1` dipakai utk build verifikasi task ini (gap TDD §26, relevan siapa pun yang menjalankan `turbo run build/dev` penuh di Windows). **Terpisah**: dua proses `pnpm dev` (`node --watch`) YATIM ditemukan masih hidup di port 3000/3001 dari sesi kerja awal percakapan panjang ini — inilah AKAR PENYEBAB "worker yang misterius muncul lagi" yang diduga-duga selama task 1.6 (`--watch` auto-restart tiap file berubah, jadi kill sekali tidak pernah benar-benar menghilangkannya); diselesaikan tuntas via `Get-NetTCPConnection -LocalPort <port> -State Listen`→`Stop-Process`, server dev pengganti dijalankan lewat harness (`preview_start`) supaya tidak lagi jadi proses yatim.
  Diverifikasi: 7 unit test baru (`preference-matrix.spec.ts`) + 7 unit test baru (`notification-time.spec.ts`) + 15 integration test baru (`notification-domain.integration-spec.ts` — literal acceptance criterion "preferensi user dihormati sebelum kirim": `upsertPreference()` mengubah kanal resolusi `NotificationService.enqueue()` 0.11 yang TIDAK disentuh, dibuktikan DUA ARAH on→off dan off→on; ke-6 endpoint, RBAC gate, isolasi tenant, BR-02 penolakan IN_APP) — **474/474 test lulus seluruh proyek** (`pnpm test`, naik dari 445; satu kegagalan `permission-fail-closed.integration-spec.ts` "Connection is closed" muncul di run penuh — dikonfirmasi PRE-EXISTING flaky Redis lintas-suite via re-run terisolasi 2/2 bersih, BUKAN regresi task ini), `pnpm build` sukses lintas seluruh 7 package (`/notifications` & `/notifications/preferences` muncul di output build Next.js sbg static route baru). **Verifikasi browser end-to-end sungguhan** (bukan cuma test otomatis) via dev server nyata + user/tenant sungguhan (dibuat & DIHAPUS lagi via script sekali-pakai setelah selesai): halaman inbox state kosong ("Belum ada notifikasi") tanpa error console; 3 notifikasi sungguhan di-seed langsung DB → render benar (badge prioritas 3 tone, tombol "Tandai dibaca" HANYA muncul di baris belum-dibaca) → klik tandai-satu (unread count 2→1, tombol hilang dari baris itu) → klik tandai-semua (unread count hilang, seluruh tombol hilang) seluruhnya lewat network request nyata (`POST .../read`, `POST .../read-all`) bukan simulasi; halaman preferensi render matriks 14×4 penuh, kolom IN_APP teks "Selalu aktif" (BUKAN toggle), 42 toggle EMAIL/WHATSAPP/TELEGRAM lainnya default UNCHECKED (dibuktikan lewat DOM `checked` property, bukan cuma visual) → toggle satu sel ON → `PUT .../preferences` 200 → **hard reload penuh** (bukan cuma re-render client) → state TETAP ON, membuktikan persist ke DB sungguhan bukan cuma state client. Console & network bersih di sepanjang seluruh langkah verifikasi.
  **Phase 1 (Fondasi) SELESAI** — seluruh 1.1-1.7 tuntas. Task berikutnya: 2.1 Document Management System (Modul 03, `apps/api/src/modules/domains/dms/*`, depends on 0.12) — awal Phase 2 (Kepatuhan Dasar), modul domain KELIMA, version control dokumen + distribusi wajib-baca dgn bukti acknowledgement + reminder review berkala via job terjadwal.

### Phase 2 — Kepatuhan Dasar (DMS, Regulatory & Compliance)

- [x] 2.1 Document Management System (Modul 03) — `apps/api/src/modules/domains/dms/*`: modul DOMAIN KELIMA, awal Phase 2. 6 tabel baru — `documents`/`document_versions`/`document_categories`/`document_distribution`/`read_acknowledgement_logs`/`document_review_schedule` (PRD §5 EKSPLISIT: "TIDAK ada tabel `document_approval` terpisah", approval sepenuhnya lewat Workflow Engine 0.9 `entity_type='document_version'`). `document_categories.code` BEYOND kolom literal PRD §5 (yang cuma sebut `name`) — dibutuhkan token `{CATEGORY_CODE}` NumberingService (0.10), unique per tenant. BELUM ada controller HTTP (pola sama 1.1-1.6 — Files task 2.1 TIDAK menyebut `apps/web`, beda 1.7) — `dms.*` (14 permission) sudah di-seed RBAC baseline, siap digerbangi begitu ada endpoint. `DOCUMENT_CONTROLLER` — role BARU beyond roster 13-role Master PRD §8.2 (PRD Modul 03 §3 eksplisit: "role baru khusus modul ini", bukan oversight).
  **Platform extension signifikan — `WorkflowApproverType.CONTEXT_USER` (baru)**: Stage 1 "Document Owner" (PRD §4.1) adalah SATU user spesifik yang BEDA per dokumen (`documents.owner_user_id`) — TIDAK ada approverType 0.9 yang cocok (`SPECIFIC_USER` fixed sama di semua instance; `ROLE_IN_SCOPE` himpunan via role, bukan satu user terikat entitas). `ApproverResolutionService.resolveApprovers()`/`WorkflowEngineService.createTasksForStage()` diperluas mengalirkan `workflow_instances.context_data` turun (`contextData.contextUserId`, key generik supaya modul Phase 3+ lain dgn kebutuhan serupa reuse tanpa enum value baru) — `DocumentVersionService.submitForApproval()` mengisinya dari `documents.owner_user_id` saat `startInstance()`. Workflow default 2-stage per tenant (Stage 1 CONTEXT_USER Document Owner, Stage 2 ROLE_IN_SCOPE HSE_MANAGER) di-**lazy-create** (`DmsBootstrapService.ensureWorkflowDefinition()`, find-or-create idempotent) begitu tenant pertama kali submit — TIDAK ada authoring service utk `workflow_definitions` di codebase ini (gap sejak 0.9), sama pola `ensureNumberingConfig()` utk `numbering_configs` (DMS jadi **konsumen produksi PERTAMA** `NumberingService.generateNext()` — 0.10-1.7 belum pernah dipanggil modul domain nyata di luar test). PRD §4.1 "Stage 3 Publikasi otomatis oleh sistem" BUKAN `workflow_stages` ketiga (WorkflowEngineService mensyaratkan tiap stage py approver sungguhan) — direalisasikan sbg reaksi domain ke event, lihat poin berikut.
  **`DocumentWorkflowCompletionListener` — KONSUMEN SUNGGUHAN PERTAMA `WORKFLOW_INSTANCE_COMPLETED_EVENT`** (event ada sejak 0.9; banner comment ASLINYA menduga 0.11/1.4 yang akan dengar — diverifikasi GREP BERSIH di seluruh `src/` sebelum task ini, keduanya TIDAK PERNAH implementasi listener). **Temuan race condition KRITIS, diverifikasi EMPIRIS via 2 probe manual** (dihapus setelah dipakai, bukan spekulasi): (1) Prisma interactive `$transaction` yang "dinested" pada `PrismaClient` yang SAMA (mis. `NotificationService.enqueue()`/`WorkflowEngineService.startInstance()` dipanggil dari DALAM `withRls()` lain) TIDAK hang (connection pool memberi koneksi berbeda ke panggilan dalam) TAPI JUGA TIDAK benar-benar atomik (dua transaksi independen, bukan SAVEPOINT) — seluruh call site di modul ini (`DocumentService.retire()`, `DocumentVersionService.submitForApproval()`, `DocumentService.createDocument()`, listener ini sendiri) SENGAJA memisahkan transaksi secara eksplisit alih-alih nesting yang menyesatkan. (2) LEBIH KRITIS: `eventEmitter.emit()` (fire-and-forget, dipanggil DI DALAM `evaluateTransitionInTx()`'s `$transaction` SEBELUM commit sungguhan) membuat listener yang re-query row yang BARU SAJA disentuh transaksi yang sedang emit TERBUKTI melihat state PRA-commit di **21 dari 30 percobaan berulang** (listener dgn koneksi pre-established, pola DI NestJS sungguhan) — race NYATA, bukan teoretis. Diperbaiki dgn **memperluas payload event itu sendiri** (`WorkflowInstanceCompletedEvent` + `entityType`/`entityId`, sudah tersedia di memory `instance` SEBELUM emit, TIDAK butuh query tambahan) — listener manapun TIDAK PERNAH re-query `workflow_instances`/`workflow_tasks` (satu-satunya tabel yang genuinely race), field lain (`document_versions`/`documents`) yang dibaca listener AMAN krn ditulis transaksi TERPISAH yang sudah lama commit (`submitForApproval()`, request/waktu berbeda sama sekali). Listener menegakkan BR-02/BR-03 (supersede otomatis + `current_version_id` hanya PUBLISHED) saat APPROVED, membuat `document_review_schedule` pertama (`documents.review_cycle_months`), dan menandai `REJECTED` + `documents.status` kembali `DRAFT`/`PUBLISHED` (tergantung py `current_version_id` atau belum) saat ditolak.
  **BR-01 (numbering)** — pattern `{PREFIX}/{CATEGORY_CODE}/{YYYY}/{SEQ:3}`, BEYOND/deviasi dari pattern disarankan PRD §5 (`.../{SITE_CODE}/...`) — `{SITE_CODE}` SENGAJA DIJATUHKAN krn `documents.site_id` NULLABLE (dokumen tenant-wide sah, PRD §5 sendiri eksplisit) sedangkan `renderNumberPattern()` (0.10) GAGAL EKSPLISIT kalau token dipakai tanpa `variables`; SATU counter tenant-wide lintas SEMUA kategori (NumberingConfig scope_level cuma TENANT/COMPANY/SITE, tidak ada level "per kategori"). **BR-02/03** ditegakkan listener (di atas). **BR-04** (acknowledgement) — `PENDING→VIEWED→ACKNOWLEDGED`, E_SIGNATURE boleh skip VIEWED (PRD literal); **OVERDUE SENGAJA TIDAK diperlakukan terminal** (BEYOND diagram literal PRD §5) — late-acknowledgement (`OVERDUE→VIEWED/ACKNOWLEDGED`) diizinkan krn memblokir user yang telat mengakui SELAMANYA kontraproduktif utk tujuan modul ini (bukti kepatuhan ISO 45001 §7.5). **BR-05/07** (distribusi) ditegakkan `DocumentDistributionService` — target USER/ROLE/DEPARTMENT/SITE/COMPANY/ALL_TENANT diekspansi jadi `read_acknowledgement_logs` via query LANGSUNG ke `sites`/`departments`/`users`/`user_roles` (BUKAN impor `RbacModule`/`OrganizationModule` — pola PERSIS `UserService.assignRole()` 1.3, `companyId` SUDAH didenormalisasi ke sites/departments sejak 1.1 justru utk query ini). PRD §4.1 poin 6 "sistem otomatis membuat `document_distribution`" SENGAJA TIDAK diimplementasikan — `document_categories` (PRD §5) TIDAK py kolom "target distribusi default" apa pun, tidak ada tempat menyimpan konfigurasi itu; Document Controller membuat distribusi SECARA MANUAL setelah publish (gap TDD §26) — notifikasi `DOCUMENT_PUBLISHED` ("Dokumen baru wajib dibaca", PRD §8) karena itu terkirim DI TITIK INI (recipient list baru ada), BUKAN saat publish. **BR-06** (review overdue) + H-30 reminder — job `document-review-scan` (cron 01:15), idempotency via kolom BARU `reviewReminderSentAt` (BEYOND literal PRD §5, pola PERSIS `WorkflowTask.escalatedAt` 0.9) supaya job yang absen tepat H-30 tetap "mengejar" hari berikutnya tanpa reminder ganda.
  **Attachment (0.12) REUSE APA ADANYA** utk file fisik `document_versions` — PRD §5 EKSPLISIT file LANGSUNG di kolom `document_versions` sendiri (BUKAN via `attachments` generik), tapi internal pipeline presign/confirm/scan 0.12 dipakai UTUH via teknik client-proposed-entityId (documentVersionId digenerate SEBELUM attachment confirm, dipakai sbg `entityId`, dibaca balik dari `attachment.entityId` saat `createVersion()`) — pola PERSIS `data_import_jobs` (1.6, gap #56). Malware-scan TIDAK diblok di `createVersion()` (versi DRAFT boleh py scan pending) — dicek `submitForApproval()` (`scanStatus!=="CLEAN"` → `BadRequestException`), UX lebih masuk akal drpd blocking synchronous di upload. Notification templates (5 dari 6 event PRD §8, `DOCUMENT_PUBLISHED`/`DOCUMENT_VERSION_REJECTED`/`DOCUMENT_RETIRED`/`DOCUMENT_REVIEW_DUE`/`DOCUMENT_ACKNOWLEDGEMENT_OVERDUE`) di-seed `prisma/seed-dms-notification-templates.ts` (system default, IN_APP+EMAIL — EMAIL turut diseed krn `resolveAdditionalChannels()` 0.11 bisa memasukkannya kalau user opt-in, dan `NotificationService.enqueue()` throw DI TENGAH transaksi kalau template channel yang diresolusi tidak ada) — **konsumen produksi PERTAMA** yang genuinely butuh baris `notification_templates` di luar test (sama alasan `numbering_configs`). "Versi diajukan approval → notifikasi approver" (PRD §8 baris 1) SENGAJA belum di-wire (gap TDD §26, pola konsisten seluruh task domain 1.1-1.7 — notifikasi selalu deprioritized dari business-rule inti). `NOTIFICATION`+`DMS` module_code ditambahkan proaktif ke SELURUH `SUBSCRIPTION_PLAN_SEEDS.includedModuleCodes` (pelajaran langsung gap yang ditemukan 1.7 — dicegah SEBELUM terjadi kali ini, bukan ditemukan reaktif).
  **Temuan operasional besar (proses debug, bukan bug kode)**: run test penuh sempat TERLIHAT "hang" (>10 menit) — root cause SEBENARNYA adalah ~34 proses `node.exe` YATIM terakumulasi lintas sesi kerja (beberapa sejak berhari-hari sebelumnya) mengunci koneksi Postgres/Redis & CPU, DIPERPARAH oleh percobaan sendiri menjalankan 2 file test jest BERSAMAAN terhadap Postgres/Redis lokal yang SAMA (bukan Testcontainers terisolasi) — dibuktikan: setelah dibersihkan (`Stop-Process` seluruh node.exe KECUALI dev-server yang terverifikasi via `Get-NetTCPConnection`), test unit pure-function yang SAMA yang tadinya "hang" selesai normal dalam 14 detik. Kegagalan DI kode SUNGGUHAN yang ditemukan justru sepele & cepat diperbaiki setelah lingkungan bersih: `DmsModule` lupa mengimpor `NumberingModule` (`DocumentService` butuh `NumberingService`) — 1 baris fix, seluruh 18 integration test lulus first-try setelahnya.
  Diverifikasi: 51 unit test baru (`document-version-lifecycle.spec.ts` 20, `read-acknowledgement-lifecycle.spec.ts` 9, `document-distribution-rules.spec.ts` 11, `review-schedule-scan.spec.ts` 8, `read-acknowledgement-scan.spec.ts` 3 — beberapa lebih banyak dari jumlah blok `it()` literal krn `it.each` dihitung Jest per-case, angka di atas dari run nyata bukan hitung manual) + 3 unit test baru extend `approver-resolution.service.spec.ts` (CONTEXT_USER) + 18 integration test baru (`dms-lifecycle.integration-spec.ts` — alur approval 2-stage PENUH via `WorkflowEngineService` SUNGGUHAN bukan mock, BR-01 s/d BR-07, path reject, kedua scan job, isolasi tenant RLS) — **546/546 test lulus seluruh proyek** (`pnpm test`, naik dari 474, TERMASUK `permission-fail-closed.integration-spec.ts` yang sebelumnya flaky — run ini bersih), `pnpm build` sukses lintas 6/6 package buildable (`turbo run build --concurrency=1`, mitigasi EPERM Windows 1.7), dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 6/6 tabel, `audit_log_trigger` terpasang 5/6 (`read_acknowledgement_logs` SENGAJA tidak, system-generated murni pola `data_import_errors`/`usage_counters`), 38/38 FK constraint terverifikasi (termasuk `document_versions.workflow_instance_id → workflow_instances`, arah domain→platform yang diizinkan), 7/7 unique index/constraint kunci terverifikasi (`pg_indexes`), 3/3 seed script stabil pada re-run (`rbac-baseline`/`subscription-plans`/`dms-notification-templates`).
- [x] 2.2 Regulatory & Compliance Management (Modul 04) — `apps/api/src/modules/domains/regulatory-compliance/*`: modul DOMAIN KEENAM. 5 tabel baru — `regulatory_frameworks` (katalog GLOBAL, TANPA `tenant_id`, pola PERSIS `industry_templates` 1.2/`subscription_plans` 1.5, 11 baris seed ISO/PP/Permenaker/PTK007/PROPER/Kepmen ESDM) + `regulatory_register`/`compliance_obligations`/`compliance_evaluations`/`licenses_permits` (4 tabel tenant, RLS TRUE+FORCE). `linked_capa_id`/`renewal_of_license_id`(self-ref) bare UUID TANPA FK (Modul 10 CAPA belum ada tabelnya — pola PERSIS `workflow_instances.entity_id`). **`COMPLIANCE_OFFICER`** — role BARU menjawab Open Question #4 PRD Modul 04 §3 secara eksplisit (banner comment `seed-rbac-baseline.ts` mendokumentasikan resolusinya); **"Top Management"** (baris PRD sama, read-only) SENGAJA TIDAK diberi role baseline — PRD tidak menandainya "(role baru)" spt Compliance Officer, gap didokumentasikan (tenant butuh custom role via `RoleService` 1.3).
  **BR-01/02 + notifikasi bertingkat H-90/H-30/H-7 (PRD §8) — 2 kolom BEYOND skema literal ditemukan BUTUH mid-implementasi**: `licenses_permits` dapat 3 kolom (`expiryReminder{90,30,7}SentAt`, migration terpisah) krn PRD minta 3 checkpoint notifikasi INDEPENDEN (bukan 1 reminder tunggal spt DMS review schedule) — job harian yang absen beberapa hari boleh "mengejar" mengirim beberapa tier SEKALIGUS hari yang sama (bukan skip), masing-masing tier py idempotency KOLOM SENDIRI. `compliance_obligations` dapat 2 kolom serupa (`dueReminderSentAt`/`overdueNotifiedAt`, migration terpisah) utk 2 event PRD §8 lain ("evaluasi jatuh tempo"/"obligation overdue") yang **BEYOND cakupan rencana task awal** (semula cuma "license-expiry-scan job") — diperluas krn PRD §8 literal py 5 baris notifikasi utk modul ini, 2 di antaranya (baris 3/5) berbasis `next_due_date` yang sama sekali belum py job; diputuskan diimplementasikan penuh (bukan gap) krn pure-function pattern-nya identik dgn yang sudah ada (`review-schedule-scan.ts` 2.1) dan tidak menambah kerumitan arsitektur baru. Kedua kolom di-RESET NULL oleh `ComplianceEvaluationService.close()` (BR-06) — perpindahan `next_due_date` ke masa depan itulah yang "menyelesaikan" siklus due/overdue sebelumnya, satu-satunya titik reset.
  **Alur evaluasi 1-stage** (BEDA dari DMS 2-stage CONTEXT_USER+ROLE_IN_SCOPE) — `RegulatoryComplianceBootstrapService.ensureWorkflowDefinition()` lazy-create SATU stage ROLE_IN_SCOPE `HSE_MANAGER` saja (PRD §4.2 literal "1 stage Review HSE Manager", evaluasi tidak py konsep "pemilik" spesifik-entitas spt Document Owner, jadi CONTEXT_USER tidak relevan di sini). **BR-03** (`assertCanCloseEvaluation`) — `NON_COMPLIANT`/`PARTIALLY_COMPLIANT` wajib `linked_capa_id` terisi sebelum `CLOSED`; penutupan evaluasi SELALU tindakan eksplisit (`close()`), TIDAK PERNAH otomatis begitu `REVIEWED` tercapai — termasuk utk `COMPLIANT`/`NOT_APPLICABLE` yang sebenarnya tidak kena gate BR-03 (PRD tidak menyebut auto-close di manapun, tidak diasumsikan). `ComplianceEvaluationStatus` TIDAK py nilai `REJECTED` (beda `DocumentVersionStatus` 2.1) — `ComplianceEvaluationWorkflowCompletionListener` (KONSUMEN KEDUA `WORKFLOW_INSTANCE_COMPLETED_EVENT`, pola PERSIS `DocumentWorkflowCompletionListener`, race-condition-safe by construction krn payload event sendiri yang dipakai bukan re-query) mengembalikan `SUBMITTED→DRAFT` (bukan status terminal terpisah) saat REJECT. **BR-06** — `compliance_obligations.next_due_date` DAN `compliance_evaluations.next_evaluation_due_date` diisi NILAI SAMA dari `computeNextObligationDueDate()` yang SAMA di `close()` (PRD §4.2 poin 4 dibaca terjadi PADA SAAT SAMA dgn BR-06, sumber & frequency yang dirujuk identik) — aritmatika bulan `setUTCMonth()` mentah TANPA clamping akhir-bulan, pola PERSIS `document-review-schedule.service.ts` (2.1), BUKAN `addMonthsUtc()` (`audit-log-partition.ts` 0.13, dirancang utk partition boundary bukan next-due-date).
  **BR-04** (`assertRegisterAcceptsNewObligation`) — register `SUPERSEDED`/`REVOKED` tidak boleh diacu obligation BARU, obligation historis yang sudah ada tidak tersentuh. **BR-07** (`assertApplicableScopeConsistency`) — `SPECIFIC_SITE` wajib `applicable_site_id`; `SPECIFIC_COMPANY→applicable_company_id` DITAMBAHKAN BEYOND teks literal BR-07 (yang cuma sebut SPECIFIC_SITE) krn bentuk validasinya identik & mencegah celah data-integrity simetris yang sama. `RegulatoryRegisterService.seedFromIndustryTemplate()` — merealisasikan PRD §4.1 poin 1 ("`industry_templates.default_regulatory_codes` di-seed jadi entri awal `regulatory_register`"), method BERDIRI SENDIRI (pola `ensureNumberingConfig()` 2.1) **SENGAJA TIDAK** dipanggil otomatis dari `ProvisioningService.provisionTenant()` (1.5) — di luar timebox menyentuh ulang orkestrasi Phase 1 yang sudah shipped (gap TDD §26); kode yang tidak match baris `regulatory_frameworks` mana pun dilaporkan via `skippedCodes` (BUKAN exception) — semestinya nihil hari ini krn `seed-regulatory-frameworks.ts` sudah mencakup SELURUH kode yang dirujuk `seed-industry-templates.ts`.
  **`LicensePermitService`** — `createRenewal()` merealisasikan PRD §4.3 poin 3 (`renewal_of_license_id` self-ref), tapi baris LAMA **SENGAJA TIDAK dipaksa pindah status**: teks PRD "izin lama → status ACTIVE diubah menjadi status historis" dibaca sbg deskripsi HASIL (STRUKTURAL lewat rantai `renewal_of_license_id`) bukan instruksi FSM literal — `LicenseStatus` skema §5 CUMA py 5 nilai (`ACTIVE`/`EXPIRING_SOON`/`EXPIRED`/`REVOKED`/`IN_RENEWAL_PROCESS`), TIDAK ADA nilai "historis" terpisah, dan diagram transisi PRD §5 sendiri tidak menyebut edge ini sama sekali. **BR-05** (`findExpiredSioCertifications`, query-only helper) — teks PRD literal "`holder_type=SIO_OPERATOR_CERT`" ternyata KELIRU merujuk kolom: `SIO_OPERATOR_CERT` adalah nilai `license_type` (bukan `holder_type`, yang cuma `COMPANY`/`SITE`/`EQUIPMENT`/`INDIVIDUAL`) — dibaca sbg `licenseType=SIO_OPERATOR_CERT`; Modul 06 (Work Permit)/Modul 19 (Training) BELUM ADA di codebase, jadi TIDAK ADA enforcement APAPUN yang genuinely terjadi, murni helper query siap dipanggil nanti (gap TDD §26). Upload bukti perpanjangan (PRD §4.3 poin 2) disederhanakan jadi transisi status murni (`markInRenewalProcess()`) TANPA wiring `attachments` generik (0.12) — `licenses_permits` tidak py kolom/entity_type baku utk lampiran itu di skema literal (gap TDD §26).
  Notification templates (6 event type × IN_APP+EMAIL = 12 baris, `prisma/seed-regulatory-compliance-notification-templates.ts`) — 5 dari PRD §8 literal + `COMPLIANCE_EVALUATION_REJECTED` (BEYOND literal, analog `DOCUMENT_VERSION_REJECTED` 2.1). `LICENSE_EXPIRY_REMINDER` SATU eventType utk KETIGA tier H-90/H-30/H-7 (PRD §8 satu baris literal utk ketiganya) — `{{daysRemaining}}` membedakan tier di teks. `NOTIFICATION`+`COMPLIANCE` module_code ditambahkan proaktif ke SELURUH `SUBSCRIPTION_PLAN_SEEDS.includedModuleCodes` (pola konsisten sejak gap 1.7 ditemukan).
  **Kali ini `NumberingModule` TIDAK terlupa** (gap 2.1 task 86 eksplisit tidak diulang) — `regulatory-compliance.module.ts` diperiksa ulang manual sebelum test pertama jalan.
  Diverifikasi: 72 unit test baru (`license-expiry-scan.spec.ts` 25, `compliance-evaluation-lifecycle.spec.ts` 23, `obligation-due-scan.spec.ts` 15, `regulatory-register-rules.spec.ts` 9 — angka dari run nyata per file, bukan hitung manual) + 19 integration test baru (`regulatory-compliance-lifecycle.integration-spec.ts` — alur evaluasi 1-stage PENUH via `WorkflowEngineService` SUNGGUHAN, BR-01/02/03/04/06/07, path reject, renewal chain, kedua scan job, isolasi tenant RLS) — **637/637 test lulus seluruh proyek** (`pnpm test`, naik dari 546; satu kegagalan suite `numbering/missing-config.integration-spec.ts` "Connection is closed" muncul di run penuh — dikonfirmasi PRE-EXISTING flaky Redis teardown lintas-worker-paralel via re-run terisolasi 3/3 bersih, BUKAN regresi task ini, file tidak pernah disentuh sesi ini), `pnpm build`/`turbo run build --concurrency=1` sukses 6/6 package, dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 4/4 tabel tenant (`regulatory_frameworks` FALSE/FALSE sesuai desain global), `audit_log_trigger` terpasang 5/5 tabel (BEDA dari DMS yang mengecualikan 1/6 — modul ini tidak py tabel "hasil ekspansi otomatis sistem"), 5/5 kolom idempotency reminder baru terverifikasi ada, notification templates 12/12 baris idempotent pada re-run.

### Phase 3 — Inti Operasional K3 (Risk Management, Work Permit, Incident Management, Inspection Management, Emergency Response)

- [x] 3.1 Risk Management — matriks risiko konfigurabel (Modul 05) — `apps/api/src/modules/domains/risk-management/matrix/*`: modul DOMAIN KETUJUH, awal Phase 3. Cakupan task ini DIBATASI SENGAJA ke 4 tabel — `risk_matrix_configs`/`risk_matrix_levels`/`risk_matrix_cells`/`hazard_register` — dari Modul 05 §5 yang literalnya jauh lebih besar (`hira_assessments`/`jsa_records`/`hiradc_records`/`risk_register`/`risk_treatment_plans` DITUNDA ke task 3.2 terpisah, TASK_INSTRUCTION.md eksplisit memisah keduanya). `risk_matrix_levels`/`risk_matrix_cells` py audit MINIMAL (`createdAt`/`updatedAt` saja, TANPA `created_by`/`updated_by`/`deleted_at`) sesuai literal PRD §5 — beda dari `risk_matrix_configs`/`hazard_register` yang audit standar penuh, TAPI tetap melekatkan `audit_log_trigger` (bukan "hasil ekspansi otomatis sistem", baris ditulis EKSPLISIT oleh Tenant Admin lewat Visual Builder PRD §12). `risk_level` (`risk_matrix_cells`) disimpan sbg `VARCHAR(30)` BUKAN Prisma enum — PRD §5 eksplisit "label dapat dikustomisasi tenant", satu-satunya field status di seluruh codebase ini yang bukan enum tetap. BELUM ada controller HTTP (pola sama seluruh modul domain Phase 2+ sejauh ini) — `risk.matrix_config.manage`/`risk.hazard.propose`/`risk.hazard.manage` (HANYA 3 kode utk cakupan task ini, `risk.hira.*`/`risk.jsa.*`/`risk.hiradc.*`/`risk.corporate_risk.*`/`risk.treatment.*` PRD §3 lainnya ditunda ke 3.2 seiring tabelnya) sudah di-seed RBAC baseline (93 permission, naik dari 90), granted `TENANT_ADMIN`+`HSE_MANAGER` (matrix_config+hazard.manage), `HSE_OFFICER`+`SUPERVISOR` (hazard.propose — "Supervisor Lapangan" PRD cocok literal `SUPERVISOR` role code yang SUDAH ada di roster sejak 0.8, belum pernah dapat grant eksplisit sebelum task ini).
  **BR-09 (versioning tanpa mengubah skor historis)** — `RiskMatrixConfigService.createNewVersion()`: baris LAMA disentuh HANYA utk `isActive->false` (SELURUH kolom lain, termasuk levels/cells anaknya, TIDAK PERNAH diupdate) — deactivate dilakukan SEBELUM insert baris baru dalam TRANSAKSI YANG SAMA (bukan dua transaksi terpisah) supaya partial unique index (di bawah) tidak pernah sempat melihat 2 baris aktif utk scope yang sama di titik mana pun. TIDAK ADA kolom self-referencing "supersedes"/"previous_version_id" (PRD §5 literal tidak memintanya, beda dari `RegulatoryRegister.supersededByRegisterId` 2.2 yang eksplisit diminta) — rangkaian versi cukup diidentifikasi via `(tenantId, applicableModuleScope, version)`. Partial unique index `WHERE is_active=true` pada `(tenant_id, applicable_module_scope)` — TIDAK bisa diekspresikan `@@unique` DSL Prisma biasa, ditulis manual di migration terpisah, pola PERSIS `numbering_configs` (0.10)/`notification_templates` (0.11); `create()` kedua kali utk scope yang sudah py versi aktif ditolak bersih via `withCleanUniqueViolation()` (P2002 -> `ConflictException`, pesan mengarahkan caller ke `createNewVersion()`).
  **`RiskMatrixConfigService.resolveActiveConfig(scope)`** — fallback 2-tingkat (config scope-spesifik mis. HIRA menang kalau aktif, baru fallback ke scope `ALL`) pola PERSIS "NULL berarti berlaku lebih luas" (`DocumentService.listPublishedForCurrentUser()` 2.1 utk site/department) — disiapkan utk dipanggil task 3.2 saat `hira_assessments`/dst butuh tahu matriks mana yang berlaku, BELUM ADA pemanggil produksi nyata di task ini sendiri. **`RiskMatrixBootstrapService.ensureDefaultMatrix()`** — lazy-create matriks default "Matriks Risiko K3 Standar 5x5" (formula multiplicative likelihood×severity, RESOLUSI Open Question #1 PRD §13 KHUSUS kasus default — tenant tetap bebas simpan skor non-linear apa pun via Visual Builder manual sesudahnya) begitu tenant belum py matriks aktif utk suatu scope, pola PERSIS `DmsBootstrapService`(2.1)/`RegulatoryComplianceBootstrapService`(2.2) — SENGAJA TIDAK dipanggil dari `ProvisioningService` (1.5), gap konsisten sejak 2.1/2.2. Band skor default (LOW 1-4/MEDIUM 5-9/HIGH 10-15/EXTREME 16-25) dipilih cocok BERSIH dgn seluruh 14 nilai skor yang genuinely *achievable* dari grid 5x5 (1,2,3,4,5,6,8,9,10,12,15,16,20,25 — BUKAN tiap integer 1-25 dicapai perkalian i×j, i,j∈[1,5]).
  **BR-01 (skor otomatis dari matriks, bukan input manual)** — `resolveRiskScore()` (`risk-matrix-lookup.ts`) HANYA lookup murni (fail loud kalau kombinasi likelihood/severity tidak py cell) — konsumen SUNGGUHAN (`hira_hazard_lines.risk_score_before/after`, `hiradc_lines.risk_score`, `risk_register.risk_score_inherent/residual`) adalah tabel task 3.2, BELUM ADA di task ini; fungsi ini disediakan sbg fondasi siap pakai.
  **BR-07 (`hazard_register` tidak dapat dihapus permanen selama masih direferensikan)** — TIDAK ADA method hard-delete di `HazardRegisterService` sama sekali (konvensi soft-delete universal codebase ini sejak awal), jadi terpenuhi *by construction*; validasi REFERENSIAL ke `hira_hazard_lines`/`jsa_step_hazards`/`hiradc_lines` (tabel task 3.2) TIDAK bisa dicek sekarang krn tabel-tabel itu belum ada — `retire()` saat ini TIDAK memvalidasi apa pun selain baris itu sendiri ada (gap TDD §26, task 3.2 wajib menutupnya begitu tabel anak dibuat).
  Diverifikasi: 19 unit test baru (`risk-matrix-lookup.spec.ts` 5, `default-risk-matrix.spec.ts` 14 — angka dari run nyata, termasuk verifikasi band boundary bersih di seluruh nilai skor achievable) + 14 integration test baru (`risk-matrix-lifecycle.integration-spec.ts` — BR-09 versioning penuh, partial unique index/`ConflictException`, `resolveActiveConfig()` 2-tingkat, `ensureDefaultMatrix()` idempotent 25 cells+10 levels, `HazardRegisterService` CRUD+retire+site-scoped fallback, isolasi tenant RLS) — **670/670 test lulus seluruh proyek** (`pnpm test`, naik dari 637, run BERSIH tanpa flake kali ini), `pnpm build`/`turbo run build --concurrency=1` sukses 6/6 package, dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 4/4 tabel, partial unique index `risk_matrix_configs_active_version_key` terverifikasi tepat (`WHERE is_active=true`), `audit_log_trigger` terpasang 4/4 tabel.

- [x] 3.2 HIRA / JSA / HIRADC — alur transaksional (Modul 05 sisa) — `apps/api/src/modules/domains/risk-management/hira-jsa-hiradc/*`: SUBMODUL SAUDARA `risk-management/matrix` (3.1) di bawah domain Modul 05 yang SAMA (BUKAN modul domain kedelapan terpisah), TIDAK mengimpor `RiskMatrixModule` — akses lintas submodul (mis. `HazardRegisterService.retire()` mengecek `hira_hazard_lines`/`jsa_step_hazards`/`hiradc_lines`) SELALU lewat query Prisma langsung, nol coupling NestJS DI. 11 tabel baru (`hira_assessments`/`hira_team_members`/`hira_hazard_lines`, `jsa_records`/`jsa_job_steps`/`jsa_step_hazards`/`jsa_crew_acknowledgements`, `hiradc_records`/`hiradc_lines`, `risk_register`/`risk_treatment_plans`) + 11 enum baru, 5 service (`HiraAssessmentService`/`JsaRecordService`/`HiradcRecordService`/`RiskRegisterService`/`RiskTreatmentPlanService`), 3 `WorkflowCompletionListener` (konsumen KETIGA/KEEMPAT/KELIMA `WORKFLOW_INSTANCE_COMPLETED_EVENT` di seluruh codebase, pola PERSIS DMS 2.1/Compliance 2.2), 3 job cron baru (`hiradc-expiry-scan` 02:15, `risk-register-review-scan` 02:30, `hira-review-due-scan` 02:45). BELUM ada controller HTTP (pola sama seluruh modul domain Phase 2+) — `risk.hira.*`/`risk.jsa.*`/`risk.hiradc.*`/`risk.corporate_risk.*`/`risk.treatment.*` (18 kode baru) sudah di-seed RBAC baseline, granted `TENANT_ADMIN`/`HSE_MANAGER`/`DEPARTMENT_HEAD`/`SUPERVISOR`/`HSE_OFFICER`/`AUDITOR_INTERNAL`/`AUDITOR_EXTERNAL`, DAN grant PERTAMA utk `WORKER_EMPLOYEE`/`CONTRACTOR_USER` (role sudah ada sejak 0.8, belum pernah dapat grant eksplisit apa pun sebelum task ini).
  **E2E-3 (percabangan kondisional risiko EXTREME) — KONSUMEN SUNGGUHAN PERTAMA JSON Logic condition pada `workflow_transitions` di seluruh codebase** (kolom `condition` ada sejak 0.9, SELALU `null`/unconditional sampai task ini). `RiskWorkflowBootstrapService.ensureHiraWorkflowDefinition()` — workflow HIRA 3-stage (Review Supervisor → Approval HSE Manager → [kondisional] Approval Company HSE Head), DUA baris `workflow_transitions` dari stage2 dgn `triggerAction=APPROVE` SAMA tapi `condition`+`priority` BEDA: priority 0 mengecek `contextData.hasExtremeHazard===true` (JSON Logic `{"==":[{"var":"hasExtremeHazard"},true]}`) → lanjut stage3; priority 1 fallback (`condition=null`) → langsung terminal `APPROVED` kalau priority 0 tidak match. `contextData.hasExtremeHazard` diisi `HiraAssessmentService.submitForApproval()` SEBELUM `startInstance()` (dihitung dari `resolveRiskScore()` FRESH per baris `hira_hazard_lines` terhadap `likelihood_before`/`severity_before` — BUKAN kolom `requiresEscalation` tersimpan yang merepresentasikan AFTER, dua momen berbeda) — `WorkflowEngineService` sendiri TIDAK PERNAH fetch data domain (menjaga batas modular monolith). Diverifikasi end-to-end: HIRA tanpa hazard EXTREME selesai 2 stage (TIDAK PERNAH ada task stage-3 dibuat sama sekali), HIRA dgn hazard EXTREME (`likelihood_before×severity_before≥16`) memicu stage3 `COMPANY_ADMIN` (role `TOP_MANAGEMENT` tidak ada di roster, dipakai sbg pengganti plausible — lihat TDD §26 #96) sebelum terminal `APPROVED`.
  **`risk_matrix_cells.requiresEscalation` (`Boolean`, task 3.2)** — flag terstruktur BARU didenormalisasi ke `hira_hazard_lines`/`hiradc_lines` (dari cell AFTER)/`risk_register` (dari cell RESIDUAL) — resolusi TDD §26 #93 (lihat detail) atas tegangan antara business rule PRD yang ditulis sbg perbandingan string `risk_level=EXTREME` vs `risk_level` yang SENGAJA `VARCHAR(30)` bebas dikustomisasi tenant (keputusan task 3.1 sendiri). **BR-02** (`assertControlsPresentIfStillHighRisk`) — `additional_controls_required` wajib kalau `risk_level_after` masih `HIGH`/EXTREME (requiresEscalation AFTER), ditegakkan `submitForApproval()`/`verify()` SEBELUM approval dimulai, bukan di listener. **BR-06** (`assertAcceptRequiresTopManagementApproval`) — `risk_treatment_plans.treatment_strategy=ACCEPT` pada source `requiresEscalation=true` (polymorphic — `RiskRegisterService`/`HiraHazardLine`/`HiradcLine` sesuai `source_type`) wajib `topManagementApprovedBy` terisi; strategi LAIN (mis. `REDUCE_MITIGATE`) TIDAK kena gate ini sama sekali.
  **JSA** (`DRAFT→APPROVED→ACTIVE`, TIDAK py status "sedang direview" terpisah spt HIRA — status TETAP `DRAFT` sepanjang approval berjalan) — reject TIDAK mengubah status apa pun (sudah `DRAFT`), cukup null-kan `workflow_instance_id` utk resubmission (unique constraint izin banyak NULL). `JsaCrewAcknowledgementService.acknowledge()` — sign-off kru per shift (ISO 45001 §5.4), unique DB `(jsa_id,user_id,work_date)` diterjemahkan `ConflictException` bersih, hanya JSA `ACTIVE` bisa diakui. **HIRADC** (`DRAFT→VERIFIED→APPROVED`, `APPROVED` OPSIONAL) — `verify(hiradcId, useWorkflow)` dua jalur mendarat status SAMA: `useWorkflow=false` self-verify langsung (PRD §4.3 "risiko rendah rutin"), `useWorkflow=true` via workflow 1-stage "Verifikasi Supervisor" (listener memetakan `APPROVED`→`VERIFIED`, BUKAN `APPROVED` literal — nama stage). **BR-03** (`assertHasBaselineOrStandaloneLines`) — wajib `related_hira_id`/`related_jsa_id` ATAU minimal 1 `hiradc_lines` mandiri.
  **`RiskRegisterService`** — `risk_score_inherent`/`residual` DAN `risk_appetite_status` (`EXCEEDS_APPETITE`/`WITHIN_APPETITE`) diturunkan otomatis dari `resolveRiskScore()` (task 3.1) thd cell RESIDUAL; `updateResidual()` merevisi HANYA sisi residual, `risk_score_inherent` tidak pernah berubah retroaktif (historis). Siklus status LINEAR literal PRD §5 (`IDENTIFIED→ASSESSED→TREATMENT_IN_PROGRESS→MONITORED→CLOSED`, TANPA jalur skip utk risiko `WITHIN_APPETITE` — keputusan SADAR tetap literal, TDD §26 #103). Notification templates (11 baris, 6 event type, `prisma/seed-risk-management-notification-templates.ts`) mencakup 2 PRD §8 gap yang ditemukan MID-IMPLEMENTASI dan LANGSUNG ditutup (bukan dideferred) — HIRA review-due reminder (`reviewReminderSentAt`+job baru) & `risk_treatment_plans` overdue (`overdueNotifiedAt`, digabung ke job `risk-register-review-scan` yang sama, BUKAN job terpisah — 2 tabel beda, 1 concern "tinjauan berkala risiko" yang sama, lihat TDD §26 #99).
  Diverifikasi: 110 unit test baru (`hira-lifecycle.spec.ts` 16, `jsa-lifecycle.spec.ts` 11, `hiradc-lifecycle.spec.ts` 12, `risk-register-lifecycle.spec.ts` 10, `hira-review-due-scan.spec.ts` 10, `risk-treatment-lifecycle.spec.ts` 9, `risk-register-review-scan.spec.ts` 9, `hira-hazard-line-rules.spec.ts` 8, `hiradc-expiry-scan.spec.ts` 7, `risk-treatment-overdue-scan.spec.ts` 7, `risk-treatment-rules.spec.ts` 6, `jsa-work-permit-validation.spec.ts` 5 — angka dari run nyata 12 file terisolasi) + 23 integration test baru (`hira-jsa-hiradc-lifecycle.integration-spec.ts` — **E2E-3 penuh 2-stage vs 3-stage**, BR-02 gate, JSA acknowledge+reject, HIRADC BR-03+self-verify+workflow, risk register siklus penuh, BR-06 accept-gate, hazard register BR-07 lintas-submodul, ketiga scan job, isolasi tenant RLS) — **814/814 test lulus seluruh proyek** (naik dari 670; run penuh `pnpm test` melaporkan 804/804 dgn 1 suite `user-role/role.integration-spec.ts` gagal start "Connection is closed" — dikonfirmasi PRE-EXISTING flaky Redis teardown via re-run terisolasi 10/10 bersih, file tsb TIDAK PERNAH disentuh task ini), `pnpm build`/`turbo run build --concurrency=1` sukses 6/6 package (termasuk `apps/worker` — 3 worker bootstrap file baru deep-import `@qhse/api/dist/...` yang hanya resolve SETELAH `apps/api` dibuild lebih dulu, urutan diverifikasi manual).

- [x] 3.3 Work Permit — tipe & alur inti (Modul 06) — `apps/api/src/modules/domains/work-permit/*`: modul DOMAIN KEDELAPAN (BEDA dari `risk-management/*` — modul TERPISAH, bukan submodul saudara; hanya mereferensikan `related_jsa_id` ke `JsaRecord` lewat FK Prisma langsung, TIDAK mengimpor `HiraJsaHiradcModule`). 6 tabel baru (`work_permit_types`/`work_permits`/`work_permit_hazard_checklist`/`gas_test_results`/`isolation_loto_records`/`work_permit_approvals`) + 7 enum baru, 5 service (`WorkPermitTypeService`/`WorkPermitService`/`GasTestResultService`/`IsolationLotoRecordService`/`WorkPermitApprovalCacheService`), 1 `WorkflowCompletionListener` (konsumen KEENAM `WORKFLOW_INSTANCE_COMPLETED_EVENT`). Cakupan DIBATASI SENGAJA (TASK_INSTRUCTION.md 3.3 vs 3.4) — `work_permit_extensions`/`work_permit_closures` DITUNDA task 3.4 terpisah; TIDAK ADA job cron baru task ini. BELUM ada controller HTTP — `work_permit.*` (11 kode) sudah di-seed RBAC baseline, granted `TENANT_ADMIN` (override penuh) / `SUPERVISOR` (Issuer, stage 1) / `HSE_MANAGER` (HSE, stage 2 + cancel) / `HSE_OFFICER` (gas test+LOTO) / `WORKER_EMPLOYEE`+`CONTRACTOR_USER` (Requester, create/update/submit) / `AUDITOR_INTERNAL`+`AUDITOR_EXTERNAL` (read+export). TDD §26 gap #101 (task 3.2) DITUTUP — FK sungguhan `hiradc_records.work_permit_id → work_permits` ditambahkan (satu migration diff dgn init 6 tabel, Prisma menghasilkannya otomatis).
  **BR-04 (percabangan kondisional Stage 2 HSE) — KEDUA kalinya JSON Logic condition genuinely dipakai pada `workflow_transitions` di seluruh codebase** (setelah HIRA, 3.2). `WorkPermitWorkflowBootstrapService.ensureWorkflowDefinition()` — workflow 1/2-stage (Review Issuer/Area Authority SELALU → [kondisional] Approval HSE), pola transisi IDENTIK HIRA: dua baris dari stage1 dgn `triggerAction=APPROVE` sama tapi `condition`+`priority` beda. `contextData.hasHseStage` = `computeHasHseStage(riskLevel, requiresHseApproval)` = `risk_level==="HIGH" OR work_permit_types.requires_hse_approval` — BEDA dari HIRA: `work_permits.risk_level` ADALAH Prisma enum TERTUTUP (bukan `VARCHAR` bebas spt `risk_matrix_cells.risk_level`), jadi perbandingan string "HIGH" langsung AMAN, tidak butuh flag terstruktur terpisah spt `requiresEscalation` Modul 05. Diverifikasi end-to-end: `risk_level=HIGH` MAUPUN `requires_hse_approval=TRUE` (independen) sama-sama memicu Stage 2; `risk_level=MEDIUM` + `requires_hse_approval=FALSE` selesai 1 stage.
  **Numbering `scope_level=SITE` — PERTAMA di codebase yang genuinely mempartisi counter per-site** (BEDA dari SELURUH precedent `scope_level=TENANT` sebelumnya, TDD §26 #106) — `ensureNumberingConfig(siteId)` di-parameterisasi per site (beda dari bootstrap service lain yang parameterless), diverifikasi empiris (2 permit site sama → sequence 0001/0002, permit site LAIN pada tenant sama tetap mulai 0001, bukan lanjut 0003).
  **BR-09 (segregation of duty) — `WorkPermitService.actOnApprovalTask()`, wrapper BARU di atas `WorkflowEngineService.actOnTask()` generik** (BEYOND pola precedent — HIRA/JSA/HIRADC/DMS/Compliance SEMUA panggil `actOnTask()` langsung, TDD §26 #111) — `ROLE_IN_SCOPE` tenant-wide (0.9) SECARA TEORETIS bisa meresolusi requester sendiri sbg approver (site kecil, rangkap jabatan Supervisor); wrapper mengecek `assertRequesterNotApprover()` SEBELUM delegasi ke `actOnTask()`, sekaligus me-refresh `work_permit_approvals` (cache). **BR-02/BR-03 (verifikasi keselamatan pra-aktivasi)** — `WorkPermitService.activate()` gate eksplisit (`APPROVED→ACTIVE`, BUKAN otomatis begitu approval selesai): gas test `PASS` minimal 1 (kalau `requires_gas_test`), SELURUH `isolation_loto_records` `VERIFIED` (kalau `requires_loto`, diperluas "minimal 1 record wajib ADA" — TDD §26 #109) dgn `verified_by≠applied_by` (prinsip verifikasi independen, ditegakkan `IsolationLotoRecordService.verify()`).
  `work_permit_approvals` — cache/read-model, PRD §5 eksplisit "BUKAN pengganti Workflow Engine". `WorkPermitApprovalCacheService.refresh()` membaca `workflow_instances`/`workflow_tasks` LANGSUNG (bukan event-driven per-stage — TIDAK ADA hook itu di `WorkflowEngineService`, TDD §26 #112), dipanggil di 3 titik (submit/`actOnApprovalTask`/listener). Notification templates (4 baris, 2 event type — HANYA yang genuinely dipicu listener saat ini, TDD §26 #113) `prisma/seed-work-permit-notification-templates.ts`.
  Diverifikasi: 26 unit test baru (`work-permit-lifecycle.spec.ts` 13, `work-permit-activation-rules.spec.ts` 8, `work-permit-hse-stage-rules.spec.ts` 3, `work-permit-segregation-of-duty.spec.ts` 2 — angka dari run nyata 4 file terisolasi) + 13 integration test baru (`work-permit-lifecycle.integration-spec.ts` — alur PENUH 1-stage & 2-stage BR-04, reject terminal, BR-09 segregation-of-duty, BR-02/BR-03 activation gate + verifikasi independen LOTO, cancel, numbering `scope_level=SITE` 3-permit 2-site, isolasi tenant RLS) — **843/843 test lulus seluruh proyek** (naik dari 814, run BERSIH tanpa flake — termasuk `user-role/role.integration-spec.ts` yang sempat flaky di window sebelumnya), `pnpm build`/`turbo run build --concurrency=1` sukses 6/6 package, dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 6/6 tabel, `audit_log_trigger` terpasang 6/6 tabel, FK `hiradc_records_work_permit_id_fkey` terverifikasi ada, `numbering_configs` `scope_level=SITE` 42 baris genuinely per-site (dibedakan dari 35 baris `scope_level=TENANT` PRE-EXISTING TIDAK TERKAIT dari test `NumberingService` generik lain yang kebetulan pakai string "WORK_PERMIT" sbg contoh module_code — collision KEDUA dgn data stale sesi ini setelah kode permission `work_permit.permit.read`/`work_permit.permit.approve`, TIDAK saling bertabrakan krn ter-partisi `tenant_id` berbeda).
- [x] 3.4 Work Permit — extension, closure & cross-link (Modul 06) — `apps/api/src/modules/domains/work-permit/*`: 2 tabel baru (`work_permit_extensions`, `work_permit_closures`) + 2 enum status — lokasi FLAT langsung di `work-permit/*` (BUKAN subdirektori `extension-closure/*` yang disebut literal TASK_INSTRUCTION.md, deviasi disengaja demi konsistensi modul, TDD §26 #119).
  **Extension — KETIGA kalinya JSON Logic condition genuinely dipakai pada `workflow_transitions` di seluruh codebase** (setelah HIRA 3.2, Work Permit Stage 2 HSE 3.3) — `WorkPermitWorkflowBootstrapService.ensureExtensionWorkflowDefinition()`: 1/2-stage (Issuer SELALU → [kondisional] HSE), kondisi kali ini `gasRetestRequired` (field PADA `work_permit_extensions` itu sendiri, BUKAN turunan `risk_level`/`requires_hse_approval` spt permit induknya). `WorkPermitExtensionService.actOnExtensionTask()` — wrapper BR-09 mereplikasi pola `actOnApprovalTask()` (3.3), `decided_by`/`decided_at` ditulis SETIAP aksi ("aktor terakhir menang" pada alur 2-stage, kolom tunggal beda dari cache `work_permit_approvals`, TDD §26 #125).
  **BR-07 (extension)** — `assertExtensionRequestAllowed()`: gate sebelum `planned_end_datetime` terlampaui DAN `existingExtensionCount<max_extension_count` (menghitung SELURUH baris PENDING+APPROVED+REJECTED, TDD §26 #124), diverifikasi kedua guard independen. **`work-permit-expiry-scan`** (cron harian, cross-tenant) — kandidat `status IN (ACTIVE,EXTENSION_REQUESTED)`, permit `EXTENSION_REQUESTED` yang BELUM diputuskan tetap `EXPIRED` kalau `planned_end_datetime` ASLI terlampaui (extension APPROVED memperbarui tanggal itu duluan via listener, otomatis lolos filter scan berikutnya); `PENDING_CLOSURE` SENGAJA dikecualikan dari kandidat (TDD §26 #123, bacaan literal PRD §4 poin 10 berbeda).
  **BR-05 (gas retest interval)** — `gas-retest-due-scan` (cron harian terpisah): `GasTestResultService.record()` kini auto-compute `retest_due_at` dari `work_permit_types.gas_retest_interval_hours` kalau caller tidak mengisi eksplisit; overdue → `SUSPENDED` + eskalasi HSE Manager. `WorkPermitService.resumeFromSuspension()` — gate retest TERBARU `PASS`, TIDAK menyentuh `actual_start_datetime`, TIDAK mengulang BR-02/BR-03 penuh.
  **`WorkPermitClosureService`** — `submit()`/`verify()` upsert-by-`work_permit_id` (baris SAMA dipakai ulang jalur RETURNED→resubmit); BR-06 gate compound (`assertClosureReadyForClosed`: `status=VERIFIED` DAN, kalau `requires_loto`, `isolation_removed_confirmed=TRUE`) ditegakkan SEBELUM tulis apa pun, satu transaksi `closure.status=VERIFIED`+`work_permits.status=CLOSED`+`actual_end_datetime`. TIDAK PERNAH py `workflow_instance_id` (pola self-verify HIRADC 3.2, konsisten skema literal PRD §5, TDD §26 #126).
  **Cross-link Modul 05/17 — dua gap nyata**: BR-10 (Contractor scope validation) TIDAK py stub sama sekali meski `TASK_INSTRUCTION.md` mengantisipasinya (TDD §26 #120); `related_jsa_id` auto-prefill checklist (PRD §7) TIDAK diimplementasikan, murni FK pasif (TDD §26 #121). **Notifikasi**: HANYA 4 dari 9 event literal PRD §8 genuinely terpicu gabungan 3.3+3.4 — disetujui/retest terlewat/expired/ditutup ADA, disubmit/HSE-approval-needed/retest-H-30-menit/H-1-jam-before-end/closure-diajukan TIDAK ADA (TDD §26 #122); `seed-work-permit-notification-templates.ts` diperluas 4→14 baris (5 event type baru ×IN_APP/EMAIL).
  Diverifikasi: 15 unit test baru (`work-permit-extension-rules.spec.ts` 5, `work-permit-closure-rules.spec.ts` 4, `work-permit-gas-retest-scan.spec.ts` 3, `work-permit-expiry-scan.spec.ts` 3 — 4 file baru terisolasi, angka dari run nyata) + `work-permit-lifecycle.spec.ts` bertambah 5 (13→18, transisi ACTIVE→EXTENSION_REQUESTED/PENDING_CLOSURE/SUSPENDED/EXPIRED) + 11 integration test baru (`work-permit-extension-closure.integration-spec.ts` — E2E-1 penuh submit→ACTIVE→extension 1-stage→closure VERIFIED→CLOSED, extension 2-stage `gasRetestRequired=true`, reject extension tanpa ubah tanggal, BR-07 kedua guard (waktu+jumlah), BR-09 extension, BR-06 gate+RETURNED→resubmit, `work-permit-expiry-scan` utk ACTIVE & EXTENSION_REQUESTED, `gas-retest-due-scan`+`resumeFromSuspension()`, isolasi tenant RLS) — **874/874 test lulus seluruh proyek** (naik dari 843, run BERSIH tanpa flake), `pnpm build`/`turbo run build --concurrency=1` sukses 6/6 package (termasuk `apps/worker` — 2 worker bootstrap baru `work-permit-expiry-scan.worker.ts`/`gas-retest-due-scan.worker.ts`), dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 2/2 tabel baru, `audit_log_trigger` terpasang 2/2 tabel baru.

- [x] 3.5 Incident Management (Modul 07) — `apps/api/src/modules/domains/incident/*`: modul DOMAIN KESEMBILAN — 8 tabel baru (`incident_reports`, `incident_investigations`, `incident_investigation_team`, `incident_root_causes`, `incident_corrective_actions`, `incident_witness_statements`, `incident_regulatory_reports`, `incident_statistics_cache`) + 11 enum — hanya mereferensikan `work_permits` (Modul 06) via FK Prisma langsung, TIDAK mengimpor `WorkPermitModule`.
  **Investigasi — KEEMPAT kalinya JSON Logic condition genuinely dipakai pada `workflow_transitions` di seluruh codebase** (setelah HIRA 3.2, Work Permit Stage 2 HSE 3.3, Work Permit Extension 3.4) — `IncidentWorkflowBootstrapService.ensureWorkflowDefinition()`: 1/2-stage (Review Laporan Investigasi SELALU → [kondisional] Persetujuan Pelaporan Regulator), KEDUA stage sama-sama `HSE_MANAGER` (BEDA dari modul lain yang pakai role berbeda per stage). `hasRegulatoryReport` (kondisi) DIHITUNG + regulatory report DIAUTO-BUAT (BR-03) SAAT `submitForApproval()` — BUKAN setelah approval — supaya kondisi Stage 2 punya data nyata saat dievaluasi, interpretasi urutan §4 poin 7 vs 8, TDD §26. `IncidentWorkflowCompletionListener` (KEDELAPAN konsumen `WORKFLOW_INSTANCE_COMPLETED_EVENT`) — APPROVED → investigasi APPROVED + `incident_reports` `UNDER_INVESTIGATION`→`INVESTIGATION_COMPLETED`→(kalau ada regulatory report)→`PENDING_REGULATORY_REPORT` SATU transaksi; REJECTED → investigasi `RETURNED` (BUKAN "REJECTED", enum tidak py nilai itu).
  **BR-01/BR-09 (gate CLOSED)** — `assertInvestigationApprovedIfRequiredForClosure()` (FATALITY/LTI/PSE wajib investigasi APPROVED) + `assertNoPendingOrOverdueRegulatoryReports()` (regulatory report PENDING/OVERDUE memblokir CLOSED), keduanya ditegakkan `IncidentReportService.close()` sebelum tulis apa pun. `incident_reports.status` MEWAJIBKAN `classify()` (REPORTED→UNDER_VERIFICATION) sebelum investigasi dapat dibuat — TIDAK ADA jalur langsung REPORTED→UNDER_INVESTIGATION (ditemukan lewat kegagalan integration test nyata, TDD §26).
  **`incident-regulatory-report-overdue-scan`** (cron 03:30, BR-09 bagian scan) — PENDING→OVERDUE begitu `required_by_date` terlampaui. **`IncidentStatisticsCacheService`/`incident-statistics-recalc-scan`** (cron 04:00, BR-06) — `total_manhours_worked` TIDAK py sumber otomatis (PRD sendiri "input manual/integrasi HRIS", TIDAK ada modul HRIS) — `recalculate()` SELALU manual (trigger HSE Manager), scan harian HANYA me-refresh baris yang SUDAH PERNAH dihitung pakai ULANG manhours tersimpan. Formula LTIFR/TRIR/Severity Rate PERSIS teks PRD (TRIR SENGAJA tidak menghitung Fatality sbg recordable, kejanggalan literal PRD didokumentasikan bukan dikoreksi diam-diam, TDD §26).
  **Gap signifikan lain**: `severity_level` pemetaan dari `classification` SEPENUHNYA diasumsikan (PRD tidak beri formula); BR-01 (gate) vs §4 poin 4 (prosa) TIDAK sepakat soal `RESTRICTED_WORK_CASE` (diimplementasikan ikut BR-01, 3 klasifikasi bukan 4); BR-03 regulatory-mapping TIDAK tenant-configurable (hardcode KEMNAKER, tanpa `SKK_MIGAS` utk kontraktor migas); `incident_corrective_actions` link-only ke Modul 10 CAPA (BELUM ADA, task 4.2) — `capaRegisterId` bare UUID, `capaStatusCache` VARCHAR bukan enum (nilai asli belum diketahui); role "Top Management" TIDAK ditambah baru (dipetakan ke TENANT_ADMIN, PRD tidak sebut eksplisit "role baru" beda dari Document Controller/Compliance Officer); distribusi lesson learned (§4 poin 10, ke DMS+broadcast) TIDAK diimplementasikan; isolasi data kesehatan `injured_person_id` (BR-05) seluruhnya ditunda Modul 13 (task 5.3); HANYA 3 dari 8 event notifikasi PRD §8 genuinely terpicu kode (TDD §26 #137).
  Diverifikasi: 39 unit test baru (`incident-lifecycle.spec.ts` 6, `incident-severity.spec.ts` 4, `incident-investigation-rules.spec.ts` 8, `incident-regulatory-report-rules.spec.ts` 11, `incident-statistics-formulas.spec.ts` 6, `incident-regulatory-report-overdue-scan.spec.ts` 2, `incident-statistics-tally.spec.ts` 3 — 7 file baru terisolasi, angka dari run nyata) + 9 integration test baru (`incident-lifecycle.integration-spec.ts` — **E2E-2 penuh** lapor→classify→investigasi(5-Why)→team→root cause→tautan CAPA→witness→submit 2-stage(BR-03)→approve×2→PENDING_REGULATORY_REPORT→BR-09 blocked→submit regulator→CLOSED, jalur 1-stage NEAR_MISS, BR-01 (2 varian: langsung dari UNDER_VERIFICATION tanpa investigasi, DAN guard struktural UNDER_INVESTIGATION), investigasi REJECTED→RETURNED, numbering `scope_level=SITE`, `incident-regulatory-report-overdue-scan`, `IncidentStatisticsCacheService.recalculate()`+`incident-statistics-recalc-scan` (manhours dipakai ulang), isolasi tenant RLS) — **922/922 test lulus seluruh proyek** (naik dari 874, run BERSIH tanpa flake), `pnpm build`/`turbo run build --concurrency=1` sukses 6/6 package (termasuk `apps/worker` — 2 worker bootstrap baru `incident-regulatory-report-overdue-scan.worker.ts`/`incident-statistics-recalc-scan.worker.ts`), dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 8/8 tabel baru, `audit_log_trigger` terpasang 8/8 tabel baru.

- [x] 3.6 Inspection Management (Modul 08) — `apps/api/src/modules/domains/inspection/*`: modul DOMAIN KESEPULUH — 8 tabel baru (`inspection_types`, `inspection_checklist_templates`, `inspection_checklist_template_items`, `inspection_schedules`, `inspection_records`, `inspection_record_items`, `inspection_findings`, `inspection_scores`) + 7 enum — TIDAK mengimpor domain module manapun.
  **SATU-SATUNYA modul domain Phase 2+ yang sama sekali TANPA `WorkflowEngineModule`** — PRD §4 poin 9 + kalimat penutup §4 eksplisit "modul ini **tidak wajib** memakai Workflow Engine multi-stage ... jika tenant memerlukan sign-off formal, disarankan 1 stage sederhana". Review opsional itu TIDAK diimplementasikan sama sekali (tidak ada `workflow_definitions` bootstrap, tidak ada `workflow_instance_id` di skema §5 literal, tidak ada listener) — sifat "tergantung konfigurasi tenant" butuh kolom konfigurasi yang tidak ada di skema, gap TDD §26 #145. Konsekuensinya modul ini juga TIDAK punya `WorkflowCompletionListener` (memutus rantai KEDELAPAN konsumen `WORKFLOW_INSTANCE_COMPLETED_EVENT` yang berjalan sejak 2.1).
  **Numbering — PERTAMA di seluruh codebase yang genuinely memakai `reset_period=MONTHLY`** (SELURUH modul sebelumnya YEARLY; MONTHLY didukung `NumberingService` sejak task 0.10 tapi belum pernah dipakai modul nyata) + `{SEQ:5}` 5 digit (modul lain 4) + `scope_level=SITE` — PERSIS anjuran PRD §5 "Numbering" utk volume tinggi harian. Diverifikasi empiris (`INS/{SITE_CODE}/{YYYY}/00001`). `inspection_record_number` SELALU digenerate meski PRD §13 Open Question #1 mempertanyakan apakah nomor formal wajib utk seluruh tipe — tidak ada kolom di `inspection_types` utk menandai "tipe ini butuh nomor" (TDD §26 #150).
  **BR-07 (versi template tidak mengubah record historis)** — `InspectionChecklistTemplateService.createNewVersion()` mereplikasi pola versioned CRUD `RiskMatrixConfigService` (3.1): versi BARU = BARIS BARU (`version+1`), baris LAMA disentuh HANYA utk `isActive->false`. Snapshot terjamin krn `inspection_records.inspection_checklist_template_id` diisi caller saat `create()` dan TIDAK PERNAH ikut berubah — diverifikasi test (record v1 tetap menunjuk v1 setelah v2 dibuat). Desain `CreateInspectionRecordInput` sengaja menerima `inspectionChecklistTemplateId` LANGSUNG (bukan meresolusi sendiri dari `inspectionTypeId`) supaya job generator bisa memakai versi yang DIPIN jadwal, bukan versi "yang sedang aktif" — refactor proaktif sebelum job-nya ditulis, bukan perbaikan setelah bug.
  **BR-01/BR-02/BR-03 (gate `COMPLETED`)** — `InspectionRecordService.complete()` menegakkan berurutan SEBELUM menulis apa pun: seluruh item `is_mandatory` terjawab (BR-01), lalu tiap item `requires_photo_if_fail=TRUE` ber-`response_value=FAIL` wajib punya ≥1 baris `attachments` `scan_status=CLEAN` (BR-02, query `attachments` LANGSUNG dgn `entity_type=inspection_record_item` — pola cross-module read-only, BUKAN inject `AttachmentService`), baru `overall_score`/`overall_result` dihitung (BR-03). Formula per `response_type` SEPENUHNYA diasumsikan — PRD tidak memberi rumus apa pun (TDD §26 #147): `PASS_FAIL`/`YES_NO` → weight penuh atau 0, `SCALE_1_5` → `(nilai/5)×weight`, `TEXT`/`NUMERIC` → `null` dan DIKELUARKAN dari agregasi (bukan dihitung 0).
  **3 job cron baru** — `inspection-record-generation-scan` (04:15, PRD §4 poin 2-3) adalah **job terjadwal PERTAMA di codebase yang genuinely MEMBUAT baris domain baru** (seluruh scan job sebelumnya hanya meng-UPDATE status), memunculkan masalah "aktor siapa" yang baru: `created_by` record hasil generate = `created_by` BARIS JADWAL itu sendiri (tidak ada user system/admin generik di skema, TDD §26 #149). Jadwal `CUSTOM_CRON` DILEWATI SELURUHNYA — bukan cuma `next_generation_date`-nya tidak maju, krn opsi itu akan menghasilkan record BARU SETIAP HARI tanpa henti (keputusan desain saat perancangan, diverifikasi test eksplisit, TDD §26 #148). `inspection-record-overdue-scan` (04:30, BR-06) + `inspection-finding-sla-scan` (04:45, BR-04).
  **BR-06 DIREINTERPRETASI dari level JADWAL ke level RECORD** — teks BR-06 literal minta "jadwal ditandai `OVERDUE`" tapi `inspection_schedules` skema §5 HANYA punya `is_active BOOLEAN`, TIDAK ADA kolom status manapun (sementara `inspection_records.status` enum-nya MEMANG sudah punya nilai `OVERDUE`) — ditegakkan di level record, penerima notifikasi tetap PERSIS teks BR-06 (inspector + HSE Manager), TDD §26 #146. **BR-04** murni eskalasi NOTIFIKASI tanpa transisi status apa pun — konsekuensinya TIDAK ADA kolom idempotency, temuan HIGH tanpa `action_tracking_id` dinotifikasi ULANG tiap scan harian ("nag harian", beda dari precedent scan lain, TDD §26 #151).
  **Stub Modul 24 — `inspection.finding_created`** — PRD §4 poin 7 "setiap `inspection_findings` **secara default** langsung membuat action item di Modul 24" tapi Modul 24 baru task 7.4; `TASK_INSTRUCTION.md` 3.6 sendiri eksplisit meminta bentuk stub-nya, jadi `InspectionFindingService.create()` genuinely emit `EventEmitter2` event bertipe (namespace terpisah dari `WORKFLOW_INSTANCE_COMPLETED_EVENT`) — **TIDAK ADA `@OnEvent` listener yang mengonsumsinya** di codebase ini, terverifikasi lewat listener yang dipasang test itu sendiri. BEDA dari BR-10 Work Permit (3.4) yang nol artefak stub. `action_tracking_id`/`escalated_capa_register_id` bare UUID tanpa FK, diisi HANYA lewat `linkActionTracking()`/`escalateToCapa()` MANUAL (BR-05 eksplisit "tidak ada proses otomatis").
  **Perbaikan RBAC lintas-task (ditemukan saat verifikasi akhir 3.6, memperbaiki JUGA task 3.5)** — blok grant `incident.*` (14 kode, 3.5) dan `inspection.*` (10 kode, 3.6) ternyata SALAH TEMPAT di array `[ROLE_CODES.COMPANY_ADMIN]` padahal komentar keduanya sendiri menulis "Tenant Admin": `TENANT_ADMIN` punya NOL permission utk kedua modul terbaru, sementara `COMPANY_ADMIN` justru dapat 24 kode termasuk `inspection.type.manage`/`template.manage` yang PRD Modul 08 §3 tetapkan utk Tenant Admin (Modul 07 §3 & Modul 08 §3 TIDAK menyebut Company Admin sama sekali). Lolos SELURUH test krn kedua modul belum punya controller HTTP — `PermissionGuard` tidak pernah mengevaluasi kode itu, jadi cacatnya invisible sampai controller dibuat (Tenant Admin akan 403 di seluruh endpoint). Ditemukan lewat query `role_permissions` LANGSUNG ke Postgres, dibandingkan pola konsisten modul 2.1-3.4. Diperbaiki: kedua blok dipindah ke `TENANT_ADMIN`, seed dijalankan ulang, DAN 24 baris basi milik `COMPANY_ADMIN` dihapus manual — `seedRbacBaseline()` ADD-ONLY (`findFirst`→`create`), mengubah katalog saja TIDAK menghapus grant lama (TDD §26 #157b). Diverifikasi ulang: `TENANT_ADMIN` kini konsisten dapat set penuh tiap modul (dms 14, risk 19, work_permit 15, compliance 12, incident 14, inspection 10); `COMPANY_ADMIN` menyisakan HANYA dms 5 yang memang sah per Modul 03 §3.
  RBAC: 10 kode `inspection.*` baru (140→**150 permission**), granted `TENANT_ADMIN` (10/10) / `HSE_MANAGER` (schedule.create+assign, record.read, score.view, record.export) / `DEPARTMENT_HEAD` (finding.read) / `SUPERVISOR`+`HSE_OFFICER`+`WORKER_EMPLOYEE` (record.create+finding.create, sbg "Inspector ditugaskan" §3) / `AUDITOR_INTERNAL`+`AUDITOR_EXTERNAL` (record.read+export). `CONTRACTOR_USER` SENGAJA nol grant (§3 tidak menyebutnya, beda dari Work Permit/Incident). Roster role TETAP 15 (tidak ada role baru). Notification templates 4 baris — HANYA 2 dari 5 event PRD §8 genuinely terpicu kode (TDD §26 #152).
  Diverifikasi: 32 unit test baru (`inspection-scoring.spec.ts` 12, `inspection-schedule-recurrence.spec.ts` 8, `inspection-lifecycle.spec.ts` 4, `inspection-mandatory-items.spec.ts` 3, `inspection-finding-sla-scan.spec.ts` 3, `inspection-record-overdue-scan.spec.ts` 2 — angka dari run nyata 6 file terisolasi) + 9 integration test baru (`inspection-lifecycle.integration-spec.ts` — lifecycle ad-hoc penuh dgn gate BR-01→BR-02→BR-03 diuji BERURUTAN sbg 3 penolakan terpisah sebelum sukses, event stub `inspection.finding_created` dibuktikan ter-emit, BR-07 snapshot versi, ketiga scan job termasuk skip `CUSTOM_CRON` & skip jadwal tanpa inspector default, numbering SITE/MONTHLY/SEQ:5, isolasi tenant RLS) — **961/963 test lulus seluruh proyek** (naik dari 922; 2 kegagalan `incident-lifecycle.integration-spec.ts` + `workflow-sla-scan.integration-spec.ts` keduanya TIMEOUT bertanda `Transaction not found ... old closed transaction` — dikonfirmasi flake kontensi DB PRE-EXISTING lewat re-run terisolasi 13/13 BERSIH, kedua file TIDAK PERNAH disentuh task ini), `turbo run build --concurrency=1` sukses 6/6 package, dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 8/8 tabel baru, `audit_log_trigger` terpasang 8/8 tabel baru.

- [x] 3.7 Emergency Response (Modul 14) — `apps/api/src/modules/domains/emergency-response/*`: modul DOMAIN KESEBELAS (Phase 3 TUNTAS) — 11 tabel baru (`emergency_muster_points`, `emergency_response_plans`, `emergency_response_plan_steps`, `emergency_response_teams`, `emergency_response_team_members`, `emergency_contacts`, `emergency_drills`, `drill_participation_logs`, `emergency_activations`, `muster_point_checkins`, `emergency_equipment_readiness_checklist`) + 14 enum — modul terbesar sejak HIRA/JSA/HIRADC (3.2, juga 11 tabel). Mereferensikan `documents` (Modul 03)/`incident_reports` (Modul 07) via FK Prisma SUNGGUHAN (KEDUA modul itu sudah ada) — TIDAK mengimpor `DmsModule`/`IncidentModule`. Modul 15 Asset & Equipment ("dependency WAJIB" milik PRD sendiri untuk kesiapan peralatan) BELUM ADA (task 6.1, jauh di Phase 6) — `emergency_equipment_readiness_checklist.asset_id` bare UUID TANPA FK MESKI NOT NULL dipertahankan sesuai kardinalitas literal PRD (beda dari pola bare-UUID nullable lain di codebase ini), TDD §26 #158.
  **KELIMA JSON Logic conditional workflow di codebase ini** (setelah HIRA 3.2/Work Permit Stage 2 3.3/Work Permit Extension 3.4/Incident investigasi 3.5) — `EmergencyResponseWorkflowBootstrapService.ensureWorkflowDefinition()`: Stage 1 Review HSE Manager (SLA 5 hari kerja ≈120 jam, TDD §26 #161) SELALU → [kondisional] Stage 2 Approval Level Tinggi HANYA utk `severity_level` LEVEL_2_SITE_WIDE/LEVEL_3_COMPANY_WIDE_EXTERNAL_AGENCY (`{"!=":[{"var":"severityLevel"},"LEVEL_1_LOCAL"]}`). Stage 2 approver prosa PRD "Top Management/Site Manager" TIDAK py permission_code terpisah di §3 — KEDUA stage memakai role `HSE_MANAGER` yang SAMA, pola PERSIS Incident 3.5 (TDD §26 #160). `module_code=EMERGENCY_PLAN` LITERAL PRD §4.1 (BEDA dari konvensi modul lain yang biasanya pakai module_code permission RBAC). Numbering GANDA — `EMERGENCY_PLAN` (`ERP/{SITE_CODE}/{YYYY}/{SEQ:3}`) & `EMERGENCY_DRILL` (`DRILL/{SITE_CODE}/{YYYY}/{SEQ:3}`), keduanya `scope_level=SITE`/`YEARLY`. `EmergencyResponseWorkflowCompletionListener` (KESEMBILAN konsumen `WORKFLOW_INSTANCE_COMPLETED_EVENT`) — APPROVED→`APPROVED_ACTIVE`+`next_review_due_date` dihitung (BR-01); REJECTED→`DRAFT` (enum tidak py nilai REJECTED). `approved_by`/`reviewed_by` SENGAJA DIBIARKAN NULL — `WorkflowInstanceCompletedEvent` tidak bawa identitas aktor, re-query `workflow_tasks` beresiko race pre-commit yang SUDAH diverifikasi empiris task 2.1 (TDD §26 #162).
  **Acceptance criterion literal TASK_INSTRUCTION.md 3.7** — "aktivasi darurat memicu notifikasi broadcast prioritas tinggi" — `EmergencyActivationService.declare()` dgn `trigger_context=REAL_EMERGENCY` broadcast ke seluruh karyawan/kontraktor SITE terkait (`userType IN INTERNAL_EMPLOYEE,CONTRACTOR`, employee LAIN/visitor/site lain dikecualikan, diverifikasi empiris) + `HSE_MANAGER`/`TENANT_ADMIN` (Top Management, tenant-wide) role holders, priority `CRITICAL`, via LOOP sequential `NotificationService.enqueue()` (0.11) — TIDAK ada infrastruktur batch-notification baru meski PRD §11 NFR eksplisit memperingatkan beban lonjakan (TDD §26 #163). `trigger_context=DRILL` TIDAK memicu broadcast sama sekali (diverifikasi 0 notifikasi).
  **BR-01** (review plan >30 hari overdue → notifikasi HSE Manager, scan `emergency-plan-review-overdue-scan` 05:00) — pola "daily nag" TANPA idempotency, sama Inspection SLA scan 3.6. **BR-03** (`assertIncidentCommanderUniquePerActiveTeam`, INCIDENT_COMMANDER unik per tim aktif kecuali `backup_for_member_id` eksplisit). **BR-04+BR-08** (gate keras `EmergencyDrillService.complete()`: FULL_SCALE_EVACUATION wajib ≥1 `emergency_activations`; `gaps_identified` terisi wajib `capa_id`). **BR-05** (`checkin_timestamp` direkam SERVER, tidak pernah terima input caller; `MARSHAL_MANUAL_ENTRY` wajib `recorded_by`=actor). **BR-07** (`missing_person_count`/`total_checked_in_count` DIHITUNG LIVE saat dibaca, TIDAK didenormalisasi — disclaimer UI wajib PRD BELUM terpenuhi krn belum ada controller/UI, TDD §26 #164). **BR-06** (eskalasi REAL-TIME sinkron, BUKAN scan, saat `readiness_status=OUT_OF_SERVICE` — ke `HSE_MANAGER` saja, "Site Manager" tidak py role addressable, TDD §26 #167; validasi `assets.is_safety_critical=TRUE` mustahil krn Modul 15 tidak ada, TDD §26 #158).
  **RBAC**: 19 kode `emergency_response.*` baru (150→**169 permission**) — 14 literal PRD §3 + 5 kode `.read` beyond-literal meresolusi prosa wildcard "Auditor Internal/Eksternal | `emergency_response.*.view`" (RBAC Engine 0.8 cocok EXACT string, TIDAK ada wildcard matching runtime). `TENANT_ADMIN` dapat SET PENUH 19/19 (konvensi sama seluruh modul, diverifikasi LANGSUNG ke `role_permissions` — lihat catatan bug pasca-3.6 di atas). "Top Management"→`emergency_response.dashboard.view` SAJA (BEDA dari Incident 3.5 — §3 modul ini SENDIRI membatasi Top Management read-only, bukan konvensi "role baru"). "Site Security/Facility"→`SUPERVISOR`+`HSE_OFFICER` (tidak ada role itu di roster 15-role, TDD §26 #167). **`VISITOR_SELF_SERVICE` dapat grant PERTAMA KALINYA sejak dibuat task 0.8** (`muster_checkin.self_submit`+`drill.view_own_participation`, literal PRD "Karyawan/Kontraktor/**Visitor**"). "Anggota ERT (khusus Incident Commander)" utk `activation.declare` TIDAK ditegakkan RBAC maupun service layer — deskripsi §3 prosa, BUKAN §6 BR bernomor (TDD §26 #168, dikoreksi dari klaim awal yang salah di deskripsi permission — lihat catatan di bawah). Notification templates 7 baris (3 event type — HANYA 3 dari 6 §8 literal genuinely terpicu, TDD §26 #169) `prisma/seed-emergency-response-notification-templates.ts`.
  **Koreksi self-audit mid-task** — deskripsi permission `emergency_response.activation.declare` SEMPAT eksplisit (salah) mengklaim "ditegakkan `EmergencyActivationService` via query team_member" padahal method itu TIDAK PERNAH menulis validasi tsb — ditemukan & diperbaiki SEBELUM commit (bukan pasca-fakta spt bug RBAC 3.5/3.6), deskripsi permission sekarang jujur menyatakan gap-nya alih-alih mengklaim enforcement yang tidak ada.
  Diverifikasi: 36 unit test baru (`emergency-response-plan-lifecycle.spec.ts` 13, `emergency-drill-rules.spec.ts` 10, `emergency-activation-rules.spec.ts` 6, `emergency-team-member-rules.spec.ts` 5, `emergency-plan-review-overdue-scan.spec.ts` 2 — angka dari run nyata 5 file terisolasi) + 13 integration test baru (`emergency-response-lifecycle.integration-spec.ts` — KELIMA JSON Logic 1-stage/2-stage/REJECTED penuh, BR-01 scan, BR-03 (tolak+izinkan backup), BR-04+BR-08 gate COMPLETED (2 skenario)+`recordParticipation()` denormalisasi, broadcast REAL_EMERGENCY tepat-sasaran (bukan visitor/site lain)+DRILL tanpa broadcast, BR-05+BR-07+`markAllClear()`, BR-06 real-time, isolasi tenant RLS) — **1012/1012 test lulus seluruh proyek** (naik dari 963, run BERSIH tanpa flake SAMA SEKALI), `turbo run build --concurrency=1` sukses 6/6 package (termasuk `apps/worker` — 1 worker bootstrap baru `emergency-plan-review-overdue-scan.worker.ts`), dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 11/11 tabel baru, `audit_log_trigger` terpasang 11/11 tabel baru, `role_permissions.TENANT_ADMIN` 19/19 kode `emergency_response.*` terverifikasi LANGSUNG (bukan diasumsikan dari output seed script — pelajaran dari bug 3.5/3.6).

**Environment ini tidak punya Docker maupun hak admin Windows** — Postgres, Redis, & MinIO jalan sebagai proses user biasa dari binary portable (`.local-pgsql/`, `.local-redis/`, & `.local-minio/`, satu level di atas repo ini, di luar git).

### Phase 4 — Kepatuhan Lanjutan & Tindak Lanjut (Audit Management, CAPA Management)

- [x] 4.1 Audit Management (Modul 09) — `apps/api/src/modules/domains/audit/*`: modul DOMAIN KEDUA BELAS, Phase 4 dimulai — 11 tabel baru (`audit_types`, `audit_checklists`, `audit_checklist_items`, `audit_programs`, `audit_program_plan_items`, `audits`, `audit_team_members`, `audit_auditee_scopes`, `audit_findings`, `auditor_competency_records`, `audit_reports`) + 8 enum. Mereferensikan `documents` (Modul 03) via FK Prisma SUNGGUHAN — TIDAK mengimpor `DmsModule`. Modul 10 CAPA (task 4.2) & Modul 19 Training BELUM ADA — `audit_findings.capa_register_id`/`auditor_competency_records.related_training_record_id` bare UUID nullable TANPA FK.
  **PERTAMA KALI satu modul domain punya DUA `workflow_definitions` process sekaligus** (`AUDIT_PROGRAM`: Review MR/HSE_MANAGER→Approval Top Management/`COMPANY_ADMIN`; `AUDIT_REPORT`: Review Lead Auditor→Approval HSE/QMS Manager) — TANPA JSON Logic condition (BEDA dari 5 modul sebelumnya yang selalu conditional), `AuditWorkflowBootstrapService` punya 2 method `ensure*WorkflowDefinition()` + 2 listener terpisah (`AuditProgramWorkflowCompletionListener`/`AuditReportWorkflowCompletionListener`, konsumen ke-10 & ke-11 `WORKFLOW_INSTANCE_COMPLETED_EVENT`). Stage 1 `audit_report` **CONTEXT_USER KEDUA KALINYA dipakai lintas modul domain** (setelah DMS 2.1) — `contextData.contextUserId` diisi dari `audits.lead_auditor_id` SAAT `submitForApproval()`, diverifikasi empiris `workflow_tasks.assigned_to` = Lead Auditor spesifik, BUKAN role holder. `audit_programs.status` TIDAK punya nilai `UNDER_REVIEW`/`REJECTED` (5 enum literal PRD) — TETAP `DRAFT` sepanjang workflow berjalan, hanya `workflowInstanceId` yang terisi; flip ke `APPROVED` murni via listener.
  **Dua pasang kolom FK "ganda SENGAJA" literal PRD, BUKAN duplikasi keliru** — `audits.audit_program_plan_item_id`↔`audit_program_plan_items.linked_audit_id` (historis-immutable vs status-realisasi KPI, diisi atomik `AuditService.createFromPlanItem()`) dan `audits.workflow_instance_id`↔`audit_reports.workflow_instance_id` (interpretasi denormalized-copy, KEDUANYA ditulis SATU method `AuditReportService.submitForApproval()` supaya tidak pernah divergen — PRD sendiri tidak jelaskan alasan dua kolom).
  **BR-06** (`assertPlanItemEditable`/`linked_audit_id` guard) — plan item EXECUTED tidak bisa dipakai `createFromPlanItem()` lagi, diverifikasi throw. **BR-07** (independensi auditor) — `assertAuditorIndependence()` bandingkan `users.department_id` kandidat vs `audit_auditee_scopes.department_id` (interpretasi lebih sempit dari literal `user_roles.scope_type/scope_id`). **BR-01** (kompetensi Lead Auditor) — `checkLeadAuditorCompetencyWarnings()` SELALU soft-warning (TIDAK PERNAH block `AuditService.start()`) — §13 Open Question #1 belum terjawab, tidak ada kolom tenant-config hard-block. **BR-04/BR-05** — `computeTargetClosureDate()` (Major NC +30 hari/Minor NC +60 hari/Observation+OFI null) dipanggil ulang `updateClassification()`, diverifikasi rekalkulasi. **BR-02/BR-03** — `assertAuditCloseAllowed()` gate `audits.status=CLOSED` (report APPROVED + seluruh finding wajib-CAPA CLOSED); `PENDING_CAPA_CLOSURE` diderivasi OTOMATIS di `markReportApproved()` (PRD tidak beri trigger manual eksplisit).
  **Event stub `audit.finding_capa_required`** (EventEmitter2, pola sama `inspection.finding_created` 3.6) — `AuditFindingService.create()` emit BILA `requires_capa=true` (default Major/Minor NC, atau override manual); `linkCapaRegister()` jadi titik sinkronisasi MANUAL begitu Modul 10 (task 4.2) genuinely ada.
  **Bug ditemukan & diperbaiki root-cause: `EventEmitterModule.forRoot()` TIDAK PERNAH diberi `maxListeners` eksplisit sampai task ini** — default eventemitter2 (10) terlampaui begitu `WORKFLOW_INSTANCE_COMPLETED_EVENT` dapat consumer ke-11, DAN `logPossibleMemoryLeak()` versi eventemitter2 ini py bug sendiri (`process.emitWarning()` dgn Error object throw `TypeError`, bukan cuma warn) — SELURUH integration test APAPUN yang boot app penuh gagal total, ditemukan empiris saat verifikasi task ini sendiri. Diperbaiki `EventEmitterModule.forRoot({ maxListeners: 50 })` (bukan mengurangi listener — fan-out per-modul memang desain benar), headroom Phase 4-10 lanjutan.
  **RBAC**: 21 kode `audit.*` baru (169→**190 permission**) — 15 literal PRD §3 + 6 kode `.read` beyond-literal. **Tabrakan nama "Auditor Internal"/"Lead Auditor" (§3 modul ini) vs role platform `AUDITOR_INTERNAL`/`AUDITOR_EXTERNAL`** (established read-only lintas 6 modul sebelumnya sejak Modul 05) — role platform TETAP read-only (`audit.audit.read`/`export` saja, cocok literal §3 "Auditor Eksternal (Guest)"/"Auditor Internal/Eksternal"); permission OPERASIONAL (`conduct`/`finding.create`/`classify`/`report.generate`) dipetakan `HSE_MANAGER`+`HSE_OFFICER` (kandidat plausible Lead-Auditor-QHSE-sungguhan) demi konsistensi platform. `TENANT_ADMIN` 21/21, `HSE_MANAGER` 17, `HSE_OFFICER` 8, `DEPARTMENT_HEAD` 2 (Auditee — match literal LANGSUNG, jarang terjadi), `COMPANY_ADMIN` 2 (Top Management: `program.approve`+`program.read`) — seluruhnya diverifikasi LANGSUNG ke `role_permissions` (disiplin sejak bug 3.5/3.6). Numbering `module_code=AUDIT` (`AUD/{SITE_CODE}/{YYYY}/{SEQ:3}`, `scope_level=SITE`/`YEARLY`) HANYA utk `audits.audit_number` — `audit_programs`/`audit_checklists`/`audit_types` TIDAK punya nomor formal (§5 literal). 2 job cron baru (`auditor-competency-expiry-scan` 05:15, `audit-finding-closure-due-scan` 05:30 BR-04) — pola "daily nag" TANPA idempotency (konsisten Emergency Response 3.7).
  Diverifikasi: 55 unit test baru (`audit-lifecycle.spec.ts` 13, `audit-program-lifecycle.spec.ts` 10, `audit-finding-rules.spec.ts` 9, `auditor-competency-rules.spec.ts` 7, `auditor-competency-expiry-scan.spec.ts` 7, `audit-finding-closure-due-scan.spec.ts` 5, `audit-team-rules.spec.ts` 4 — angka dari run nyata 7 file terisolasi) + 11 integration test baru (`audit-lifecycle.integration-spec.ts` — `audit_program` 2-stage penuh (status TETAP DRAFT selama IN_PROGRESS)+REJECTED, plan item→audit BR-06, BR-07 (tolak+izinkan), BR-01 soft-warning (dgn+tanpa competency), BR-04+BR-05 event stub, `audit_report` 2-stage CONTEXT_USER (assignedTo=lead_auditor_id dibuktikan)+BR-02/BR-03 gate, kedua scan job, isolasi tenant RLS) — **1078/1078 test lulus seluruh proyek** (naik dari 1012; run parallel penuh sempat 1068/1078 dgn 5 suite `system-administration/provisioning`+`attachment/*` (4 file) gagal — dikonfirmasi PRE-EXISTING flake kontensi Redis/MinIO via re-run terisolasi 23/23 BERSIH, KELIMA file TIDAK PERNAH disentuh task ini sama sekali), `turbo run build --concurrency=1` sukses 6/6 package (termasuk `apps/worker` — 2 worker bootstrap baru `auditor-competency-expiry-scan.worker.ts`/`audit-finding-closure-due-scan.worker.ts`), dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 11/11 tabel baru, `audit_log_trigger` terpasang 11/11 tabel baru.
- [x] 4.2 CAPA Management (Modul 10) — `apps/api/src/modules/domains/capa/*`: modul DOMAIN KETIGA BELAS, menutup Phase 4 — 5 tabel baru (`capa_register`, `capa_root_cause_analysis`, `capa_action_plans`, `capa_effectiveness_verification`, `capa_approvals`) + 9 enum, PLUS 3 FK retroaktif `ADD CONSTRAINT` yang menutup stub `capaId`/`capa_register_id` bare-UUID yang ditinggalkan Incident 3.5 (`incident_corrective_actions.capa_register_id`), Inspection 3.6 (`inspection_findings.escalated_capa_register_id`), Audit 4.1 (`audit_findings.capa_register_id`).
  **`capa_register` sbg HUB POLYMORPHIC PERTAMA di codebase ini** — `source_type`/`source_id` menunjuk baris di tabel modul LAIN TANPA FK level-DB (Postgres tidak punya polymorphic FK native), divalidasi service-layer (BR-05) HANYA utk 3 dari 9 `source_type` yang kontraknya konkret (`INCIDENT`/`AUDIT_FINDING`/`INSPECTION_FINDING` — sisanya PRD §7 sendiri bilang "kontrak belum final", TDD §26 #189), pola dimodelkan dari `attachments` generik (`entity_type`/`entity_id`) yang sudah ada sejak 0.12. **PERTAMA modul domain yang mengimpor DUA modul domain lain sekaligus** (`AuditModule`+`IncidentModule`, pola sama `ProvisioningService` 1.5) — genuinely dibutuhkan utk sync-back closure PRD §4 poin 10 (lihat di bawah).
  **DUA `workflow_definitions` process TERPISAH, KEDUANYA 1-stage** (`capa_action_plan`, `capa_effectiveness_verification`, keduanya `ROLE_IN_SCOPE HSE_MANAGER`) — TAPI SATU kolom `capa_register.workflow_instance_id` dipakai BERGANTIAN sbg pointer "current" (PRD §5 literal hanya beri satu kolom itu). **Deviasi entity_type SADAR**: `capa_action_plan` menunjuk `capa_register.id` (submission mencakup SELURUH action plan CAPA itu, tidak ada satu baris representatif), `capa_effectiveness_verification` menunjuk baris `capa_effectiveness_verification.id`-nya SENDIRI (SATU siklus verifikasi = SATU baris) — beda konvensi dari modul lain yang selalu menunjuk baris tunggal langsung. `capa_approvals` (cache) karena itu upsert per `workflowInstanceId` (bukan per `capaRegisterId`, beda `work_permit_approvals` 3.3 yang selalu satu baris tetap per permit).
  **BUG NYATA ditemukan+diperbaiki lewat integration test siklus penuh (TDD §26 #188)**: `workflow_instance_id` awalnya HANYA di-null-kan di jalur REJECTED, TIDAK di jalur APPROVED — begitu action plan APPROVED, kolom itu tertinggal stale menunjuk instance yang sudah selesai, membuat `CapaEffectivenessVerificationService.submitForApproval()` (workflow KEDUA) SELALU salah menolak dgn "masih ada workflow aktif". 27 unit test murni BR-01..08 sebelumnya semua lolos (masing-masing menguji SATU service terisolasi) — baru ketahuan begitu `test/capa/capa-lifecycle.integration-spec.ts` benar-benar mensimulasikan DUA workflow berurutan.
  **AuditFindingCapaTriggerListener — konsumen SUNGGUHAN PERTAMA `audit.finding_capa_required`** (stub sejak 4.1) — mewujudkan Modul 09 §4 poin 6 "CAPA dibuat OTOMATIS" utk Major/Minor NC. **CAPA utk `source_type=INCIDENT` SEBALIKNYA TETAP MANUAL** (TDD §26 #190) — Modul 07 §4 poin 6 literal "investigator **memicu**" (human-triggered, bukan "otomatis" spt Modul 09), jadi `IncidentCorrectiveActionLinkService.link()` (3.5) TIDAK diubah, tetap menerima `capaRegisterId` caller-supplied. **Sync-back closure PRD §4 poin 10** (`CapaEffectivenessVerificationWorkflowCompletionListener.syncClosureToSourceModule()`, dienqueue begitu outcome=EFFECTIVE_CLOSED) HANYA utk `AUDIT_FINDING` (`verify()`+`close()`, CAPA_LINKED→VERIFIED→CLOSED) & `INCIDENT` (`updateCapaStatusCache()`) — 2 contoh literal PRD, `INSPECTION_FINDING` dikecualikan (Modul 08 §7 "opsional/manual", TDD §26 #191).
  **BR-01/BR-04** (`resolveEffectivenessOutcome()`) — `result=EFFECTIVE`→`EFFECTIVE_CLOSED` (SATU-SATUNYA jalur closed, tidak ada jalur pintas), `result=NOT_EFFECTIVE`→`NOT_EFFECTIVE_REOPENED` (wajib root cause BARU, ditegakkan BY CONSTRUCTION lewat `ALLOWED_TRANSITIONS`: `NOT_EFFECTIVE_REOPENED` hanya bisa ke `ROOT_CAUSE_ANALYSIS`). **BR-02** (`assertAllActionPlansCompleted`)/**BR-03** (`assertVerifierNotPic`, segregation of duty) ditegakkan `CapaEffectivenessVerificationService.create()` SEBELUM status masuk `PENDING_EFFECTIVENESS_VERIFICATION`. **BR-08** (`assertActionTrackingIdRequired`) ditegakkan `CapaActionPlanService.submitForApproval()`. **BR-06** (CRITICAL wajib approval HSE Manager) TERPENUHI BY CONSTRUCTION — tidak ada mekanisme auto-approve di `WorkflowEngineService` manapun (TDD §26 #194).
  **2 job cron baru** (`capa-root-cause-sla-scan` 05:45 — ambang 7 hari kalender INVENTED, TDD §26 #192; `capa-effectiveness-verification-due-scan` 06:00 — `verification_due_date` konkret per baris, tanpa ambang invented) + 4 dari 7 event `notification_templates` PRD §8 (TDD §26 #193: 2 scan + `CAPA_NOT_EFFECTIVE_REOPENED`/`CAPA_EFFECTIVE_CLOSED` dienqueue LANGSUNG dari listener saat outcome dihitung, bukan scan).
  **RBAC**: 11 kode `capa.*` baru — `TENANT_ADMIN` 11/11, `HSE_MANAGER` 10, `HSE_OFFICER` 6, `DEPARTMENT_HEAD` 6, `COMPANY_ADMIN` 2, `AUDITOR_INTERNAL`/`AUDITOR_EXTERNAL` 2 masing² — seluruhnya diverifikasi LANGSUNG ke `role_permissions`. Numbering `module_code=CAPA` (`CAPA/{SITE_CODE}/{YYYY}/{SEQ:4}`, `scope_level=SITE`/`YEARLY`).
  **Mengaktifkan listener/FK retroaktif yang task LAMA sengaja tinggalkan sbg stub memaksa 2 test file task LAMA ikut diubah** (TDD §26 #195) — `test/audit/audit-lifecycle.integration-spec.ts` (4.1, panggilan manual `linkCapaRegister()` jadi transisi self-invalid begitu auto-link sungguhan jalan lebih dulu) & `test/incident/incident-lifecycle.integration-spec.ts` (3.5, placeholder UUID `capaRegisterId` melanggar FK real) — keduanya diperbaiki, bukan dilewati.
  Diverifikasi: 27 unit test baru (`capa-register-lifecycle.spec.ts`, `capa-action-plan-rules.spec.ts`, `capa-effectiveness-rules.spec.ts`, `capa-root-cause-sla-scan.spec.ts`, `capa-effectiveness-verification-due-scan.spec.ts` — angka dari run nyata terisolasi `src/modules/domains/capa`) + 11 integration test baru (`capa-lifecycle.integration-spec.ts` — **E2E-2** Incident lapor→investigasi→root cause→link CAPA manual→siklus CAPA penuh→`EFFECTIVE_CLOSED`+sync-back `capa_status_cache`; **E2E-6** Audit MAJOR_NC→CAPA OTOMATIS via listener→siklus penuh→`audit_findings.status=CLOSED`→audit close gate (termasuk gate `audit_reports` 2-stage TERPISAH) lolos; BR-01/BR-04 siklus NOT_EFFECTIVE→REOPENED→EFFECTIVE 2x; BR-02/BR-03/BR-05×2/BR-08; kedua scan job idempotency; isolasi tenant RLS) — **1116/1116 test lulus seluruh proyek** (naik dari 1078; run parallel penuh sempat 1103/1116 dgn 7 suite gagal — `dms`/`emergency-response`/`regulatory-compliance`-lifecycle, `capa-lifecycle` BARU, `incident-lifecycle` (diedit task ini), `auth/rate-limit`, `system-administration/data-import` — dikonfirmasi PRE-EXISTING flake kontensi Postgres/Redis via re-run terisolasi (7 file bersama, TANPA 142 suite lain) 79/79 BERSIH; kedua file CAPA-terkait gagal di titik yang SAMA SEKALI TIDAK bersentuhan dgn perubahan task ini — `incident-lifecycle` gagal di assertion status investigasi PRA-CAPA, `capa-lifecycle` gagal di baca status register yang listener belum sempat tulis di bawah beban, pola identik "Expected X, Received status-sebelumnya" yang sudah didokumentasikan sbg tanda kontensi, bukan bug logika), `turbo run build --concurrency=1` sukses lintas seluruh package (termasuk `apps/worker` — 2 worker bootstrap baru `capa-root-cause-sla-scan.worker.ts`/`capa-effectiveness-verification-due-scan.worker.ts`), dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 5/5 tabel baru, `audit_log_trigger` terpasang 5/5 tabel baru, 3/3 FK retroaktif dgn `ON DELETE` yang benar.

### Phase 5 — Peningkatan Berkelanjutan (Quality, Environmental, Occupational Health, KPI, Management Review)

- [x] 5.1 Quality Management (Modul 11) — `apps/api/src/modules/domains/quality/*`: modul DOMAIN KELIMA BELAS, Phase 5 dimulai — 7 tabel baru (`ncr_records`, `customer_complaints`, `quality_inspections`, `quality_inspection_parameters`, `supplier_quality_records`, `quality_objectives`, `quality_objective_progress_logs`) + 18 enum.
  **PERTAMA KALI satu modul domain butuh EMPAT `workflow_definitions` process sekaligus** (Audit 4.1/CAPA 4.2 masing² 2) — `QUALITY_NCR` 3-stage (Review Supervisor 24j→Approval Disposisi Quality Manager 48j→Verifikasi Penutupan Quality Manager 24j), `QUALITY_COMPLAINT` 3-stage (Investigasi QC Inspector 72j→Review & Approval Quality Manager 48j→Konfirmasi Penutupan Quality Manager 24j), `QUALITY_INSPECTION_DEVIATION` 1-stage (Quality Manager 24j), `QUALITY_SUPPLIER_EVAL` 1-stage (Quality Manager 120j/5 hari kerja, PRD §4.4 "Review→Approve & Publish" dibaca SATU stage — "Approve & Publish" hasil approval, bukan approver terpisah). Stage 3 KEDUA workflow 3-stage ("Verifikasi Penutupan"/"Konfirmasi Penutupan") TIDAK diberi approver eksplisit PRD §4 — dipetakan Quality Manager + SLA 24 jam diinvent; `QUALITY_COMPLAINT` Stage 1 ("QC Inspector/Quality Manager") dipetakan QC_INSPECTOR + SLA 72 jam diinvent ("sesuai kesepakatan pelanggan" tanpa angka konkret) — keempatnya gap TDD §26.
  **TIDAK mengimpor `CapaModule`** — beda dari CAPA 4.2 sendiri (impor `AuditModule`+`IncidentModule`) dan Audit 4.1 (auto-trigger CAPA via event) — CAPA-linkage modul ini SELURUHNYA MANUAL (`linkCapaRegister()` terima `capaRegisterId` caller-supplied di ketiga service NCR/Complaint/Supplier), pola sama Incident 3.5, dibaca dari TASK_INSTRUCTION.md acceptance 5.1 "NCR **dapat** memicu CAPA" (lunak) vs Modul 09's "**otomatis**". Arah dependency karena itu SEARAH: `CapaRegisterService.assertSourceValidIfKnownContract()` (BR-05, task 4.2) diperluas TERPISAH di `capa-register.service.ts` sendiri utk `QUALITY_NCR`/`CUSTOMER_COMPLAINT` (FK Prisma langsung ke `ncr_records`/`customer_complaints`) — **5 dari 9 `CapaSourceType` kini punya kontrak konkret tervalidasi** (naik dari 3; COMPLIANCE/RISK/MANAGEMENT_REVIEW/OTHER masih belum, PRD §7 sendiri bilang kontraknya belum final).
  **BR-01** (`assertCapaRequiredBeforeClose`, NCR severity MAJOR/CRITICAL wajib `capa_id` sebelum CLOSED) & **BR-07** (`assertReInspectionPassedBeforeClose`, disposisi REWORK/REPAIR wajib `re_inspection_result=PASS`) ditegakkan `NcrRecordService.close()`, keduanya pure function murni `ncr-lifecycle.ts`. **BR-02** (respons awal komplain 2×24 jam default) via scan job (bukan gate submit — SLA "menerima" bukan "menolak"). **BR-03** (Inspeksi FAIL pada FINAL/PRE_SHIPMENT → draft NCR otomatis) — `QualityInspectionService.submit()`; `QualityNcrDetectionStage` enum TIDAK punya nilai `PRE_SHIPMENT` (beda `QualityInspectionType` yang punya) — `toNcrDetectionStage()` memetakan `PRE_SHIPMENT`→`FINAL`, gap TDD §26. **BR-05** (Objective auto `AT_RISK` jika deviasi > `at_risk_threshold_percentage`) — `calculateObjectiveStatus()` pakai deviasi ABSOLUT (arah-agnostik, PRD tidak jelaskan arah), status manual-terminal (`ACHIEVED`/`OFF_TRACK` via override Quality Manager) dilindungi dari ketimpa scan berikutnya. **BR-06** (nomor NCR/Complaint/Inspection immutable setelah terbit) TERPENUHI BY CONSTRUCTION — tidak ada method service manapun yang mengekspos update ke `ncrNumber`/`complaintNumber`/`inspectionNumber` (pola sama CAPA BR-06). **BR-08** (`overallDisposition=USE_AS_IS_DEVIATION` wajib approval sebelum CLOSED) — **BEDA dari CAPA/NCR**: `QualityInspectionService.close()` baca `workflow_instance.status` LANGSUNG dari `deviationWorkflowInstanceId` (kolom TIDAK PERNAH di-null-kan — satu keputusan deviasi per inspeksi, bukan siklus berulang), bukan lewat listener dedicated. **BR-09** (kontak pelanggan komplain BUKAN data sensitif kesehatan, tidak butuh enkripsi khusus) — SENGAJA tidak ada kode tambahan, RBAC+audit log standar sudah cukup. **BR-04 literal** (`DISQUALIFIED` informasional, disarankan diteruskan ERP eksternal via Modul 30 yang belum ada) karena itu TIDAK butuh gate platform apa pun — comment kode `assertCapaRequiredForRating()` (`supplier-quality-record.service.ts`) sempat diberi label "BR-04-analog" TAPI sebenarnya rule berbeda dari §4.4 poin 3 ("rating Conditional/Suspended wajib capa_id") bukan §5 BR-04 formal — mislabel kode, TIDAK memengaruhi perilaku, dicatat gap TDD §26.
  **RBAC**: 24 kode `quality.*` baru (201→**225 permission** katalog; DB dev mentah menunjukkan 226 — 1 baris `work_permit.permit.approve`/moduleCode lowercase `work_permit` adalah **sampah fixture test pra-ada** dari `role.integration-spec.ts`, 1.3, sudah didokumentasikan `seed-rbac-baseline.ts` baris 1408-1421 sejak task 3.3; TIDAK disentuh atau ditambah task ini). **DUA role baru** `QUALITY_MANAGER`+`QC_INSPECTOR` (17 role sistem, naik dari 15). §3 literal sebut staf "Customer Service/Sales" (`quality.complaint.create` saja) & "Supplier Quality Engineer" — DIPETAKAN ke `WORKER_EMPLOYEE`(+2)/`QC_INSPECTOR`(+2 dari 5) demi konsistensi platform (bukan role baru sempit single-purpose), pola sama modul manapun sejak Modul 05. Grant diverifikasi LANGSUNG `role_permissions`: `TENANT_ADMIN` 24/24, `QUALITY_MANAGER` 14, `HSE_MANAGER` 14, `QC_INSPECTOR` 5, `AUDITOR_INTERNAL`/`AUDITOR_EXTERNAL` 5 masing², `DEPARTMENT_HEAD`/`SUPERVISOR`/`WORKER_EMPLOYEE` 2 masing². Numbering 3 `module_code` TERPISAH (`QUALITY_NCR` `NCR/{SITE_CODE}/{YYYY}/{SEQ:4}` SITE, `QUALITY_INSPECTION` `QI/{SITE_CODE}/{YYYY}/{SEQ:4}` SITE, `QUALITY_COMPLAINT` `CC/{YYYY}/{SEQ:4}` **TENANT** — PRD literal TANPA token `{SITE_CODE}`, konsisten `customer_complaints.site_id` nullable).
  **PRD §8 notifikasi Modul 11 — SATU-SATUNYA modul sejauh ini dgn cakupan LITERAL PENUH 5/5** (beda CAPA 4/7, Audit sebagian) — 10 baris `notification_templates` lintas `QUALITY_NCR_CRITICAL`(IN_APP/EMAIL/WHATSAPP, `NcrRecordService.create()`)/`QUALITY_COMPLAINT_RESPONSE_SLA_OVERDUE`(IN_APP/EMAIL, scan)/`QUALITY_INSPECTION_FAIL_AUTO_NCR`(IN_APP, BR-03)/`QUALITY_SUPPLIER_RATING_DOWNGRADED`(IN_APP/EMAIL, listener SUSPENDED/DISQUALIFIED)/`QUALITY_OBJECTIVE_AT_RISK`(IN_APP/EMAIL, BR-05). **Penerima sedikit menyimpang dari literal PRD**: baris SLA komplain sebut "Quality Manager, Customer Service Head" — role "Customer Service Head" TIDAK ADA (folded ke `WORKER_EMPLOYEE` tanpa varian Head), penerima sungguhan = `receivedBy` (pencatat komplain asli) + seluruh `QUALITY_MANAGER` tenant-wide, gap TDD §26. Job cron baru `quality-complaint-response-sla-scan` (06:15, BR-02) — idempotency-write & `NotificationService.enqueue()` loop dipisah dgn benar SEJAK DRAFT PERTAMA (pelajaran nested-transaction CAPA 4.2 diterapkan preventif, bukan ditemukan ulang lewat bug).
  **2 bug nyata ditemukan+diperbaiki**: (1) **`apps/worker/src/index.ts` `closeAll()` LATEN sejak task 4.2/217** — parameter+call-site sudah sertakan `capaRootCauseSlaScan`/`capaEffectivenessVerificationDueScan` tapi BODY fungsi tidak pernah memanggil `.worker.close()`/`.appContext.close()` keduanya — ketahuan baru saat menambah close-call Quality sendiri, diperbaiki ketiganya (2 CAPA + 1 Quality) sekaligus. (2) **2 test file** `quality-lifecycle.integration-spec.ts` memanggil `qualityInspectionService.getById()` langsung tanpa bungkus `tenantContextStorage.run()` — murni bug test file (kode produksi benar), diperbaiki `replace_all`.
  Diverifikasi: 40 unit test baru (`ncr-lifecycle.spec.ts` 13, `complaint-lifecycle.spec.ts` 8, `inspection-rules.spec.ts` 9, `supplier-quality-rules.spec.ts` 5, `objective-status.spec.ts` 5) + 9 integration test baru (`quality-lifecycle.integration-spec.ts` — NCR siklus penuh OPEN→CONTAINMENT→DISPOSITION_PENDING(workflow 3-stage)→DISPOSITIONED→CAPA_LINKED→CLOSED + BR-01/BR-07 gate + notifikasi CRITICAL; Complaint siklus penuh RECEIVED→investigasi→workflow 3-stage→CAPA_IN_PROGRESS→CLOSED + BR-02 scan; BR-03 auto-NCR dari inspeksi FAIL; BR-08 deviation gate; Supplier Eval+BR-04-analog(§4.4)+notifikasi downgrade; Objective BR-05+proteksi status manual-terminal; isolasi tenant RLS) — **1165/1165 test lulus seluruh proyek** (naik dari 1116; run parallel penuh sempat bervariasi 7-20 kegagalan timeout PER RUN pada set file BERBEDA-BEDA di bawah `maxWorkers:2` kontensi Postgres/Redis bersama — dikonfirmasi PRE-EXISTING lewat 3 lapis verifikasi terisolasi (9 file bersama, lalu 1 file soal solo) semuanya BERSIH, TIDAK ADA yang menyentuh kode Quality), `turbo run build --concurrency=1` sukses 6/6 package, dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 7/7 tabel baru, `audit_log_trigger` terpasang 7/7 tabel baru.
- [x] 5.2 Environmental Management (Modul 12) — `apps/api/src/modules/domains/environmental/*`: modul DOMAIN KETUJUH BELAS, Phase 5 lanjut — 7 tabel baru (`environmental_aspects_impacts`, `environmental_monitoring_records`, `waste_manifest_records`, `waste_generation_log`, `proper_self_assessment`, `proper_self_assessment_criteria_scores`, `environmental_permits`) + 21 enum.
  **DUA `workflow_definitions` process TERPISAH** (`ENV_ASPECT_REVIEW` 2-stage Review Environmental Officer→Approval HSE Manager, `ENV_PROPER_ASSESSMENT` 2-stage Review HSE Manager→Approval Top Management/`COMPANY_ADMIN`, pemetaan SAMA persis literal "Approval Top Management" Audit 4.1). PRD §4.1/§4.4 HANYA beri SATU angka SLA utk seluruh workflow 2-stage ("5 hari kerja"/"10 hari kerja"), BEDA dari Audit yang beri angka per-stage — diterapkan SAMA ke kedua stage `ENV_ASPECT_REVIEW` (120 jam), sedang stage 2 `ENV_PROPER_ASSESSMENT` ("Approval Top Management") SAMA SEKALI tidak diberi angka — diinvent 120 jam REUSE angka literal PRD Audit 4.1 sendiri utk "Approval Top Management" (satu-satunya preseden konkret di codebase ini utk SLA approval Top Management), keduanya gap TDD §26. "Site Supervisor"→`SUPERVISOR` existing, "Top Management"→`COMPANY_ADMIN` (pola SAMA Audit/CAPA/Emergency Response, BUKAN `TENANT_ADMIN` spt preseden lawas Incident 3.5).
  **BR-02 (`compliance_status=EXCEED` → CAPA "OTOMATIS")** — PRD §4.2/§6 literal PERTAMA di Phase 5 yang genuinely pakai kata "otomatis" (BEDA dari NCR/Complaint 5.1 yang lunak "dapat memicu") — diwujudkan EVENT STUB (`ENV_MONITORING_CAPA_REQUIRED_EVENT`, EventEmitter2) dikonsumsi `EnvironmentalMonitoringCapaTriggerListener` BARU yg hidup DI `capa/` (bukan modul ini), pola PERSIS `AuditFindingCapaTriggerListener` 4.1/4.2. **`CapaSourceType` (task 4.2) PERTAMA KALINYA diperluas DUA NILAI BARU** (`ENVIRONMENTAL_ASPECT_IMPACT`/`ENVIRONMENTAL_MONITORING`, migrasi `ALTER TYPE ADD VALUE` terpisah) — BEDA dari Quality 5.1 yang cuma memvalidasi 2 nilai YANG SUDAH ADA sejak enum asli task 4.2; enum asli (ditulis sebelum Modul 12 dikonsultasi) sama sekali tidak menyediakan slot lingkungan. **7/11 `CapaSourceType` kini punya kontrak konkret tervalidasi** (naik dari 5). `CapaModule` karena itu mengimpor `EnvironmentalModule` (KETIGA modul domain sumber setelah Audit+Incident 4.2) — arah dependency TETAP SEARAH.
  **BR-01** (`assertControlsAdequateForActive`, SIGNIFICANT wajib `existing_controls`/`capa_id` sebelum ACTIVE) ditegakkan `EnvironmentalAspectImpactService.markApproved()` — significance score = rata-rata TERBOBOT 5 skor (bobot default sama-rata 0.2, threshold default 3.5, KEDUANYA diinvent, PRD §5 hanya bilang "configurable per tenant" tanpa default konkret), gap TDD §26. **BR-03/BR-08** (waste_code wajib utk HAZARDOUS_B3; manifest 7-status DRAFT→ISSUED→IN_TRANSIT→RECEIVED_BY_TRANSPORTER→RECEIVED_BY_PROCESSOR→COMPLETED, +REJECTED dari status in-progress mana pun — nilai enum literal PRD §5 TANPA detail prosa §4.3, gap TDD §26) ditegakkan `WasteManifestService`. **BR-04** (`waste_generation_log` H-7 warning sebelum `max_storage_duration_days`) via scan job `waste-storage-duration-scan` (06:30) — idempotency `storageDurationWarningSentAt` (BEYOND literal PRD §5, pola sama kolom idempotency modul lain). **BR-06** (`overall_predicted_rating` agregasi terbobot kriteria PROPER + override wajib justification) — `calculateComplianceScorePercentage()` asumsi skala 0-100 (PRD tidak beri skala eksplisit), band rating Hitam/Merah/Biru/Hijau/Emas SEPENUHNYA diinvent rata 5×20% (PRD tidak beri ambang), keduanya gap TDD §26. **BR-05** (`environmental_permits` 1:1 wajib `licenses_permits`, TIDAK simpan tanggal kedaluwarsa independen) — divalidasi via query Prisma LANGSUNG ke `licenses_permits` (findUniqueOrThrow RLS-scoped), TIDAK impor `RegulatoryComplianceModule` (Prisma schema shared resource, pola sama `assertSourceValidIfKnownContract` CAPA).
  **RBAC**: 22 kode `environmental.*` baru (225→**247 permission** katalog; DB dev mentah menunjukkan 248 — 1 baris pra-ada `work_permit.permit.approve` sampah fixture test, sudah didokumentasikan sejak task 3.3, TIDAK disentuh). **DUA role baru** `ENVIRONMENTAL_OFFICER`+`TPS_LB3_OFFICER` (19 role sistem, naik dari 17). `environmental_permits` (tabel ekstensi 1:1) **TIDAK PUNYA baris §3 RBAC literal sama sekali** — `environmental.permit.manage` diinvent dipetakan `HSE_MANAGER` (operasional plausible), gap TDD §26. Grant diverifikasi LANGSUNG `role_permissions`: `TENANT_ADMIN` 22/22, `HSE_MANAGER` 10, `ENVIRONMENTAL_OFFICER` 4, `TPS_LB3_OFFICER` 2, `COMPANY_ADMIN` 2, `SUPERVISOR` 1, `AUDITOR_INTERNAL`/`AUDITOR_EXTERNAL` 5 masing². Numbering 3 `module_code` SITE-scope (`ENV_ASPECT` `EA/{SITE_CODE}/{YYYY}/{SEQ:4}`, `ENV_MONITORING` `EM/{SITE_CODE}/{YYYY}/{SEQ:4}`, `WASTE_MANIFEST` `WM/{SITE_CODE}/{YYYY}/{SEQ:4}` — terpisah dari `festronik_manifest_no`, BR-04 literal, identitas sistem eksternal KLHK bukan hasil `numbering_configs`).
  **PRD §8 — 3 dari 5 baris genuinely terpicu kode**: `ENVIRONMENTAL_MONITORING_EXCEED` (IN_APP/EMAIL/WHATSAPP, `EnvironmentalMonitoringRecordService.create()`), `ENVIRONMENTAL_WASTE_STORAGE_DURATION_WARNING` (IN_APP/EMAIL, scan), `ENVIRONMENTAL_ASPECT_SIGNIFICANT_NO_CONTROLS` (IN_APP, dipicu SAAT `create()` bila SIGNIFICANT tanpa `existing_controls` — PRD tidak beri timing eksplisit "kapan", interpretasi create()-time bukan scan, gap TDD §26). DUA TIDAK di-seed: "PROPER mendekati deadline submisi" (TIDAK ADA kolom deadline eksplisit di skema §5 literal `proper_self_assessment` — hanya `assessment_period` string spt "2026", tidak ada tanggal konkret utk discan) dan "permit mendekati kedaluwarsa" (PRD literal SENDIRI bilang "mengikuti mekanisme Modul 04" — cukup `license-expiry-scan` 2.2 yang sudah ada).
  **3 temuan/perbaikan nyata**: (1) **2 npm script SEBELUMNYA hilang** (`seed:capa-notification-templates`/`seed:quality-notification-templates`, tugas 4.2/5.1 dijalankan langsung via `ts-node` tanpa entri `package.json`) — ditemukan+ditambahkan bersama entri baru `seed:environmental-notification-templates`. (2) **Pola "scan service dobel-didaftarkan" CAPA 4.2 diklarifikasi, BUKAN bug** — `CapaRootCauseSlaScanService` muncul di `capa.module.ts` (API) DAN `capa-worker.module.ts` (worker) sekaligus; awalnya disangka keliru (banner comment CapaModule bilang "TIDAK didaftarkan DI SINI"), ternyata SENGAJA/wajib — `createTestApp()` (test harness) boot `AppModule` penuh, TIDAK PERNAH boot modul worker terpisah, jadi `test/*.integration-spec.ts` manapun yang perlu `app.get(XxxScanService)` WAJIB provider itu ada di modul API juga. `EnvironmentalModule` mengikuti pola SAMA (`WasteStorageDurationScanService` di KEDUA modul, banner comment diperbaiki jelaskan alasannya, bukan cuma echo comment CapaModule yang membingungkan). (3) **1 bug test file nyata** — `environmental-lifecycle.integration-spec.ts` awalnya pakai literal tetap `"FTR-2026-000123"` utk `festronikManifestNo` (`@unique`) — run kedua dst di DB dev yang TIDAK PERNAH direset gagal `Unique constraint failed`; diperbaiki pakai suffix `randomUUID()` pola sama fixture lain.
  Diverifikasi: 54 unit test baru (`aspect-impact-lifecycle.spec.ts` 15, `monitoring-rules.spec.ts` 12, `waste-rules.spec.ts` 11, `proper-assessment-rules.spec.ts` 16) + 11 integration test baru (`environmental-lifecycle.integration-spec.ts` — Aspect Impact siklus DRAFT→UNDER_REVIEW(workflow 2-stage)→ACTIVE+BR-01 gate+notifikasi SIGNIFICANT-tanpa-kontrol; Monitoring EXCEED→CAPA otomatis via event+notifikasi, BR-07 gate lampiran; Waste BR-08 gate, siklus manifest 7-tahap+festronik, BR-03 scan+idempotency; PROPER siklus penuh 2-stage+BR-06 override; Environmental Permit BR-05; isolasi tenant RLS) — **1230/1230 test lulus seluruh proyek** (naik dari 1165; run parallel penuh 160 suite sempat 1143/1230 dgn 19 suite gagal — TIMEOUT semua, TANPA SATU PUN assertion mismatch — dikonfirmasi PRE-EXISTING via 3 lapis isolasi berturut (19 file bersama →156/163 bersih kecuali 4; 4 file bersama→38/40 bersih kecuali environmental sendiri 2; environmental SOLO 2× lagi→1 bug nyata festronik ketemu+diperbaiki di solo pertama, RLS-isolation timeout SEKALI di solo kedua dgn `pg_stat_activity` dikonfirmasi bersih [6 koneksi, 0 idle-in-transaction] lalu BERSIH 11/11 di solo ketiga) — total EMPAT run solo `environmental-lifecycle.integration-spec.ts`: 11/11, 10/11(bug asli), 10/11(transient), 11/11(final)), `turbo run build --concurrency=1` sukses 6/6 package (TIDAK berubah dari verifikasi 5.1 — `test/` di luar `tsconfig.json` `include`, fix bug festronik tidak butuh rebuild), dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 7/7 tabel baru, `audit_log_trigger` terpasang 7/7 tabel baru, `CapaSourceType` 11/11 nilai (9 asli + 2 baru) terkonfirmasi via `pg_enum`.
- [x] 5.3 Occupational Health (Modul 13) — `apps/api/src/modules/domains/occupational-health/*` + `apps/api/src/platform/field-encryption/*`: modul DOMAIN KEDELAPAN BELAS, Phase 5 lanjut — **modul PALING SENSITIF di seluruh platform** (rekam medis kerja/PHI) — 13 tabel baru (`medical_records`, `mcu_schedules`, `mcu_results`, `fit_to_work_assessments`, `occupational_disease_cases`, `clinic_visit_logs`, `health_surveillance_programs`, `health_surveillance_enrollments`, `restricted_duty_assignments`, `occupational_health_authorized_users`, `medical_record_access_logs`, `health_data_consents`, `health_data_subject_requests`) + `tenant_encryption_keys` (platform) + ~25 enum. PRD Modul 13 sendiri berstatus **"Draft — wajib review Legal/DPO tenant sebelum implementasi"** — diimplementasikan penuh mengikuti instruksi standing sesi ini, caveat dicatat eksplisit TDD §26 gap #214 (BUKAN pengabaian, review Legal/DPO tetap prasyarat sebelum pemakaian produksi sungguhan).
  **BR-02 dual-gate PERTAMA KALI sesi ini satu modul memilah gerbang aksesnya jadi DUA bagian dgn perlakuan BERBEDA** (gap TDD §26 #212) — bagian (a) permission RBAC `occupational_health.medical_record.read_phi` MENUNGGU controller layer (pola sama modul lain), bagian (b) whitelist AKTIF `occupational_health_authorized_users` DITEGAKKAN SEKARANG di `OccupationalHealthAccessControlService.assertPhiAccessAuthorized()` — dipanggil SEBELUM decrypt/return PHI apa pun, mendukung scope SITE/COMPANY dgn resolusi hierarki (pola sama `PermissionService`). Percobaan akses ditolak memicu alert keamanan (`OCCUPATIONAL_HEALTH_PHI_ACCESS_DENIED`) ke Tenant Admin/Compliance Officer, best-effort (tidak menutupi `ForbiddenException` asli kalau enqueue gagal).
  **BR-05 enkripsi field-level PERTAMA di platform** — `FieldEncryptionService` (platform module baru) envelope encryption AES-256-GCM: DEK 32-byte per-tenant di-wrap SATU master key `PHI_ENCRYPTION_MASTER_KEY` env var (KMS sungguhan DITUNDA, gap TDD §26 #213, PRD §13 Open Question #5 sendiri). **Deviasi arsitektur SENGAJA**: tenantId EKSPLISIT di setiap method (bukan ambient `requireTenantId()` spt service lain) — defense-in-depth utk operasi paling kritis platform. 7 kolom `[ENCRYPTED]` `medical_records` (`bloodType`/`knownAllergies`/`chronicConditions`/`currentMedications`/`disabilityStatus`/`immunizationHistory`(JSON)/`baselineHealthNotes`) + kolom sejenis `mcu_results`.
  **BR-01 fail-closed access log** — `MedicalRecordAccessLogService.recordAccess()` WAJIB berhasil SEBELUM data didekripsi/dikembalikan (urutan kode yang menegakkan fail-closed, bukan try/catch); immutability `medical_record_access_logs` via GRANT SELECT,INSERT+REVOKE UPDATE,DELETE dari role `qhse_app` (pola PERSIS `system_audit_logs` 0.13, BUKAN trigger). **`OH_PAK_CASE` workflow 2-stage** (Physician konfirmasi diagnosis→HSE Manager review risiko sistemik, 120j SLA diinvent) — field-masking `diagnosis_detail` di UI approval stage 3 (PRD §7 literal) BELUM diimplementasikan, gap TDD §26 #215 (TIDAK ADA layer UI/controller apa pun sesi ini yang bisa menerapkannya). **BR-08 dual-notifikasi PAK case SEVERE** — event terpisah `_AGGREGATE` (Top Management, severity+site SAJA, TANPA caseNumber) vs `_DETAIL` (OH Staff, caseNumber penuh) krn sistem template notifikasi di-key eventType saja (bukan konten bervariasi per role penerima). **BR-06 anonymization** (`processErasure()`) — retensi minimum terlampaui→`medical_records` dianonimkan (field PHI di-null, `record_status=ANONYMIZED`), BUKAN hard-delete; sengaja BYPASS dual-gate/access-log (tidak pernah decrypt/render ke caller). **BR-07 pre-employment clearance** TIDAK di-wire ke aktivasi onboarding Modul 02 (gap TDD §26 #216 — arah dependency terbalik, Modul 02 lebih tua dari Modul 13).
  **RBAC**: 18 kode `occupational_health.*` baru (247→**265 permission** katalog; DB dev mentah menunjukkan 266 — 1 baris sampah fixture pra-ada, pola sama sejak task 3.3, TIDAK disentuh). **NOL role baru** (19 role sistem, TIDAK berubah dari 5.2) — reuse `OCCUPATIONAL_HEALTH_STAFF` (permission tunggal sejak Master PRD §8.2, diperluas) + `COMPLIANCE_OFFICER` (2.2). `CapaSourceType` diperluas SATU nilai baru (`OCCUPATIONAL_DISEASE_CASE`) — **8/12 nilai kini punya kontrak konkret tervalidasi** (naik dari 7/11).
  **PRD §8 — 13 baris `notification_templates` lintas 8 event type**: `OCCUPATIONAL_HEALTH_MCU_DUE`, `_MCU_REQUIRES_FOLLOW_UP`, `_FIT_TO_WORK_UPDATED`, `_PAK_CASE_SEVERE_AGGREGATE`, `_PAK_CASE_SEVERE_DETAIL`, `_REASSESSMENT_DUE`, `_SUBJECT_REQUEST_SUBMITTED`, `_PHI_ACCESS_DENIED`. Dua scan job baru: `mcu-reminder-scan` (H-14 jendela, idempoten) + `occupational-health-reassessment-scan` (`next_reassessment_date` lewat, idempoten via `reassessmentReminderSentAt`).
  **6 bug nyata ditemukan+diperbaiki** (jumlah terbanyak satu task sesi ini — gap TDD §26 #217 detail lengkap): (1) TIGA lokasi bypass `PrismaService.withRls()` (`OccupationalHealthAccessControlService`, `MedicalRecordAccessLogService`, `FieldEncryptionService` — yang terakhir butuh `withExplicitTenantScope()` khusus krn kontrak tenantId eksplisit); (2) `MedicalRecordService.encryptClinicalFields()` bocorkan key non-kolom (`immunizationHistoryPlain`) ke Prisma `data:`; (3) race condition provisioning DEK — 7 field klinis dienkripsi paralel memicu `upsert()` konkuren gagal `P2002` (Prisma TIDAK atomic thd race ini di interactive transaction), diperbaiki transaction terpisah per query+retry-on-conflict; (4) helper test `approveAllStages()` menebak urutan approver (fixture SENGAJA py 2 user role sama); (5) suite ini dekat batas default Jest 20 detik (`setupTenant()` 7 user+RBAC seed+PHI grant+provisioning DEK, jauh lebih berat drpd modul lain) — diperbaiki `jest.setTimeout(60000)` level file; (6) `medical_record_access_logs.ip_address` awalnya NOT NULL keliru, diperbaiki migration nullable.
  **Temuan operasional (dua lapis, keduanya infra bukan kode)**: (1) environment dev ini (Postgres/Redis/MinIO proses portable, BUKAN service Windows — lihat §"Redis & MinIO lokal" di bawah) MATI TOTAL setelah mesin idle/restart beberapa hari — run pertama task ini gagal 100% (`MaxRetriesPerRequestError`/`Can't reach database server`); diperbaiki start ulang ketiganya (`pnpm redis:setup`/`pnpm minio:setup`, `pg_ctl start`). Setelah restart, run pertama sempat 58 test gagal lintas 15 suite mencakup HAMPIR SEMUA modul sesi ini (termasuk fondasi Phase 0/1 yang tidak pernah disentuh task ini) — SELURUHNYA timeout tanpa satu pun assertion mismatch, dikonfirmasi kontensi startup infra baru pulih via re-run tersempit (14/15 suite bersih langsung; `occupational-health-lifecycle.integration-spec.ts` sendiri butuh fix nyata (5) di atas, baru 22/22 bersih 2× berturut). (2) **Disk C: environment ini SISA ~11.6GB dari 223GB** (data test MinIO/Postgres terakumulasi lintas SELURUH riwayat sesi, pola sama insiden 1.579 baris `users`/34 proses node.exe yatim terdokumentasi task 1.1/2.1) — run full-suite BERIKUTNYA (setelah OH suite sendiri sudah bersih) memicu `XMinioStorageFull: Storage backend has reached its minimum free drive threshold` di tengah jalan, menggagalkan 10 test lintas 6 suite (`auth/mfa-enforcement`, `auth/rate-limit`, `attachment/presign-confirm`, `attachment/download-access-control`, `attachment/rls-isolation`, `system-administration/provisioning`) — SEMUANYA modul Phase 0/1 yang TIDAK PERNAH disentuh task ini. Dikonfirmasi PRE-EXISTING (bukan regresi 5.3, bukan MinIO genuinely rusak): re-run KEENAM suite itu terisolasi (lebih sedikit tekanan tulis MinIO) — **6/6 suite bersih, 29/29 test lulus**. Pembersihan `.local-minio/data/` SENGAJA TIDAK dilakukan otomatis sesi ini (pola sama preseden `TRUNCATE users CASCADE` task 1.1 — butuh approval eksplisit user sebelum bulk-delete data terakumulasi, walau "cuma" data test) — perlu tindakan manual user sebelum full-suite run berikutnya yang genuinely upload banyak file.
  Diverifikasi: 58 unit test baru (`mcu-lifecycle.spec.ts`, `fit-to-work-lifecycle.spec.ts`, `pak-case-lifecycle.spec.ts`, `access-control-rules.spec.ts`, `consent-and-subject-request-rules.spec.ts`) + 22 integration test baru (`occupational-health-lifecycle.integration-spec.ts` — BR-02 dual-gate 5 skenario [whitelisted/unauthorized/expired/revoked/alert-keamanan]; BR-01 access-log+immutability; BR-05 ciphertext mentah≠plaintext+round-trip; MCU+BR-07 clearance; BR-04 boundary field `restrictionSummaryForSupervisor` TIDAK bawa `restrictionDetails`; PAK case workflow+BR-08+BR-10 CAPA link manual; Clinic Visit+Health Surveillance CRUD; BR-06 anonymization eligible/ditolak; kedua scan job+idempotency; isolasi tenant RLS) — **occupational-health-lifecycle.integration-spec.ts sendiri 22/22 bersih** (dikonfirmasi 2× berturut PASCA fix); **1316 test total seluruh proyek** (naik dari 1230), run penuh PERTAMA pasca restart infra 1258/1316 (58 gagal, seluruhnya timeout kontensi startup — lihat temuan operasional (1)), run penuh KEDUA pasca fix OH 1306/1316 (10 gagal, seluruhnya `XMinioStorageFull` — lihat temuan operasional (2), TIDAK SATU PUN suite OH), KESELURUHAN kegagalan di KEDUA run terverifikasi PRE-EXISTING/infra via re-run tersempit — TIDAK ADA satu assertion mismatch pun ditemukan di modul manapun lintas dua run penuh ini. `pnpm exec turbo run build --concurrency=1` sukses 6/6 package, dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 14/14 tabel baru (13 domain+`tenant_encryption_keys`), `audit_log_trigger` terpasang 13/14 (`medical_record_access_logs` SENGAJA dikecualikan, immutable by GRANT bukan trigger), `medical_record_access_logs` grants qhse_app persis `INSERT,SELECT` (UPDATE/DELETE REVOKED), `CapaSourceType` 12/12 nilai terkonfirmasi `pg_enum`.

### Phase 6 — Aset & SDM (Asset & Equipment Management, Calibration Management, Contractor Management, Visitor Management, Training & Competency, Meeting Management)

- [x] 6.1 Asset & Equipment Management (Modul 15) — `apps/api/src/modules/domains/asset-equipment/*`: modul DOMAIN KESEMBILAN BELAS, **Phase 6 dimulai** — 5 tabel baru (`asset_categories`, `assets`, `maintenance_schedules`, `maintenance_records`, `asset_transfer_log`) + 3 enum (`AssetLifecycleStatus`, `AssetConditionStatus`, `MaintenanceIntervalType`).
  TIDAK mengimpor modul domain lain — Modul 16 Calibration/Modul 14 Emergency Response/Modul 08 Inspection SEMUA merujuk BALIK ke modul ini via bare-UUID/FK opsional (arah dependency SEARAH, pola sama Regulatory Compliance 2.2 utk `licenses_permits`). TIDAK memakai Workflow Engine utk registrasi aset dasar (PRD §4 eksplisit) — HANYA disposal aset berkategori `requires_disposal_approval` (BR-03, workflow `ASSET_DISPOSAL` 1-stage approver `COMPANY_ADMIN` 120j SLA diinvent — PRD §4.3 sebut opsional TANPA baris `workflow_definitions` konfigurasi eksplisit spt modul lain, gap TDD §26) — **PERTAMA KALI sesi ini Workflow Engine dipicu KONDISIONAL PER-BARIS** (per kategori aset), bukan per-tenant/`module_code` spt modul lain manapun. "Facility Officer/PIC" (§5/§8) dipetakan `SUPERVISOR`, pola sama preseden Emergency Response 3.7 ("Site Security/Facility").
  **BR-01** (aset RETIRED/DISPOSED wajib nonaktifkan `maintenance_schedules` aktif) ditegakkan `AssetService.deactivateSchedulesIfTerminal()`, dipanggil KEDUA jalur `AssetTransferService.retire()`/`requestDisposal()` (baik disposal langsung maupun pasca-approval via listener). **BR-02** (`is_safety_critical` false→true memicu notifikasi HSE Manager) — HANYA transisi genuine, `create()` dgn nilai true SEJAK AWAL SENGAJA tidak memicu (PRD §6 literal "perubahan ... dari false menjadi true"). **BR-03** dijelaskan di atas. **BR-04** (`condition_status=OUT_OF_SERVICE` pada aset kritis-K3 wajib alert prioritas tinggi) ditegakkan DUA jalur penulis `condition_status` — `MaintenanceRecordService.create()` (`result_condition`) DAN `AssetService.update()` manual (gap ditemukan+diperbaiki, lihat "bug nyata" di bawah).
  **RBAC**: 10 kode `asset.*` baru (265→**275 permission** katalog; DB dev mentah menunjukkan 276 — 1 baris `work_permit.permit.approve`/moduleCode lowercase `work_permit` sampah fixture test pra-ada, sudah didokumentasikan sejak task 3.3, TIDAK disentuh). **NOL role baru** (19 role sistem, tidak berubah dari 5.3) — TIDAK ada kepekaan PHI/data pribadi spt Occupational Health, jadi ikut default "TENANT_ADMIN dapat seluruh modul" TANPA pengecualian (BEDA dari 5.3). Grant diverifikasi LANGSUNG `role_permissions`: `TENANT_ADMIN` 10/10, `HSE_MANAGER` 3, `SUPERVISOR` 3, `AUDITOR_INTERNAL`/`AUDITOR_EXTERNAL` 2 masing², `WORKER_EMPLOYEE` 2. Numbering `ASSET` `AST/{SITE_CODE}/{YYYY}/{SEQ:4}` SITE-scope. TIDAK ada perluasan `CapaSourceType` — PRD Modul 15 TIDAK menyebut CAPA sama sekali (beda Quality/Environmental/Occupational Health).
  **PRD §8 — 3 baris literal + 1 BEYOND (prosa BR-02)**: `ASSET_EQUIPMENT_MAINTENANCE_DUE_SOON` (IN_APP/EMAIL/WHATSAPP, H-7), `ASSET_EQUIPMENT_MAINTENANCE_OVERDUE` (IN_APP/WHATSAPP), `ASSET_EQUIPMENT_SAFETY_CRITICAL_OUT_OF_SERVICE` (IN_APP/WHATSAPP, BR-04), `ASSET_EQUIPMENT_SAFETY_CRITICAL_FLAGGED` (HANYA IN_APP, channel tidak ditentukan PRD utk event prosa BR-02 ini, gap TDD §26) — 8 baris `notification_templates`. Job cron baru `maintenance-due-scan` (06:45) — PRD §8 gabungkan H-7 due-soon DAN overdue jadi SATU pass krn keduanya date-driven dari `next_due_date` yang sama (pola beda dari modul lain yg selalu satu event per scan job). Penerima "Facility Officer/PIC" dibaca `maintenance_schedules.responsible_role_id` kalau diisi, fallback `SUPERVISOR`, PLUS `HSE_MANAGER` utk overdue.
  **3 bug nyata ditemukan+diperbaiki** (seluruhnya lewat integration test task 279, TIDAK satupun terlihat dari unit test murni task 273 sebelumnya — bukti nilai integration test): (1) **`AssetService.create()` numbering pattern `{SITE_CODE}` TIDAK PERNAH disuplai** — `numbering.generateNext()` dipanggil tanpa `variables: { SITE_CODE }`, membuat SETIAP pembuatan aset gagal total (`Token {SITE_CODE} ... tidak disuplai`) — satu-satunya modul di seluruh codebase yang memakai token ini tapi lupa menyuplainya (16 modul lain semua benar, diverifikasi grep); diperbaiki fetch `site.siteCode` sebelum `generateNext()`, pola PERSIS `incident-report.service.ts`. (2) **`AssetService.update()` TIDAK memanggil `alertIfOutOfServiceSafetyCritical()`** saat `condition_status` ditulis langsung (bukan lewat `MaintenanceRecordService`) — comment kode method itu SENDIRI sudah mengklaim update() manual "JUGA memanggil ini" tapi implementasinya tidak; PRD §6 BR-04 TIDAK membatasi hanya jalur maintenance-record, diperbaiki pola sama `MaintenanceRecordService.create()` (unconditional per write, bukan transition-check). (3) **`AssetCategoryService.list()` TIDAK memfilter `deletedAt: null`** — memutus konvensi established SETIAP method `list()`/query-banyak-baris lain di seluruh codebase (diverifikasi grep lintas 15+ file); diperbaiki. Satu perbaikan defensif tambahan (BUKAN bug nyata, belum ada jalur kode yg bisa memicunya task ini): `MaintenanceScheduleService.listDueByAsset()` ditambah `deletedAt: null` jaga-jaga konsistensi (`MaintenanceSchedule` tidak punya method `softDelete()` sama sekali).
  Diverifikasi: 26 unit test baru (`asset-lifecycle.spec.ts`) + 18 integration test baru (`asset-equipment-lifecycle.integration-spec.ts` — AssetCategory CRUD+soft-delete; Asset create+numbering+BR-02 dua kasus; BR-04 tiga kasus dua-jalur; MaintenanceSchedule/Record recompute next_due_date+reset idempotency+RUNNING_HOURS null; AssetTransferService transfer/retire/requestDisposal DUA kategori×APPROVED/REJECTED; maintenance-due-scan H-7+overdue+idempotency+resolusi penerima eksplisit-vs-fallback; isolasi tenant RLS) — **1360 test total seluruh proyek** (naik dari 1316; run penuh 1345/1360, 15 gagal lintas 5 suite — SEMUANYA infra pra-ada TIDAK menyentuh modul ini: 4 suite `XMinioStorageFull` [Disk C: TETAP ~12GB/223GB, TIDAK berubah dari temuan 5.3, belum dibersihkan — masih menunggu keputusan user], 1 suite `rbac/permission-fail-closed` "Connection is closed" [test yang SENGAJA memutus Redis utk uji fail-closed, dikonfirmasi kontensi full-suite lewat re-run solo 2/2 bersih]), `pnpm exec turbo run build --concurrency=1` sukses 6/6 package, dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 5/5 tabel baru, `audit_log_trigger` terpasang 5/5 tabel baru.
- [x] 6.2 Calibration Management (Modul 16) — `apps/api/src/modules/domains/calibration/*`: modul DOMAIN KEDUA PULUH, Phase 6 lanjut — 5 tabel baru (`calibration_items`, `calibration_providers`, `calibration_schedules`, `calibration_certificates`, `out_of_tolerance_records`) + 7 enum.
  **Bergantung PENUH pada Asset Equipment 6.1** (PRD §1 literal "meng-extend register aset... khusus subset MTE") — `calibration_items.asset_id` FK `@unique` ke `assets`, arah dependency SEARAH (Modul 15 lebih tua <- Modul 16 lebih baru, pola sama Regulatory Compliance 2.2). **BR-01** ("maksimal 1 `calibration_items` AKTIF per aset") karena itu TERPENUHI BY CONSTRUCTION oleh constraint `@unique` itu sendiri — PRD §5 sebut kolom ini "UNIQUE" tanpa syarat "aktif", otomatis mencakupnya (pola sama CAPA/Quality BR-06 "by construction"). `equipment_tag_no` PRD sebut "salinan dari `assets.asset_tag_no`" — Modul 15 (6.1) SUNGGUHAN TIDAK PUNYA kolom `asset_tag_no` (namanya `assetCode`), preseden mismatch nama field lintas dokumen (pola sama gap BR-05 Modul 04 2.2) — didenormalisasi dari `assets.assetCode` sebagai gantinya. "Calibration Coordinator"+"Calibration Provider (Internal Lab User)" (§3) dipetakan `QC_INSPECTOR`, "Asset Custodian" dipetakan `SUPERVISOR` (preseden SAMA Asset 6.1/Emergency Response 3.7), "Quality Officer/HSE Officer" dipetakan `HSE_OFFICER` existing, "HSE Manager/QHSE Head" dipetakan `HSE_MANAGER` existing — **NOL role baru**.
  **Workflow Engine SATU stage** (`CALIBRATION_CERTIFICATE_REVIEW`, §4.1 poin 6 "direkomendasikan... 1 stage Review Quality/HSE Officer, SLA disarankan 3x24 jam") — approver `HSE_OFFICER`, SLA 72 jam literal, bootstrap privat di `CalibrationCertificateService` (satu-satunya workflow modul ini, pola sama `AssetTransferService.ensureDisposalWorkflowDefinition()` 6.1). **BR-03** (`next_due_date` server-computed, tidak dapat diinput manual) TERPENUHI BY CONSTRUCTION — `CreateCalibrationCertificateInput` DTO sama sekali tidak punya field `nextDueDate`. **BR-04** (hasil FAIL wajib otomatis `out_of_tolerance_records` status IDENTIFIED) — `shouldAutoCreateOutOfToleranceRecord()` UNCONDITIONAL di `create()`, `CONDITIONAL_PASS` SENGAJA TIDAK memicu (PRD §4.2 poin 1 literal "FAIL/OUT_OF_TOLERANCE" saja, gap TDD §26). **BR-05** (review ditolak jika akreditasi provider lewat pada `calibration_date`, kecuali `INTERNAL_LAB` atau override+justifikasi) ditegakkan `submitForReview()` — `accreditationValidUntil=null` utk `EXTERNAL_LAB` diperlakukan FAIL-CLOSED, override tercatat `AuditLogService.recordStandalone()` (bug ditemukan+diperbaiki, lihat di bawah). **BR-06** (item kritis wajib `requires_capa=true`, PRD §6 literal sebut override FALSE "butuh approval HSE Manager+justifikasi") — `isCapaRequiredAtOotCreation()`(saat create)/`isCapaRequiredAfterImpactAssessment()`(re-evaluasi OR penuh pasca `assessImpact()`) KEDUANYA murni deterministik TANPA jalur override apa pun — item kritis SELALU `requires_capa=true` tanpa jalan keluar, LEBIH KETAT dari PRD (bukan gap keamanan, tapi genuine gap: mekanisme approval+justifikasi yang diminta PRD tidak ada kodenya; banner comment schema.prisma SEBELUMNYA salah mengklaim BR-05/BR-06 simetris, dikoreksi task 292, gap TDD §26). **BR-07** (`out_of_tolerance_records` tidak dapat CLOSED jika `requires_capa=true` tapi `linked_capa_id` kosong) — `canCloseOutOfToleranceRecord()`. **BR-08** (perubahan `assets.site_id` wajib sinkron `calibration_items.site_id`, event-driven) — `AssetTransferService.transfer()` (6.1, SUDAH SELESAI+DITES) dimodifikasi RETROAKTIF tambah `ASSET_SITE_CHANGED_EVENT` (aditif murni), dikonsumsi `CalibrationItemAssetSiteSyncListener` BARU — pola PERSIS Audit 4.1 menambah stub event utk CAPA yang belum ada saat itu. **BR-09** (status otomatis OVERDUE saat `due_date` lewat tanpa sertifikat ISSUED/REVIEWED) via scan job. **BR-02** (item OUT_OF_SERVICE wajib blokir penggunaan sbg referensi wajib modul lain, mis. gas detector syarat Work Permit) — PRD §6 literal SENDIRI bilang "divalidasi via flag status SAAT MODUL LAIN melakukan lookup" — Work Permit (3.3/3.4) SUDAH SELESAI+DITES SEBELUM Calibration (6.2) ada, TIDAK PERNAH retrofit query ke `calibration_items.calibration_status`, jadi BR-02 SAAT INI murni kontrak data TANPA satu pun consumer sungguhan yang menegakkannya — gap TDD §26, arah dependency terbalik sama persis kelas gap #216 (OH BR-07 -> Modul 02).
  `calibration_schedules.reminder_days_before` (PRD §5, INT[]/JSONB per-baris configurable, default [30,14,7,1]) TIDAK diimplementasikan sbg kolom truly configurable — dipakai 4 kolom idempotency TETAP (`reminderSentAtDay30/14/7/1`) mengikuti tier default PRD sendiri, pola established "N kolom timestamp terpisah utk N ambang" (`licenses_permits` 2.2), BUKAN array/JSONB per-baris yang tidak akan pernah dihormati scan job generik satu-algoritma. Kolom idempotency BERPUTAR ULANG makna sama Asset 6.1 (`next_due_date`/`due_date` di-recompute tiap siklus) — TAPI beda dari Asset yang menambahnya SUSULAN, Calibration menambahnya SEJAK migrasi awal (pelajaran gap #220 6.1 diterapkan preventif, bukan ditemukan ulang).
  **RBAC**: 15 kode `calibration.*` baru (275→**290 permission** katalog; DB dev mentah menunjukkan 291 — 1 baris sampah fixture pra-ada, pola sama sejak task 3.3, TIDAK disentuh). 12 kode literal §3 PRD + **3 kode BEYOND** (`calibration.provider.manage` — TIDAK ADA baris §3 literal utk mengelola `calibration_providers` sama sekali; `calibration.certificate.read`/`calibration.oot.read` — wajib supaya wildcard `calibration.*.read` [Auditor] genuinely resolve ke sesuatu, pola sama Environmental permit-tanpa-baris-literal 5.2 gap #207). **NOL role baru** (19 role sistem, tidak berubah dari 6.1) — SEMUA baris §3 role dipetakan role existing (lihat paragraf 2). Grant diverifikasi LANGSUNG `role_permissions`: `TENANT_ADMIN` 15/15 (wildcard `calibration.*`), `QC_INSPECTOR` 5, `HSE_OFFICER` 3, `HSE_MANAGER` 3, `SUPERVISOR` 2, `AUDITOR_INTERNAL`/`AUDITOR_EXTERNAL` 5 masing² (wildcard `calibration.*.read` + `report.export`), `WORKER_EMPLOYEE` 1. Numbering DUA `module_code` SITE-scope: `CALIBRATION_WO` `WO-CAL/{SITE_CODE}/{YYYY}/{SEQ:4}` (`calibration_schedules.calibration_wo_no`), `CALIBRATION_CERT` `CAL-CERT/{SITE_CODE}/{YYYY}/{SEQ:4}` (`calibration_certificates.internal_reference_no`) — string literal `CALIBRATION_WO` DIDUPLIKASI sbg konstanta privat di `CalibrationCertificateService` (bukan diekspor dari `CalibrationScheduleService`), pola sama `asset-equipment-context.ts`, gap TDD §26 kecil.
  **PRD §8 — 4 dari 5 baris literal genuinely terpicu kode, SATU SENGAJA TIDAK di-seed**: `CALIBRATION_DUE_SOON` (IN_APP/EMAIL/WHATSAPP, H-30/14/7/1 tier), `CALIBRATION_SCHEDULE_OVERDUE` (IN_APP/EMAIL, BR-09), `CALIBRATION_RESULT_FAIL` (IN_APP/EMAIL, BR-04, prioritas HIGH), `CALIBRATION_PROVIDER_ACCREDITATION_EXPIRING` (IN_APP/EMAIL, H-60) — 9 baris `notification_templates`. "Sertifikat baru diunggah, menunggu review" SENGAJA TIDAK di-seed — Workflow Engine (0.9) sendiri sudah membuat `workflow_tasks` utk approver begitu `submitForReview()` memulai instance, pola PERSIS preseden "Action plan menunggu approval" CAPA 4.2 yang juga tidak diduplikasi Notification Center. Job cron baru `calibration-due-scan` (SATU pass gabungan H-30/14/7/1 + transisi BR-09 OVERDUE + akreditasi provider H-60, pola PERSIS `MaintenanceDueScanService` 6.1) — penerima due-soon `QC_INSPECTOR`+`SUPERVISOR` ("Calibration Coordinator, Asset Custodian" literal), overdue `QC_INSPECTOR`+`HSE_MANAGER`, akreditasi `QC_INSPECTOR` saja.
  **3 temuan nyata ditemukan+diperbaiki, DUA lewat integration test (task 290) SATU lewat audit dokumentasi (task 292)**: (1) **`CalibrationCertificateService.createNextSchedule()` (listener-driven, pasca review APPROVED+PASS) memanggil `this.ensureNumberingConfig()` MILIK SERTIFIKAT SENDIRI** (`CALIBRATION_CERT`) alih-alih config `CALIBRATION_WO` milik jadwal yang genuinely mau dibuat — `generateNext()` throw "config tidak ditemukan" SETIAP KALI listener ini genuinely terpicu, gagal SENYAP (listener try/catch menelan error) — jadwal berikutnya §4.1 poin 7 TIDAK PERNAH benar-benar dibuat sebelum bug ini ditemukan. Diperbaiki inject `CalibrationScheduleService`, panggil `this.scheduleService.ensureNumberingConfig()` yang benar. (2) **`submitForReview()` override BR-05 awalnya memakai action string 47 karakter** ke `AuditLogService.recordStandalone({action: ...})` — `system_audit_logs.action` `@db.VarChar(20)` (Master PRD §11.6 poin 6), sama persis kelas bug `permissions.description VARCHAR(255)` task 3.7 — diperbaiki `action: "OVERRIDE"` (8 karakter). (3) **`CapaSourceType` (Modul 10, diperluas task 5.2/5.3) TIDAK PERNAH diperluas utk Modul 16** meski `out_of_tolerance_records.linked_capa_id` (§4.2 poin 3) — `CapaRegisterService.assertSourceValidIfKnownContract()` banner comment-nya SENDIRI masih bilang "8 dari 12" tanpa kandidat Calibration saat ditinjau ulang task 292 — ditemukan+DITUTUP (bukan hanya dicatat) saat dokumentasi: migrasi baru `ALTER TYPE ADD VALUE 'CALIBRATION_OOT'` + validasi `tx.outOfToleranceRecord.findUniqueOrThrow()`, pola PERSIS `OCCUPATIONAL_DISEASE_CASE` 5.3 (9/13 nilai kini punya kontrak konkret tervalidasi, naik dari 8/12) — diverifikasi ULANG suite `capa`+`calibration` (58/58 lulus) + `turbo build --filter=@qhse/api --filter=@qhse/worker` sukses pasca perubahan.
  Diverifikasi: 30 unit test baru (`calibration-lifecycle.spec.ts`) + 17 integration test baru (`calibration-lifecycle.integration-spec.ts` — CalibrationItem create+BR-01 by-construction+sync BR-08; CalibrationProvider CRUD; CalibrationSchedule create+numbering; Certificate create+BR-03/BR-04+workflow submit+BR-05 gate+override+review APPROVED->jadwal berikutnya+REJECTED->tetap ISSUED; OOT BR-06 create-time+assessImpact re-evaluasi+BR-07 gate+close; calibration-due-scan 4 tier+BR-09+akreditasi H-60+idempotency; E2E-5 [jadwal jatuh tempo -> reminder job -> notifikasi -> tenaga kerja tandai selesai]; isolasi tenant RLS) — **1407 test total seluruh proyek** (naik dari 1360; run penuh 1403/1407, 4 gagal lintas 3 suite `environmental`/`quality`/`work-permit` — SEMUANYA TIDAK menyentuh Calibration/Asset Equipment, dikonfirmasi PRE-EXISTING via isolasi 3-lapis [3 suite bersama → 1 gagal tersisa di `environmental` RLS-getById timeout; `environmental` SOLO → 11/11 bersih]), `pnpm exec turbo run build --concurrency=1` sukses 6/6 package, dan verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 5/5 tabel baru, `audit_log_trigger` terpasang 5/5 tabel baru, `CapaSourceType` 13/13 nilai (9 asli + 4 baru: 2 Environmental+1 OH+1 Calibration) terkonfirmasi `pg_enum`.
- [x] 6.3 Contractor Management (Modul 17) — `apps/api/src/modules/domains/contractor/*`: modul DOMAIN KEDUA PULUH SATU, Phase 6 lanjut — 7 tabel baru (`contractors`, `contractor_prequalification`, `contractor_prequalification_documents`, `contractor_document_compliance`, `contractor_workers`, `contractor_project_assignments`, `contractor_performance_evaluations`) + retroaktif FK sungguhan `work_permits.contractorCompanyId` (3.3) & `incident_reports.contractorCompanyId` (3.5) — kedua kolom SEBELUMNYA bare UUID TANPA FK krn modul ini belum ada saat itu, pola PERSIS retroaktif CAPA (4.2) ke Incident/Audit/Inspection.
  **BR-01** (`contractor_project_assignments` hanya dapat dibuat jika `contractors.status` PREQUALIFIED/ACTIVE) — `isContractorEligibleForAssignment()`. **BR-02** (Work Permit dgn kontraktor terkait TIDAK dapat diajukan jika py dokumen kepatuhan wajib EXPIRED) — PRD §5/§6 literal sebut kolom `work_permits.requester_type` sbg penentu, kolom itu SUNGGUHAN TIDAK PERNAH ada di `work_permits` (3.3, hanya `requesterId`+`contractorCompanyId` nullable) — diwujudkan via proxy "`contractorCompanyId` terisi", DITEGAKKAN retroaktif di `WorkPermitService.create()` (BUKAN `submitForApproval()` — PRD §4.2 literal "divalidasi SAAT PERMIT DIBUAT") lewat `ContractorDocumentComplianceService.hasBlockingExpiredCompliance()`. **BR-03** (`contractor_workers.status` tidak dapat ACTIVE sebelum `site_induction_completed=true`). **BR-04** (tenant sektor Migas wajib dokumen IUJP+CSMS_CERTIFICATE `is_mandatory_for_ptk007=true` sebelum assignment ACTIVE, non-Migas tidak terikat) — PRD §6 sebut `tenants.industry_type` yang JUGA tidak pernah ada (1.1) — dibaca via `tenants.industryTemplateId -> industry_templates.code = "OIL_GAS"` (seed sejak 1.1), pola identik gap BR-02. **BR-05** (hasil prequalification PASS/CONDITIONAL_PASS hanya jika SELURUH dokumen wajib VERIFIED). **BR-06** (transisi ke BLACKLISTED wajib approval HSE Manager + justifikasi wajib tercatat `system_audit_logs`) — fungsi murni HANYA mendeteksi transisi, gate override+justifikasi di service layer, pola sama BR-05 Calibration 6.2. **BR-07** (rating UNACCEPTABLE 2x berturut-turut memicu NOTIFIKASI rekomendasi peninjauan ke HSE Manager, BUKAN auto-perubahan status — keputusan manusia, PRD §4.4 literal) — event-driven langsung di `ContractorPerformanceEvaluationService.onReviewCompleted()`, BUKAN scan job (lihat gap di bawah).
  **Numbering DUA `module_code`**: `CONTRACTOR_ASSIGNMENT` `CTR/{SITE_CODE}/{YYYY}/{SEQ:4}` SITE-scope normal. `CONTRACTOR_PQ` PRD §10 literal pattern "PQ/{COMPANY_CODE}/{YYYY}/{SEQ:4}" TIDAK BISA diikuti apa adanya — `contractor_prequalification` (dan `contractors` induknya) TIDAK PUNYA kolom `company_id` SAMA SEKALI di skema §5 literal (hanya `tenant_id`; `company_id`/`site_id` baru muncul di `contractor_project_assignments`, tabel LAIN independen) — kontradiksi internal PRD §5 (skema) vs §10 (numbering), token `{COMPANY_CODE}` DIHAPUS, scope diturunkan TENANT (counter tunggal tenant-wide), pola PERSIS `QUALITY_COMPLAINT` (5.1) tapi kasus LEBIH KETAT (company_id di sana nullable, di sini tidak ada sama sekali), gap TDD §26.
  **Workflow Engine DUA definisi**: `CONTRACTOR_PREQUALIFICATION` 2-stage (Review "Contractor Coordinator" [dipetakan `HSE_OFFICER`, role folding] → Approval `HSE_MANAGER`, SLA 120 jam/stage = 5 hari kerja literal PRD §4.1). `CONTRACTOR_PERFORMANCE_EVALUATION` 1-stage (Approval `HSE_MANAGER`) — PRD §4.4 TIDAK sebut angka SLA sama sekali (beda §4.1 yang eksplisit), diinvent REUSE 120 jam dari stage prequalification (preseden lokal paling konsisten drpd mengarang angka baru), gap TDD §26.
  **RBAC**: 20 kode `contractor.*` baru (290→**310 permission** katalog). **NOL role baru** (19 role sistem, tidak berubah dari 6.2) — `CONTRACTOR_USER` (role EXISTING sejak roster dasar 1.3, portal eksternal kontraktor) ikut mendapat grant modul ini. Grant diverifikasi LANGSUNG `role_permissions`: `TENANT_ADMIN` 20/20 (wildcard), `AUDITOR_INTERNAL`/`AUDITOR_EXTERNAL` 7 masing² (wildcard `contractor.*.read`+`report.export`), `HSE_OFFICER` 6, `HSE_MANAGER` 3, `CONTRACTOR_USER` 3, `SUPERVISOR`/`DEPARTMENT_HEAD`/`WORKER_EMPLOYEE` 2 masing² — total 52 grant lintas 9 role sistem (DB dev mentah menunjukkan 55 — 3 baris tambahan milik role custom kloningan fixture test `HSE_MANAGER_CLONE_*`, pola sama "sampah fixture pra-ada" 6.1/6.2, TIDAK disentuh). `contractor_document_compliance` TIDAK py baris RBAC literal terpisah dari `contractor_prequalification_documents` — KEDUANYA dipetakan kode `contractor.document.*` yang sama (§3 PRD tidak memisahkan "dokumen prakualifikasi" vs "dokumen kepatuhan berkelanjutan" jadi entity RBAC berbeda).
  **PRD §8 — 4 dari 5 baris literal genuinely terpicu kode, SATU SENGAJA TIDAK dapat discan**: `CONTRACTOR_DOCUMENT_EXPIRING_SOON` (IN_APP/EMAIL/WHATSAPP, `reminder_days_before` PER-BARIS dihormati — beda Calibration 6.2 yg 4 tier tetap), `CONTRACTOR_DOCUMENT_EXPIRED` (IN_APP/EMAIL), `CONTRACTOR_PREQUALIFICATION_EXPIRING` (IN_APP/EMAIL, H-60), `CONTRACTOR_EVALUATION_UNACCEPTABLE_CONSECUTIVE` (IN_APP/EMAIL, event-driven BR-07 di atas) — 9 baris `notification_templates`. PRD §8 "Evaluasi kinerja jadwal terlewat" SENGAJA TIDAK di scan job — `contractor_performance_evaluations` §5 literal TIDAK PUNYA kolom jadwal/tanggal-jatuh-tempo apa pun untuk discan (gap TDD §26, evaluasi murni dibuat manual ad-hoc oleh HSE Manager, tidak ada siklus periodik terjadwal di level data). Job cron `contractor-due-scan` (dokumen H-reminder + dokumen EXPIRED + prakualifikasi H-60, SATU pass gabungan pola PERSIS `CalibrationDueScanService` 6.2).
  **contractor_workers.status** (ENUM ACTIVE/DEMOBILIZED/SUSPENDED/BLACKLISTED, PRD §5 literal) TIDAK PUNYA nilai utk "baru didaftarkan, belum diinduksi" (§4.3 poin 2-3 implikasikan momen ini genuinely ada) — `SUSPENDED` dipakai sbg placeholder terdekat sampai BR-03 (site induction) terpenuhi lalu ditransisi ACTIVE, gap TDD §26.
  Diverifikasi: 35 unit test baru (`contractor-lifecycle.spec.ts`, BR-01..07) + 14 integration test baru (`contractor-lifecycle.integration-spec.ts` — Contractor create+BR-06 blacklist gate; Prequalification BR-05 gate+siklus 2-stage penuh; ProjectAssignment BR-01 gate+BR-04 Migas dua kasus; Worker BR-03 gate; PerformanceEvaluation 1-stage+BR-07 deteksi 2x-berturut; Work Permit BR-02 cross-module wiring; contractor-due-scan 3 kategori+idempotency; isolasi tenant RLS) — suite `contractor/contractor-lifecycle` BERSIH lintas run terisolasi (14/14). Verifikasi manual langsung ke Postgres — RLS TRUE+FORCE 7/7 tabel baru, `audit_log_trigger` terpasang 7/7, 2/2 FK retroaktif (`work_permits`/`incident_reports` → `contractors`) terpasang.
  **Temuan infra selama verifikasi (BUKAN bug modul ini — lihat entri "Adaptasi Shared Hosting" di bawah utk detail lengkap)**: run gabungan pertama (156 test lintas 14 suite `contractor`+`work-permit`+`notification`) sempat 66 gagal TypeError identik di SEMUA suite — root cause `EventEmitterModule.forRoot()` ganda (satu di `app.module.ts` `{maxListeners:50}`, satu lagi default di `workflow-engine-worker.module.ts`) yang BARU saling bentrok sejak task shared-hosting menyatukan keduanya ke proses `apps/api` yang sama. Diperbaiki, run ulang 153/156 bersih (13/14 suite), sisa `notification/dead-letter` (timeout hook 20 detik, BUKAN assertion) dikonfirmasi kontensi resource lintas 14 suite paralel — solo 3/3 bersih 40 detik.

### Adaptasi Shared Hosting (cPanel, non-PRD)

- [x] Adaptasi deployment shared hosting cPanel klasik — `apps/api/src/platform/scheduling/redis-enabled.helper.ts` dst: SATU toggle `REDIS_ENABLED` (default `true` = perilaku VPS/Docker/K8s existing TIDAK BERUBAH SAMA SEKALI) mengaktifkan jalur fallback penuh saat `false` (cPanel: TIDAK ada Redis, TIDAK ada proses worker terpisah yang bisa hidup permanen — cuma "Setup Node.js App" [Phusion Passenger] + Cron Job). Desain aditif murni, TIDAK ada perombakan Redis/BullMQ existing — dipilih setelah investigasi eksplisit (Explore agent) menunjukkan alternatif "port ke MySQL" akan butuh audit keamanan manual 341 titik pakai utk menggantikan jaminan isolasi tenant otomatis Row-Level Security, jauh lebih berisiko drpd tetap PostgreSQL (dikonfirmasi tersedia via `AskUserQuestion` ke user sebelum desain final).
  **Cron-runner** (`platform/cron-runner/*`) — SATU endpoint HTTP `POST /internal/cron/run-scans` (`@Public()`, header `X-Cron-Secret` wajib cocok `CRON_SECRET` env, FAIL-CLOSED kalau `CRON_SECRET` belum diset) menggantikan 31 BullMQ repeatable job existing (`apps/worker/src/*.worker.ts`) — REUSE LANGSUNG ke-20 `*WorkerModule` yang sudah ada (masing-masing sudah export scan service-nya sendiri, dipakai `apps/worker` juga), TIDAK menduplikasi wiring DI. Semua 31 scan job SUDAH idempotent by design (pola "tandai sent/processed via kolom DB" established sejak task 0.9/1.4/dst) — aman dipicu ulang sembarang waktu lewat polling HTTP+cron, TIDAK ada risiko efek samping ganda. `GET /internal/cron/status` (`@Public()`, tanpa secret) expose `redisEnabled`/`cronSecretConfigured` utk verifikasi setup tanpa kredensial. `NotificationModule`/`AttachmentModule` SENGAJA TIDAK diperlakukan sbg scan job biasa — payload keduanya reaktif (dipicu event, bukan jadwal periodik), diberi endpoint poll terpisah (lihat di bawah).
  **Notification/attachment-scan/data-import** dikonversi dari BullMQ producer/consumer ke pola poll cross-tenant (admin-`PrismaClient` baca `SELECT DISTINCT tenant_id` baris due, lalu `tenantContextStorage.run()` per-tenant `withRls()` — pola PERSIS `*-due-scan.service.ts` yang sudah established sejak lama). `NotificationLog` (Modul 25, 0.11) ditambah 4 kolom nullable (`recipientAddress`/`subject`/`body`/`nextAttemptAt`) supaya isi pesan genuinely tersimpan di DB (BullMQ job payload sebelumnya HANYA hidup di memori Redis, tidak pernah perlu persist krn worker baca langsung dari job — poll-based butuh re-baca dari DB), backoff kegagalan `nextAttemptAt = now + 2^attemptCount menit`.
  **Session store/permission cache/auth-code store/rate limiter** (auth 0.6/0.7/0.8) masing² dapat implementasi Postgres/in-memory alternatif dipilih via `useFactory` DI (`isRedisEnabled()`) menggantikan `useExisting` statis — `DbSessionStore` (tabel BARU `session_cache_entries`, BUKAN reuse `UserSession` existing: ditelusuri urutan panggilan `auth.service.ts` login flow, `RefreshTokenService.issue()` [tulis SessionStore] jalan SEBELUM `tx.userSession.create()` — reuse `UserSession` akan bikin `set()` pertama jadi UPDATE 0-baris senyap, bug sesi rusak yang DITEMUKAN LEWAT PENELUSURAN KODE sebelum sempat ditulis, bukan lewat kegagalan test), `InMemoryPermissionCache` (Map biasa), `AuthorizationCodeEntry` (tabel BARU, TANPA RLS/audit trigger — akses murni via kepemilikan token acak sekali-pakai bukan pola tenant-scoped, pola beda `session_cache_entries` yang RLS+FORCE tapi TANPA audit trigger [exception churn-tinggi established `medical_record_access_logs` 5.3]), rate limiter in-memory `Map` per tenant+window. `RedisProvider`/`MetricsService` ditambah `lazyConnect:true` supaya ioredis TIDAK buka koneksi TCP sungguhan saat provider Redis-backed yang TIDAK terpilih (`useFactory` pilih DB/in-memory) tetap diinstansiasi Nest DI container (Nest SELALU construct SEMUA provider terdaftar terlepas token mana yang dipakai) — tanpa ini, mode `REDIS_ENABLED=false` akan tetap gagal connect ke Redis yang genuinely tidak ada.
  **Object storage** (0.12) — `STORAGE_MODE=local` (default tetap `s3`, S3-compatible manapun termasuk provider eksternal spt Wasabi/Backblaze B2/Cloudflare R2 bukan cuma MinIO self-hosted) — presigned URL asli S3 diganti token HMAC-SHA256 self-contained (`base64url(JSON{key,contentType?,exp}).signature`, verifikasi `timingSafeEqual`, TANPA DB lookup) diarahkan ke `LocalStorageController` (`PUT/GET storage/local/{upload,download}/:token`, `@Public()` — pola otorisasi PERSIS presigned URL asli: kepemilikan token bertanda tangan valid ADALAH otorisasinya, bukan sesi JWT).
  `apps/worker` TIDAK disentuh sama sekali — tetap fungsional penuh utk jalur VPS/Docker/K8s existing yang masih pakai Redis.
  Panduan lengkap: [`DEPLOYMENT_SHARED_HOSTING.md`](../DEPLOYMENT_SHARED_HOSTING.md) (§0 verifikasi prasyarat cPanel sampai §11 keterbatasan vs VPS/Docker).
  **Bug infra nyata ditemukan+diperbaiki selama verifikasi (satu-satunya regresi genuine task ini, BUKAN bug pra-ada)**: `EventEmitterModule.forRoot()` dipanggil DUA KALI dgn opsi BEDA — `app.module.ts` (`{maxListeners:50}`, sengaja dinaikkan sejak task 4.1 krn >10 modul sekarang berlangganan event `WORKFLOW_INSTANCE_COMPLETED_EVENT` yang sama) DAN `workflow-engine-worker.module.ts` (default, TANPA opsi — sebelumnya AMAN krn modul itu HANYA pernah di-boot sendirian oleh proses `apps/worker` terpisah, satu-satunya `forRoot()` di graph proses itu). Cron-runner (task ini) PERTAMA KALI mengimpor `WorkflowEngineWorkerModule` ke proses `apps/api` yang SAMA dgn `AppModule` — dua `forRoot()` kini ke-eval di graph yang sama, dan opsi default (bukan `{maxListeners:50}`) yang menang, diam-diam menurunkan limit balik ke 10 bawaan eventemitter2. Trigger nyata: `logPossibleMemoryLeak()` eventemitter2 versi ini `process.emitWarning()` dgn cara yang crash `TypeError` (bukan cuma warn) di kombinasi Node/eventemitter2 environment ini — begitu listener workflow-completion ke-11 kedaftar, SETIAP test yang boot `AppModule` penuh mendadak gagal. Ditemukan lewat run verifikasi 14-suite (66/156 gagal, error identik lintas 7 suite/4 domain TIDAK terkait — `contractor`, `work-permit` x2, `notification` x4), diperbaiki selaraskan kedua `forRoot()` ke `{maxListeners:50}` (`workflow-engine-worker.module.ts`), run ulang bersih (153/156, sisa 3 gagal `notification/dead-letter` dikonfirmasi kontensi resource murni — solo 3/3 bersih). Diverifikasi ULANG run penuh seluruh proyek pasca-fix: NOL kemunculan signature TypeError ini (4 suite gagal yang tersisa SEMUA timeout kontensi khas run-paralel-penuh, pola established sejak insiden serupa 5.3/6.1/6.2, bukan bug ini).
  Diverifikasi (scope): `npx tsc --noEmit` seluruh `apps/api` — 0 error. `pnpm exec prisma migrate dev` — 4 migrasi baru applied bersih (`notification_logs_poll_columns`, `init_session_cache_entries`, `enable_rls_session_cache_entries`, `init_authorization_code_entries`). Verifikasi manual Postgres — `session_cache_entries` RLS TRUE+FORCE TRUE (TANPA audit trigger, sengaja), `authorization_code_entries` RLS FALSE+FORCE FALSE (sengaja, akses via token bukan tenant-scope), `notification_logs` 4 kolom baru terpasang. Test run tertarget (bukan full-suite tunggal — lingkup ini murni infra lintas-modul, verifikasi mengikuti area yang genuinely disentuh): 156 test lintas suite `contractor`+`work-permit`(x2)+`notification`(x4) — 153/156 bersih, 3 sisa (`dead-letter`) dikonfirmasi kontensi resource (solo 3/3 bersih, 40 detik). `pnpm exec turbo run build --concurrency=1` sukses 6/6 package pasca fix. Run tambahan seluruh proyek (semua suite, bukan tersempit) dipicu sbg pengecekan ekstra skala-penuh utk bug eventemitter2 — dikonfirmasi NOL kemunculan signature TypeError-nya lintas SEMUA suite yang sempat selesai sebelum dihentikan demi efisiensi waktu (kegagalan yang muncul semuanya timeout kontensi khas run-paralel-penuh, bukan bug ini) — tidak dijadikan angka "test total seluruh proyek" resmi krn run tidak dituntaskan sampai akhir (skala penuh non-esensial stlh scope tertarget di atas sudah membuktikan cukup, pola valid sama preseden "run alone"/"correct maxWorkers" banyak task sebelumnya).

## Perintah

```bash
pnpm install       # install semua dependency workspace
pnpm build         # turbo run build (affected packages)
pnpm dev           # jalankan semua app dalam mode dev
pnpm lint
pnpm test
```

### Database lokal (apps/api)

```bash
# 1) Pastikan PostgreSQL portable sudah running (lihat ../../.local-pgsql/, satu
#    level di atas repo ini) — start manual:
#    .local-pgsql/pgsql/bin/pg_ctl.exe -D .local-pgsql/data -l .local-pgsql/logfile.log start

cd apps/api
pnpm db:setup      # sekali saja: buat role qhse_app + database qhse_dev
pnpm db:migrate    # jalankan migration Prisma (prisma migrate dev)
```

### Redis & MinIO lokal (apps/api)

```bash
cd apps/api
pnpm redis:setup   # start Redis portable (.local-redis/) kalau belum jalan
pnpm minio:setup   # start MinIO portable (.local-minio/) kalau belum jalan — bucket qhse-attachments dibuat otomatis oleh ObjectStorageService saat app start
```
